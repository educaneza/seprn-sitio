# Bitácora de sesiones — SEPRN Sitio Web

Registro cronológico (más reciente arriba) de qué se hizo en cada sesión de trabajo y por qué.
Complementa, no duplica, a los demás docs:

- **Este archivo** responde "¿qué pasó y cuándo?" — historia, no estado actual ni planes.
- `docs/ROADMAP.md` responde "¿qué falta?" — solo pendientes genuinamente futuros.
- `docs/ARCHITECTURE.md` responde "¿cómo está construido?" — modelo mental vigente, sin fechas.
- `CLAUDE.md` responde "¿cómo trabajo en este proyecto?" — convenciones y guía operativa.

Se actualiza automáticamente al cerrar sesión vía el skill `/close`
(`.claude/skills/close/SKILL.md`) — ver la tabla "Ritual de cierre de sesión" en `CLAUDE.md`
para qué otro documento tocar además de este.

---

## CHECKPOINT — 2026-09-24 (cont. 3) · Plan aprobado: Formación Docente, Soporte y Correo alimentan la Bitácora OTDE (solo planeación, sin código)

| | |
|---|---|
| **Fecha** | 2026-09-24 |
| **Sesión** | Antes de la Fase 2 (oficios), Jorge pidió una estrategia para que los cursos de Formación Docente, Soporte remoto y las solicitudes de Correo institucional también generen su parte del reporte mensual sin captura manual. |
| **Revisado en el código** | Formación Docente tiene lo necesario en `Cursos`/`Inscripciones`, pero no tiene endpoint con token ni un vínculo con la planeación. Soporte **no guarda la fecha de resolución** (`sopOnEditCierre` solo anota "Sí"). Correo tiene `Fecha de entrega` en 4 de sus 5 hojas; falta revisar Incidencias. |
| **Estrategia** | Mismo pull con `PANEL_TOKEN` que Mantenimiento y Asesorías. Por el límite de 17 filas por meta: eventos → **una fila por evento** (Formación Docente); trámites de alto volumen → **una fila de resumen mensual** (Soporte, Correo). |
| **Decisiones de Jorge** | Aceptó las recomendaciones: Soporte en **META 23** y Correo en **META 25**; un curso de varios meses va **en cada mes** con desarrollo; Soporte y Correo como resumen mensual; todo **antes** de la Fase 2. Orden: A Formación Docente (columna `NP_planeacion` en `Cursos`), B Soporte (columna `Fecha de atención` llenada al cerrar), C Correo. |
| **Duda resuelta** | Jorge preguntó si puede editar la planeación directo en el Sheet y agregar acciones. Sí: `Planeacion` manda y se lee en cada consulta, y el formulario no la guarda en el navegador. Cuidados: N.P. único y meta 23/25; "Nombre corto" a mano para acciones nuevas; no insertar ni reordenar columnas antes de "Nombre corto" (`bitLeerPlaneacion_` las lee por posición); no renumerar los N.P. 7 y 8 (la alimentación automática los busca por número). |
| **Documentación actualizada** | Este checkpoint, `docs/PLAN-OPERACION-INTERNA.md` (sección nueva con el plan completo y las reglas para editar la planeación), `docs/ROADMAP.md` ítem 27, `docs/ARCHITECTURE.md §26` (reglas de edición de `Planeacion`), `CLAUDE.md` (sección de `bitacora.gs` y pendientes). |
| **Commits** | Solo el commit de cierre. |

---

## CHECKPOINT — 2026-09-24 (cont. 2) · Bitácora OTDE: las asesorías resueltas llegan solas (META 25, N.P. 8), desplegado; falta el primer caso real

| | |
|---|---|
| **Fecha** | 2026-09-24 |
| **Sesión** | Segundo paso de la alimentación automática, con el mismo patrón que Mantenimiento: las asesorías con Estatus "Resuelto" de `asesorias.gs` se vuelven actividades de la bitácora. |
| **Hallazgo** | Asesorías no tiene nada equivalente al reporte de visita: al marcar "Resuelto" no quedaba registrado cuándo se dio la asesoría ni cuántas personas asistieron (solo "Fecha programada de visita" y el "Número de Docentes" solicitado). Revisada en vivo, la hoja real tiene una sola solicitud (`OTDE-ASE-0001`) en "Pendiente de validar". Tampoco hay ejemplo de asesoría en el Excel entregado, así que el texto se armó a partir de la acción N.P. 8 de la planeación. |
| **Decisiones de Jorge** | (1) **Dos columnas que llena Nancy** al marcar "Resuelto": *Fecha de realización* y *Asistentes*. Si faltan, se usa la fecha programada y el número solicitado, y la bitácora avisa. Se descartó un formulario post-asesoría por ser más trabajo. (2) **La asesoría de IA (N.P. 9) no entra por ahora:** el formulario solo tiene Banco de Materiales y Chuka y Excel básico, que corresponden al N.P. 8. |
| **Construido** | `asesorias.gs`: columnas X/Y (`COL_ASE_FECHA_REALIZACION`/`COL_ASE_ASISTENTES`) con validación suave de fecha y número, creadas por "Aplicar validación y semáforo"; endpoint de solo lectura `?action=asesoriasMes` (`aseListarResueltasMes_`, `PANEL_TOKEN`). `bitacora.gs`: menús "Traer asesorías resueltas" y "Configurar conexión con Asesorías" (`ASE_URL`; reutiliza el `PANEL_TOKEN` guardado). `bitImportarAsesorias_` crea una actividad por folio (`ASE:<folio>`) con `BIT_TIPO_ASE_BANCO`/`BIT_TIPO_ASE_EXCEL`, descripción (`bitAseDescripcion_`, incluye los temas de Excel) y beneficiarios (`bitAseBeneficiarios_`). "Generar reporte del mes" trae Mantenimiento y Asesorías. La importación de Mantenimiento se reorganizó para compartir conexión, consulta y alta de filas (`bitConfigurarConexion_`, `bitConsultarBackend_`, `bitAgregarActividades_`) sin cambiar su comportamiento. |
| **Verificación** | Local: `node --check` de los dos archivos y prueba con hoja simulada en Node (1 visita + 2 asesorías en la primera corrida, 0 nuevas en la segunda; aviso "sin fecha de realización ni asistentes" cuando faltan). Producción: Jorge desplegó los dos proyectos; "Traer asesorías resueltas" devuelve 0 nuevas sin error, y las columnas X/Y aparecen en la hoja real. **Falta** importar un caso real, cuando haya una asesoría en "Resuelto". |
| **Documentación actualizada** | Este checkpoint, `docs/ARCHITECTURE.md` §15 y §26, `docs/ROADMAP.md` ítem 27, `docs/PLAN-OPERACION-INTERNA.md`, `CLAUDE.md` (secciones de `asesorias.gs` y `bitacora.gs`, pendientes). |
| **Commits** | `4bc68b7` y el commit de cierre. |

---

## CHECKPOINT — 2026-09-24 (cont.) · Bitácora OTDE: las visitas de Mantenimiento llegan solas (META 23, N.P. 7), en producción y verificada

| | |
|---|---|
| **Fecha** | 2026-09-24 |
| **Sesión** | Primer paso de la alimentación automática recomendada en el checkpoint anterior: quitar la doble captura de las visitas de Mantenimiento, que el técnico ya registra en `reporte-visita.html` y que Jorge o Nancy volvían a teclear en `bitacora.html`. |
| **Referencia** | La pestaña META 23 del Excel entregado reporta **una fila por escuela visitada**, con la descripción de equipos, software y "Equipos totales: N" y Beneficiarios en la forma "1 Director Escolar / 12 docentes / 292 alumnos". Ningún sistema guardaba docentes ni alumnos. |
| **Decisiones de Jorge** | (1) **La bitácora jala los datos** (pull), no Mantenimiento los empuja: el envío del técnico, que ya es lento (PDF, fotos, correo), no se toca, y se traen también las visitas anteriores. (2) El técnico captura **docentes y alumnos de la escuela**. (3) En el reporte, la acción se presenta como **rehabilitación del Aula de Medios**; la ejecución sigue siendo el mantenimiento. Se conserva "mantenimiento preventivo y correctivo" porque es lo que dice el N.P. 7, y las visitas solo administrativas mantienen el texto de mantenimiento. |
| **Construido** | `mantenimiento.gs`: endpoint de solo lectura `?action=reportesMes&mes=AAAA-MM&token=…` (`manListarReportesMes_`, mismo `PANEL_TOKEN` del Panel OTDE) que cruza "Reportes de visita" con `Solicitudes`; columnas nuevas AB/AC "Docentes de la escuela"/"Alumnos de la escuela" (`COL_MAN_REP_DOCENTES`/`COL_MAN_REP_ALUMNOS`), obligatorias y que aceptan 0 (`manEnteroNoNegativo_`). `reporte-visita.html`: los 2 campos numéricos en "Aula al llegar". `bitacora.gs`: menús "Traer visitas de Mantenimiento" (`bitTraerMantenimiento`) y "Configurar conexión con Mantenimiento" (`MAN_URL` + `PANEL_TOKEN` en cuadros de diálogo); `bitImportarMantenimiento_` agrupa por folio y mes (dos días = rango de fechas), usa `ID de envío` `MAN:<folio>:<mes>` y no toca filas que ya existen; textos en `bitManSede_`/`bitManDescripcion_`/`bitManBeneficiarios_`; "Generar reporte del mes" trae las visitas antes de armar el reporte y, si falla, genera el reporte igual y avisa. |
| **Despliegue** | Primero `reporte-visita.html` (GitHub Pages lo sirvió en ~150 s), después `mantenimiento.gs` y `bitacora.gs`: al revés, el formulario viejo no manda docentes ni alumnos y el backend nuevo rechazaría los reportes. Jorge desplegó y configuró la conexión. |
| **Verificación** | Local: `node --check` de los 3 archivos; funciones de texto probadas en Node (dos visitas del mismo folio en días seguidos → "Los días 21 y 22…", visita a una jefatura → texto administrativo); campos nuevos probados en Playwright (vacío marca error; 12 y 0 pasan). Producción: la primera corrida trajo **4 visitas** de septiembre (BIT-0006 a 0009: `OTDE-MAN-0015`, `0023`, `0001`, `0002`) y **la segunda, 0 nuevas · 4 ya estaban**. Filas revisadas en Chrome: lugar, descripción y propósito salen con el formato esperado. Se corrigió el singular ("se atendió 1 equipo"), que salió mal en BIT-0006. Los reportes de prueba (`OTDE-MAN-0014`/`0016`) no llegaron. |
| **Queda para Jorge** | Confirmar que `OTDE-MAN-0001`/`0002` son visitas reales; poner a mano las cifras de Beneficiarios de las 4 filas (son anteriores a los campos nuevos); corregir BIT-0006 y quitar la frase repetida de equipos administrativos en BIT-0008; pegar el `bitacora.gs` con el arreglo del singular (Nueva versión). |
| **Documentación actualizada** | Este checkpoint, `docs/ARCHITECTURE.md` §15 y §26, `docs/ROADMAP.md` ítem 27, `docs/PLAN-OPERACION-INTERNA.md`, `CLAUDE.md` (secciones de `mantenimiento.gs` y `bitacora.gs`, pendientes). |
| **Commits** | `8634c50` (alimentación desde Mantenimiento), `8408e8c` (singular) y el commit de cierre. |

---

## CHECKPOINT — 2026-09-24 · Operación interna OTDE, Fase 1: bitácora única y reporte mensual (construida, desplegada y validada con datos reales)

| | |
|---|---|
| **Fecha** | 2026-09-24 |
| **Sesión** | Arranque de la Fase 1 del plan del 23 sep, con los insumos reales que trajo Jorge en `docs/insumos/`: el Word de planeación institucional 2026-2027, la plantilla en blanco del reporte mensual y un reporte ya entregado. La carpeta queda en `.gitignore` porque son documentos internos y el sitio es público. |
| **Hallazgo que cambió el plan** | El reporte mensual **no es un Word**: es un **Excel en SharePoint** compartido con la Oficina de Planeación, un archivo por mes, con **una pestaña por meta**. OTDE solo usa META 23 (mantenimiento) y META 25 (lo demás), con filas de actividad de la 14 a la 30 y 7 campos en celdas combinadas (A:B, C, D, E, F:I, J:K, L). No hay rubro de "no planeadas" y la evidencia es un PDF por mes. La planeación tiene 9 acciones con su meta, y esa columna es la que conecta los dos documentos. Apps Script no escribe en SharePoint, así que se descartó la plantilla de Google Docs: la salida es una pestaña de Sheets con las mismas columnas y combinaciones, para copiar y pegar. |
| **Construido** | `apps-script/bitacora.gs` (601 líneas): hojas `Planeacion` (sembrada con las 9 acciones), `Actividades` y `Config`; clave de captura `CLAVE_CAPTURA`, configurada desde el menú con un cuadro de diálogo; la meta de una actividad planeada la decide la hoja, no el navegador; `ID de envío` + `LockService` para no duplicar en reintentos; texto de fecha al estilo del reporte (`bitFechaTexto_`); menú "Generar reporte del mes" (`bitGenerarReporteMensual`), que crea la pestaña "Reporte AAAA-MM" con bloques META 23/25, aviso si pasa de 17 filas y avance por N.P. solo para uso interno. `bitacora.html` (816 líneas): formulario móvil `noindex` con borradores de propósito, beneficiarios y responsable tomados de la planeación, y el lugar armado desde la CCT (`js/cct-db.js`) o desde el medio virtual. |
| **Ajustes tras el despliegue** | (1) Jorge no podía entrar: el backend ahora distingue `sin_clave` de `no_autorizado` y la página explica qué corregir. (2) En la captura real, dos guardados tardaron más de 30 s en regresar al navegador aunque Apps Script terminó en ~1 s (visto en el registro de ejecuciones); no hubo duplicados. El envío ahora espera 60 s (`BITACORA_TIMEOUT_ENVIO_MS`) y el reintento devuelve la confirmación completa. La causa del retraso no se identificó. (3) "Tipo y nombre" quitaba los saltos de línea; ahora es `<textarea>` (`docs/QA-NOTES.md` #43). (4) Las opciones de acción se cortaban en el celular: columna nueva "Nombre corto" en `Planeacion` (`bitAsegurarNombresCortos_`, la crea "Preparar hojas" sin pisar lo editado) y el texto completo debajo del select. |
| **Verificación** | Local: backend con hoja simulada en Node (fechas, clave, duplicados, meta forzada por la planeación, reporte ordenado por fecha y filtrado por mes) y formulario en Playwright con el backend interceptado, a 360 y 390 px, sin errores ni desplazamiento horizontal. Producción: las 5 actividades reales de septiembre se capturaron desde Chrome (BIT-0001 a BIT-0005, texto exacto del Excel entregado); se corrigieron a mano las 3 celdas sin salto de línea y se regeneró el reporte (META 23: 0 · META 25: 5). **Jorge pegó el bloque en el Excel real de SharePoint y funcionó bien.** Nombres cortos verificados en la página en producción. |
| **Siguiente paso** | Jorge preguntó si Mantenimiento, Asesorías, Soporte y lo demás de Oficina Virtual requieren doble captura: hoy sí. Se recomendó empezar la próxima sesión con la **alimentación automática**, primero `reporte-visita.html` → actividad en META 23, N.P. 7. Después Asesorías con estatus Resuelto → N.P. 8, y decidir qué hacer con Soporte y Correo, que no tienen acción en la planeación. La Fase 2 (oficios) queda después; necesita saber si hay Power Automate en M365 y uno o dos oficios reales. |
| **Documentación actualizada** | Este checkpoint, `docs/PLAN-OPERACION-INTERNA.md` (formato real y decisiones de construcción), `docs/ARCHITECTURE.md §26` (nuevo), `docs/ROADMAP.md` ítem 27, `docs/QA-NOTES.md` #43, `CLAUDE.md` (tabla de páginas, sección de backend y pendientes), `.gitignore`. |
| **Commits** | `b25271f` (Fase 1), `64b8c98` (sin clave), `ebe9657` (reintentos y 60 s), `a41a796` (textarea), `cdadb1c` (docs validada), `991d4b6` (nombres cortos) y el commit de cierre. |

---

## CHECKPOINT — 2026-09-23 (cont. 2) · Plan de operación interna OTDE: bitácora única, oficios, padrón de programas, agenda y tablero (solo planeación, sin código)

| | |
|---|---|
| **Fecha** | 2026-09-23 |
| **Sesión** | Jorge planteó que la operación interna de la oficina (oficios que llegan al Outlook institucional y se turnan; recordatorios de reuniones que hoy se hacen a mano por WhatsApp; padrones de Cuantrix, Internet en una caja, MIED, Internet CFE, UNETE y Red Digital IEEM en Excel sueltos; planeación institucional, agenda mensual en Outlook y reporte mensual en Word) sigue manual, y pidió un análisis y un plan. Sesión en modo plan, sin código. |
| **Diagnóstico** | Planeación, agenda, reporte mensual y seguimiento de oficios describen lo mismo: **actividades**, capturadas 3 o 4 veces. Propuesta: capturar cada actividad una sola vez y generar los documentos a partir de esa captura. Prioridad elegida por Jorge: el **reporte mensual**. |
| **Plan acordado** | 5 fases en `docs/PLAN-OPERACION-INTERNA.md`: (1) Bitácora única + reporte mensual automático, ligada a los N.P. de la planeación; (2) Control de oficios con recordatorios por Telegram y el texto de WhatsApp ya redactado; (3) Padrón de programas por CCT + consulta móvil; (4) `.ics` mensual para Outlook; (5) Tablero. Límites anotados: Apps Script no lee el buzón de Outlook (hay que verificar si hay Power Automate en la licencia M365), WhatsApp no tiene envío automático gratuito y la cuota de MailApp es compartida. |
| **Arquitectura** | Jorge preguntó si todo puede ser un solo sistema. Respuesta: uno solo para quien lo usa y separado por dentro. Se unifican el catálogo por CCT (`js/cct-db.js`), la bitácora y el tablero; cada trámite conserva su propio Sheet y su propio script. Coherente con la decisión del 5 ago. **Corrección durante el cierre**: el plan hablaba de un "Panel OTDE nuevo", pero ya existe `apps-script/panel-otde.gs` (`docs/ARCHITECTURE.md §20`); el plan se ajustó para ampliarlo con el mismo patrón de endpoint con token, sin crear un panel nuevo. |
| **Siguiente paso** | Jorge trae el Word de la planeación institucional 2026-2027 y el del reporte mensual (uno en blanco y uno ya entregado) para diseñar la estructura de `Actividades` y la plantilla del reporte antes de escribir código. |
| **Documentación actualizada** | Este checkpoint, `docs/PLAN-OPERACION-INTERNA.md` (nuevo), `docs/ROADMAP.md` ítem 27, `CLAUDE.md` §"Pendientes vigentes". |
| **Commits** | El commit de cierre de esta sesión (solo documentación). |

---

## CHECKPOINT — 2026-09-23 (cont.) · Formación Docente: fechas con etiqueta, cierre con hora, "Cupo agotado", historial sin `Activo` y rediseño de tarjetas

| | |
|---|---|
| **Fecha** | 2026-09-23 |
| **Sesión** | Feedback de producción de Jorge (catálogo vacío en ese momento): la tarjeta no mostraba cuándo se imparte el curso; hacía falta cerrar por cupo sin quitar el curso; cerrar la inscripción a una hora exacta; mejorar el diseño de las tarjetas. En el camino pidió que "Cursos anteriores" muestre los 6 más recientes. Plan en modo plan, por pasos. |
| **Hallazgos** | (1) La tarjeta mostraba `Fecha_inicio`–`Fecha_fin` sin etiqueta y `Fecha_limite_inscripcion` nunca llegaba al frontend. (2) `doPost` no rechazaba registros después de la fecha límite (solo cursos pasados). (3) El historial ya ordenaba por `Fecha_fin` descendente, pero filtraba `Activo=TRUE` antes, así que apagar un curso al terminar lo sacaba del historial. Ver `docs/QA-NOTES.md #42`. Descartado en la sesión: al principio se pensó que F/G se capturaban como fechas de inscripción; Jorge aclaró que son de desarrollo (coincide con el código). |
| **Backend** | Columnas nuevas en `Cursos`: AA `Hora_limite_inscripcion`, AB `Cupo_agotado`, AC `Ocultar_historial` (se autocompletan, con dropdown y notas en los encabezados vía "Aplicar validación en Cursos"). `momentoCierreInscripcion_()` + `horaYMinutos_()`; `evaluarEstadoCurso_(row, ahora)` con 4 estados. `doPost` rechaza registros nuevos en cerrada/agotada (excepción: agotada + registro externo confirmado), antes de escribir. Historial sin `Activo`. Recordatorio a pendientes solo en `abierta`, con la hora de cierre en el correo. |
| **Frontend** | Tarjetas con la cabecera de color (opción elegida por Jorge entre dos mockups) pero más baja, con chip de estado; fechas con etiqueta ("Inscríbete hasta" / "Curso"); botón visual "Elegir este curso" → "✓ Elegido"; tarjeta agotada elegible solo en modo "ya me inscribí"; cierre en vivo cada 30 s y revisión justo antes de enviar. Ajuste posterior: "En desarrollo" solo si el curso ya empezó (`8b2fb23`). |
| **Verificación** | Node con stubs de Apps Script: 9 casos de estado (hora como celda y como texto, hora ilegible → fail-open, agotada vs. cerrada, sin fecha límite, "Por definir"). Chrome contra un mock local: 5 tarjetas + historial, selección, agotado en modo aviso, cierre en vivo en catálogo y en formulario, sin errores de consola, sin desborde a 375px. Producción (Jorge pegó el `.gs` y creó la versión): `doGet` con los campos nuevos; historial ahora con `CNF-2627-001` (133 inscritos, `Activo=FALSE`); fila de prueba `PRUEBA-QA-002` → hora leída como 12:25 CDMX (18:25Z), `agotada` + POST rechazado, cierre a las 12:00 → `cerrada` + POST rechazado; tarjeta real en GitHub Pages con "Inscripción cerró mié 23 sep · 12:00 h". La fila de prueba quedó con `Activo=FALSE`/`Ocultar_historial=TRUE`, sin borrar (Jorge la borra). |
| **Duda de `ACF-2627-001` resuelta** | Leída la hoja real: las 3 Acciones Formativas tienen `Visible_hasta` vencido (se usaba como cierre de inscripción) y `ACF-2627-001` no tiene `Fecha_fin`. Recomendación a Jorge: cerrar con `Fecha_limite_inscripcion` (+ hora) y llenar `Fecha_fin`. |
| **Documentación actualizada** | Este checkpoint, `docs/ARCHITECTURE.md` §12, `docs/DESIGN_SYSTEM.md`, `docs/QA-NOTES.md #42`, `docs/ROADMAP.md` ítem 18, `docs/manual-formacion-docente.html` (columnas de `Cursos`), `CLAUDE.md`. |
| **Commits** | `ece90e3` (backend + frontend, 2 archivos, +432/−93 junto con el siguiente), `8b2fb23` (texto "En desarrollo"), y el commit de cierre de esta sesión. |

---

## CHECKPOINT — 2026-09-23 · Recordatorios de constancia UNETE sin salir — cuota, no activadores; fix de "Último aviso" pegado

| | |
|---|---|
| **Fecha** | 2026-09-23 |
| **Sesión** | Jorge reportó que los recordatorios de constancia no salieron entre 9 y 10 am. Diagnóstico en modo plan, en vivo desde "Mis ejecuciones" (solo lectura). |
| **Hallazgos en vivo** | Activadores sanos: `enviarRecordatoriosWebinar` cada 15 min sin errores y `enviarRecordatoriosDiarios` a las 9:39 ("0 enviado(s), 0 pospuesto(s)"). Constancias: "0 enviado(s), 133 pospuesto(s)" en cada corrida desde las 12:07 am, porque la cuota sigue a la reserva (≤ 30) por los envíos del 22 sep. Los recordatorios de constancia no dependen del activador de las 9am. El formulario sigue abierto hasta el 28 sep. **Ningún docente ha recibido todavía un recordatorio de constancia**: la lista para el CCO manual del 22 sep se preparó, pero Jorge no la envió. |
| **Fix** | `docs/QA-NOTES.md #41`: el recordatorio 2 ya no sale el mismo día que el 1 (antes, si el 1 salía tarde, el "Último aviso" llegaba 15 min después), y se deja de consultar la cuota por cada folio una vez que llega a la reserva. Probado en Node con datos simulados (5 casos). |
| **Producción** | Código cargado en el editor del proyecto real (diff previo: editor = repo, salvo este cambio). El modo automático bloqueó el guardado en producción; **Jorge lo guardó a mano el mismo día** (repo = producción). No hace falta "Nueva versión" ni reinstalar los activadores (los activadores corren el código guardado y `doGet`/`doPost` no cambiaron). |
| **Documentación actualizada** | Este checkpoint, `docs/QA-NOTES.md #41`, `docs/ARCHITECTURE.md` §12 (regla del recordatorio 2). |
| **Commits** | Commit de cierre de esta sesión (`.gs` = producción + docs). |

---

## CHECKPOINT — 2026-09-22 (cont. 3) · Chequeo de salud de Formación Docente — Brevo descartado, repo = producción (Versión 22)

| | |
|---|---|
| **Fecha** | 2026-09-22 |
| **Sesión** | Jorge pidió revisar la salud de Formación Docente tras el incidente del día (perdió el hilo entre Brevo, Workspace y los fixes). Revisión en vivo vía Chrome, solo lectura primero. |
| **Hallazgos en vivo** | Activadores bien; modo de prueba apagado; funciones temporales borradas. Cada 15 min los 3 lotes de `CNF-2627-001` fallaban (cuota + `Invalid email`) con la ejecución "Completada". Constancias: 0/133 recibidas, 115 recordatorios pospuestos, formulario cerraba el 23 sep. Catálogo público vacío por datos (CNF y `ACF-2627-003` con Activo=FALSE, a propósito; `ACF-2627-002` cerró el 16 sep), no un bug. Workspace en prueba (cobro desde el 6 oct), pero la doc oficial dice que las cuentas de prueba tienen límites extra hasta USD $100 pagados + 60 días. Repo ≠ producción (repo tenía Brevo sin desplegar). |
| **Versión 21** | Editado directo en el editor (a pedido de Jorge): `DIAS_LIMITE_CONSTANCIA` 2→7, `RESERVA_CUOTA_CORREO` 20→30 (Jorge: 25-30 libres para los demás sistemas) y revisión de cuota+reserva por lote, activador diario 8am→9am (reinstalado desde el menú, verificado 9-10am GMT-6). |
| **Versión 22** | Brevo retirado por completo del código; regex estricta; envío parcial sin duplicar (`claveSeguimiento`); corte del "ya comenzó" al terminar el evento. Cargado al editor desde el archivo del repo y verificado con un diff línea por línea contra la V21 (solo cambios previstos) — **repo y producción idénticos**. Ver `docs/QA-NOTES.md #40`. |
| **Versión 23** | Tope del seguimiento de envío parcial (~630 destinatarios por aviso): si la lista ya no cabe en la Script Property, se detiene sin mandar lotes que no pueda anotar, se da por concluido y avisa a Jorge — antes, pasar ese tope habría reenviado el mismo lote en cada corrida. `BREVO_API_KEY` borrada de Script Properties. Jorge corrigió a mano `vero_130171@hotmail.com` en `Docentes`. |
| **Pendiente** | Decidir Workspace antes del 6 oct (ROADMAP 26). |
| **Verificación** | Simulación en Node con mocks de `MailApp`/`PropertiesService` (17 casos: regex, 133 inscritos con reserva → 45/día sin duplicados, lote con error, modo de prueba, curso de 800 → se detiene en 630 sin duplicar y alerta). En vivo: diff línea por línea editor vs. repo antes de cada versión; corridas de `enviarRecordatoriosWebinar` de 22:37, 23:07 y 23:37 sin errores; `CNF-2627-001` quedó con `Recordatorio_webinar_enviado=TRUE` al pasar su `Hora_fin`; `doGet` responde igual. |
| **Documentación actualizada** | Este checkpoint, `docs/QA-NOTES.md #40`, `docs/ROADMAP.md` 25/26, `docs/ARCHITECTURE.md` §12 (envío en lote, reserva 30, ventana de constancia 7 días), `CLAUDE.md`. |
| **Commits** | Commit de cierre de esta sesión (`.gs` = Versión 23 de producción + docs). |

---

## CHECKPOINT — 2026-09-22 (cont. 2) · Brevo tiene un bloqueador real con Hotmail/Outlook — se optó por probar Google Workspace en su lugar

| | |
|---|---|
| **Fecha** | 2026-09-22 |
| **Sesión** | Continuación del incidente de la conferencia UNETE. Con la migración a Brevo ya escrita (checkpoint anterior), Jorge pidió una salida inmediata para avisar a los pendientes de constancia mientras se decidía el resto — y al revisar el panel de Brevo apareció un problema que no se había visto antes. |
| **Salida manual para constancias (resuelto)** | Se generó en vivo (función temporal `chequeoTemporalListaConstancia()`, eliminada después) la lista de docentes de `CNF-2627-001` con `Constancia_recibida` aún vacío: **133 correos** (subió de 119 porque las inscripciones seguían abiertas). Se le entregó a Jorge lista para pegar en CCO de un correo manual desde Gmail — ese envío usa el límite normal de Gmail (~500/día), no la cuota de `MailApp` de Apps Script, y no toca nada del sistema automático. Hallazgo de paso: `vero_130171@hotmail.com` tiene un **punto extra al final** (`...hotmail.com.`) — esa es la causa real de su rechazo, no un carácter invisible como se pensó antes; pendiente corregirlo a mano en `Docentes`. |
| **Bloqueador real encontrado en Brevo** | El panel de Brevo (Remitentes, dominio, IP) advierte en su propio texto: sin dominio propio autenticado con DKIM/DMARC, **"todos los emails enviados a destinatarios de Microsoft (@outlook.com, @hotmail.com, @live.com) se marcarán como spam o se rechazarán"**, y Brevo puede sustituir el remitente sin avisar. Con `@gmail.com` como remitente (sin dominio propio), esto compromete justo a la mitad de los destinatarios reales de OTDE (varios `@hotmail.com`/`@outlook.com` ya vistos en los 133 de arriba). |
| **`aulia.mx` (dominio personal de Jorge) evaluado y descartado** | Jorge preguntó si podía usar su dominio personal independiente para resolver el DKIM/DMARC de Brevo. Se recomendó explícitamente que no: mezclaría reputación de envío entre un proyecto personal y correo institucional, expondría a Jorge a preguntas de auditoría si algún día se descubre, y rompe la separación que él mismo mantiene a propósito entre `aulia.mx` y su trabajo — mismo principio que ya se había aplicado antes para descartar `aulia.mx` como remitente de Brevo. |
| **Alternativa elegida: Google Workspace Business Starter (prueba de 14 días)** | Investigado: cuota de `MailApp`/`GmailApp` para cuentas Workspace es **1,500/día** (vs. 100/día en una cuenta gratuita) — documentación oficial de cuotas de Apps Script. Precio real confirmado en la página de Google: MXN $151.20/mes (10% descuento, primeros 12 meses). La prueba de 14 días sí requiere tarjeta pero no cobra nada si se cancela antes de que termine. Jorge decidió **no comprar RFC de SEIEM** para el registro (perfil "Organización") por el mismo motivo que `aulia.mx` — pagándolo con tarjeta personal, lo correcto es registrar "Persona física" con su propio RFC, no mezclar la identidad fiscal de SEIEM con una transacción personal sin autorización institucional. |
| **Prueba activada, sin dominio** | Se usó el camino "Actualizar esta cuenta" (no "Obtener un nuevo dominio"), lo que permitió activar Business Starter sobre `otde.nezahualcoyotl@gmail.com` tal cual, sin comprar ni configurar ningún dominio. Confirmado en la Consola del administrador: "Tu servicio pagado comienza el 6 oct 2026" (coincide con 14 días desde hoy). Se ignoró a propósito el paso opcional de "Configurar correo electrónico personalizado" (pide dominio) navegando directo a `admin.google.com` en vez de completarlo. |
| **Verificación de cuota, resultado pendiente** | Corrida manual de `MailApp.getRemainingDailyQuota()` (función temporal `chequeoTemporalCuota2()`, eliminada después) **sigue reportando 20**, igual que antes de activar Workspace — no se ve todavía el salto a 1,500. Posibles causas sin confirmar: el contador de hoy pudo quedar atado al límite viejo (100/día) desde antes del upgrade, o la licencia nueva tarda en propagarse. **Pendiente: volver a correr la misma prueba después del reinicio de cuota de mañana (~1am hora de México) para confirmar si ya subió a 1,500.** |
| **Documentación actualizada** | Este checkpoint, `docs/ROADMAP.md` ítem 25 (actualizado con el estado de la prueba de Workspace). |
| **Commits** | Sin commit todavía — cambios en `apps-script/formacion-docente.gs` (migración a Brevo, sin desplegar) siguen en el working tree local; la prueba de Workspace es un cambio de cuenta/facturación, no de código. |

---

## CHECKPOINT — 2026-09-22 (cont.) · Causa de fondo era la cuota compartida de MailApp — migración a Brevo preparada, sin desplegar

| | |
|---|---|
| **Fecha** | 2026-09-22 |
| **Sesión** | Continuación del incidente de la conferencia UNETE (checkpoint anterior). Jorge volvió, se desplegó el segundo fix (`esEmailValido_()`), y al verificarlo con una corrida manual se descubrió que la causa de fondo no era ninguno de los dos bugs ya corregidos. |
| **Verificación en vivo del segundo fix** | Se pegó y redesplegó (Versión 20, descripción "Filtra correos inválidos y aisla cada lote"). Corrida manual de `enviarRecordatoriosWebinar()`: completó sin tronar, con los 3 lotes intentados de forma aislada — confirma que el aislamiento por lote funciona. |
| **Causa de fondo descubierta** | Los 3 lotes fallaron igual: 2 con `Service invoked too many times for one day: email.`, 1 con `Invalid email`. Se agregó temporalmente `chequeoTemporalCuota()` (eliminada después) para consultar `MailApp.getRemainingDailyQuota()` en vivo: **20**, no 0. Con 20 de cuota restante, un lote de 45 revienta de inmediato — confirma que la cuota de Apps Script se cuenta por **destinatario individual**, no por llamada a `sendEmail()` (un supuesto equivocado, tanto del código como de Jorge, "CCO = un solo envío"). Esa cuota (~100/día para una cuenta no-Workspace) la comparten **todas** las automatizaciones de OTDE en `otde.nezahualcoyotl@gmail.com`. |
| **Camino descartado: Google Workspace for Education** | Es gratis para instituciones acreditadas, pero requiere dominio propio verificado — `dee.edu.mx` ya está en Microsoft 365 (SIGEE, Microsoft Authenticator), controlado a nivel SEIEM/estatal, fuera del alcance de OTDE. |
| **Fix preparado: migración a Brevo** | 300 destinatarios/día gratis, sin dominio propio (remitente verificado por correo). Jorge se registró, generó una API key en Brevo y se guardó como Script Property `BREVO_API_KEY` (nunca pasó por el chat). Cambios en `apps-script/formacion-docente.gs`: `enviarPorBrevo_()` nueva (`UrlFetchApp.fetch()` a la API de Brevo), `enviarCorreoLote()`/`enviarCorreoIndividual_()` reescritas para usarla, quitado el chequeo de `MailApp.getRemainingDailyQuota()`/`RESERVA_CUOTA_CORREO`. `verificarActivadoresInstalados()` se dejó a propósito en `MailApp`. Detalle completo en `docs/QA-NOTES.md #39`. |
| **Estado** | **Escrito en el repo local, sin pegar/redesplegar y sin probar contra la API real de Brevo todavía** — pendiente confirmar remitente verificado en Brevo, redeploy, y prueba con modo de prueba activo. Ver `docs/ROADMAP.md` ítem 25 para el checklist. |
| **Documentación actualizada** | `docs/QA-NOTES.md #39`, `docs/ROADMAP.md` ítems 24 (resuelto) y 25 (nuevo), `CLAUDE.md` (sección del `.gs` y Pendientes vigentes), este checkpoint. |
| **Commits** | Sin commit todavía — cambios en `apps-script/formacion-docente.gs` en el working tree local, no comiteados. |

---

## CHECKPOINT — 2026-09-21 (cont. 5) · Tamaño del repo: cifras reales actualizadas, Git LFS descartado, umbrales de aviso proactivo

| | |
|---|---|
| **Fecha** | 2026-09-21 |
| **Sesión** | A raíz de la publicación de Primera Sesión Ordinaria (checkpoint anterior), Jorge preguntó por el pendiente ya anotado sobre el tamaño del repo (`docs/ROADMAP.md` ítem 10, ago 2026) y pidió revisar el tamaño actual del `.git`. Se le explicó qué es Git LFS a petición suya. Decidió dejar la decisión de fondo para después, pero pidió aviso proactivo — no solo una nota pasiva — en cuanto el repo se acerque a un límite real. |
| **Medición real** | Contenido publicado (`git ls-files`, sin `.git`): **514.4 MB**. Historial `.git`: **491 MB** (339 MB empaquetados + ~152 MB sueltos del commit recién hecho). Working tree completo: ~1.2 GB. Archivos más pesados: `cte-primera-sesión-completa.zip` (80 MB, nuevo esta sesión), `sexta-sesion-completa.zip` (69 MB), `presentacion-tema12.pptx` (53 MB), `2627_s1_doc1_cultura_de_paz.pdf` (33 MB, nuevo esta sesión) — todos en `pdfs/cte/`. |
| **Git LFS descartado como opción viable** | GitHub Pages no resuelve punteros de Git LFS al desplegar — serviría el archivo de puntero (texto) en vez del PDF/ZIP real a quien lo descargue desde `cte.html`. Es una limitación documentada de GitHub Pages, no configurable. Con el sitio sirviendo estos materiales directo desde Pages sin build step, la única ruta real para bajar el peso sigue siendo mover el material pesado fuera del repo (enlace externo, Drive). |
| **Umbrales de aviso agregados** (`docs/ROADMAP.md` ítem 10, `CLAUDE.md` regla 4) | Contenido publicado ≥ 800 MB (hoy 514 MB, ~51%); cualquier archivo nuevo ≥ 80 MB; un segundo timeout de deploy de GitHub Pages (el primero, ago 2026, se resolvió solo con un reintento). Cualquier sesión que agregue material pesado a `pdfs/` debe revisar estos umbrales y avisar a Jorge de inmediato si se cruzan, no esperar al cierre de sesión. |
| **Memoria persistente** | Se guardó `feedback_avisar_tamano_repo_seprn.md` en el sistema de memoria (fuera del repo) para que sesiones futuras recuerden avisar proactivamente aunque no estén revisando `docs/ROADMAP.md` en ese momento. |
| **Documentación actualizada** | `docs/ROADMAP.md` (ítem 10 reescrito con cifras reales y umbrales), `CLAUDE.md` (regla nueva bajo la regla 4, sobre revisar tamaño antes de comitear material pesado). |
| **Commits** | `5f2cc1c`, pusheado a `origin/main` a pedido explícito de Jorge. |

---

## CHECKPOINT — 2026-09-21 (cont. 4) · CTE: Opening de Fase Intensiva + nueva Primera Sesión Ordinaria 2026-2027 (video + 5 materiales) publicados

| | |
|---|---|
| **Fecha** | 2026-09-21 |
| **Sesión** | Jorge ya tenía en mano los dos videos de Opening pendientes (Fase Intensiva 2026-2027 y la nueva Primera Sesión Ordinaria) y los materiales de esta última, ya copiados a `pdfs/cte/cte-2026-2027/cte-primera-sesión/` (untracked) antes de empezar. Pedido directo de publicar ambos opening + los materiales de la primera sesión. |
| **Cambios en `cte.html`** | Se agregó el `video-container`/`iframe` (`data-src`, lazy-load, mismo patrón que el resto de sesiones) con el Opening de Fase Intensiva (`bXq_TtMf_pc`) al acordeón ya existente. Se agregó un acordeón nuevo "Primera Sesión Ordinaria 2026-2027" arriba de Fase Intensiva, con su propio Opening (`vhuipmEOEuY`) y los 5 materiales (PPTX + 4 PDFs) ya presentes en disco, más el ZIP y el link a Portal SEP (mismo patrón que cualquier sesión ordinaria completa, ej. Octava). Primera Sesión Ordinaria pasó a ser el acordeón `active`/badge NUEVO (más reciente); Fase Intensiva perdió el badge NUEVO y quedó colapsada, pero se queda dentro del bloque del ciclo actual (2026-2027), no se archiva — sigue siendo del ciclo vigente. |
| **Encoding de rutas** | La carpeta nueva lleva tilde (`cte-primera-sesión`) y el PPTX tiene espacios + tilde (`SEPRN Primera Sesión Ordinaria CTE 2026-2027.pptx`) — todos los `href` usan `%C3%B3`/`%20`, mismo criterio que el resto del sitio (regla 6 de `CLAUDE.md`). |
| **`index.html`** | Banner `.update-banner` actualizado de "Fase Intensiva 2026-2027 ya disponible" a "Primera Sesión Ordinaria 2026-2027 ya disponible" — es la sesión más reciente ahora. |
| **Documentación actualizada** | `CLAUDE.md` (tabla de sesiones CTE + pendiente resuelto), `docs/ROADMAP.md` (mismo pendiente tachado). |
| **Verificación** | Revisado a mano el anidado de `<div>` alrededor de la edición (el conteo global de `<div>`/`</div>` en `cte.html` tiene el mismo desbalance de +1 que ya existía antes de esta sesión — no es nuevo). Sin servidor local levantado ni prueba en navegador esta ronda — cambio de solo HTML/enlaces estáticos, mismo patrón ya usado docenas de veces en este archivo. |
| **Commits** | `6799128`, pusheado a `origin/main` a pedido explícito de Jorge — incluye 6 archivos binarios nuevos (~145MB entre PPTX/PDFs/ZIP) en `pdfs/cte/cte-2026-2027/cte-primera-sesión/`, todos bajo el límite de 100MB de GitHub. |

---

## CHECKPOINT — 2026-09-21 (cont. 3) · Formación Docente: badge/aviso de "plataforma externa" ya no dependen solo de tener liga, + nota de folio en la confirmación

| | |
|---|---|
| **Fecha** | 2026-09-21 |
| **Sesión** | Jorge estaba en vivo revisando el flujo de Formación Docente durante la conferencia "Hablemos de IA: IA ubicua en la educación" y notó que, pese a que esa conferencia no requiere registro externo (solo con OTDE; el link se muestra al final y se manda en el recordatorio), la tarjeta del catálogo mostraba la etiqueta "Registro en plataforma externa" y el paso 2 mostraba el aviso "Este registro es para OTDE...". Diagnosticado en modo plan (Explore) antes de tocar código. |
| **Causa raíz** | 4 lugares en `formacion-docente.html` mostraban esas señales con solo `liga_convocatoria`/`liga` (que exista un link), sin revisar `registro_previo_requerido`/`registroPrevio` — la señal real de que el registro externo es obligatorio. `Liga_convocatoria` se usa legítimamente en esta conferencia para guardar el link mostrado como referencia al final y mandado en el recordatorio, sin que eso implique registro obligatorio. La pantalla de confirmación ya distinguía bien ambos casos desde antes; el bug estaba solo en las señales previas al envío. Detalle completo en `docs/QA-NOTES.md #37`. |
| **Fix en `formacion-docente.html`** | Se agregó la condición `registro_previo_requerido`/`registroPrevio` junto al chequeo de liga en: badge de la tarjeta (`.cc-externo-tag`, línea ~1374), ícono del chip móvil (`.rs-chip-warn`, ~1526), nota del panel de escritorio (`.rsb-nota-externo`, ~1581) y el aviso `#aviso-legal` del paso 2 (~1681, dentro de `mostrarFormularioOtde()`). No se tocó nada en `apps-script/formacion-docente.gs` ni en la hoja `Cursos` — el dato de `Liga_convocatoria` de la conferencia se sigue usando igual. |
| **Nota de folio en la confirmación** | Jorge pidió agregar una nota para que el docente guarde/tome captura de su folio, ya que el registro nunca manda un correo de confirmación inmediato (confirmado revisando `doPost()` en el `.gs`: solo los recordatorios automáticos posteriores mandan correo). Párrafo fijo nuevo bajo `#folios-lista` (clase `.conf-folio-tip`), wording ajustado por Jorge: "Guarda tu folio: tómale una captura de pantalla o anótalo. Tu folio te servirá para cualquier aclaración o para subir tu constancia cuando aplique." |
| **Documentación actualizada** | `docs/DESIGN_SYSTEM.md` ("Patrón: aviso de registro externo") corregido para reflejar la condición real (antes documentaba el comportamiento viejo —con bug— como intencional) y para apuntar a la función real donde se togglea `#aviso-legal` (`mostrarFormularioOtde()`, no `actualizarResumenSticky()`). `docs/QA-NOTES.md` — entrada nueva #37 con la causa raíz completa. |
| **Verificación** | `git diff --stat`: `formacion-docente.html` (+12/-4). Revisado a mano contra las condiciones ya correctas del resto del archivo (`cursosConRegistroPrevio()`, `externoPendiente()`, rama "paso obligatorio" de `mostrarConfirmacion()`), que ya usaban `registroPrevio && liga` desde antes — el fix las alinea, no inventa un criterio nuevo. Pendiente de que Jorge confirme en vivo en el catálogo publicado que la tarjeta de "Hablemos de IA" ya no muestra la etiqueta y que un curso con `Registro_previo_requerido=TRUE` real sigue mostrándola igual que antes (ver checklist de verificación). |
| **Commits** | Pendiente de comitear — no se ha pedido push. |

---

## CHECKPOINT — 2026-09-21 (cont. 2) · Formación Docente: recordatorio + carga de constancia para conferencias UNETE, probado E2E en producción

| | |
|---|---|
| **Fecha** | 2026-09-21 |
| **Sesión** | Jorge preguntó cómo sistematizar el "Ciclo de Conferencias Virtuales de UNETE" del Centro de Formación Docente: la conferencia se transmite por YouTube (liga ya en mano) y UNETE emite constancia si el docente hace un trámite en su propio sitio — OTDE necesita una copia digital de esa constancia, algo que hoy nadie automatiza. Se resolvió primero en modo plan (Explore + AskUserQuestion) antes de tocar código, ya que el usuario pidió "solo clarificar el proceso" en una primera pasada; una vez definidos los 3 parámetros operativos (2 recordatorios: inmediato + al día siguiente; constancia individual por conferencia; formulario cerrado al segundo día) pidió construirlo. |
| **Diseño**: reutilizar el catálogo existente, no un sistema aparte | Cada conferencia UNETE se da de alta como un Curso más (`Categoria=Conferencia`, prefijo `CNF` ya existente en `PREFIJOS_CATEGORIA`) — nada de tabla ni endpoint nuevo. El mecanismo completo se activa por una sola señal, opt-in por curso: que `Liga_tutorial_constancia` (columna nueva en `Cursos`) esté llena. Mientras ese campo esté vacío (todo el catálogo actual), el curso se comporta exactamente igual que hoy — confirmado explícitamente con Jorge. |
| **Implementación en `apps-script/formacion-docente.gs`** | 2 columnas nuevas en `Cursos` (`Liga_tutorial_constancia`, `Hora_fin`) y 5 en `Inscripciones` (`Constancia_recordatorios_enviados`, `Fecha_ultimo_recordatorio_constancia`, `Constancia_recibida`, `Fecha_recepcion_constancia`, `Liga_constancia_drive`), todas al final vía el mismo auto-heal de encabezados que ya usa el resto del archivo. `lineaTutorialConstancia_()` agrega el link al tutorial de UNETE al correo de aviso normal (solo si el campo está lleno). Recordatorio nuevo, `enviarRecordatoriosConstancia_()` (llamado desde `enviarRecordatoriosWebinar()`, cada 15 min): recordatorio 1 en cuanto pasa `finConferencia_()` (Fecha_fin + Hora_fin, o 23:59 si no hay hora capturada), recordatorio 2 al día siguiente, ventana de carga abierta días 0–1 y cerrada al día 2 (`ventanaConstanciaAbierta_()`, `DIAS_LIMITE_CONSTANCIA=2`). Liga firmada con HMAC propio (`tokenConstancia_()`/`ligaConstancia_()`, mensaje distinto al de `tokenConfirmacion_()` del doble registro — nunca intercambiables) que abre `formacion-docente.html?subirConstancia=<folio>&t=<firma>`. Carga real a Drive vía `subirConstancia_()` (doPost) + `constanciaInfo_()` (doGet, chequeo previo de "¿ya se recibió?"/"¿sigue abierta la ventana?"), mismo patrón base64→`DriveApp` que `mantenimiento.gs`, carpeta nueva "Constancias de Conferencias". Menú nuevo: "Enviar recordatorio de prueba de constancia (folio)". |
| **Implementación en `formacion-docente.html`** | Sección nueva `#paso-subir-constancia` + `mostrarSubirConstancia()`/`enviarConstancia()`: lee `?subirConstancia=&t=`, pide `constanciaInfo` antes de mostrar el formulario (oculta la carga si ya se recibió o si el plazo venció), sube el archivo en base64 igual que el patrón ya usado en Mantenimiento. |
| **Bug real #1 — el HTML nunca llegó a producción** | Jorge redesplegó el `.gs` en Apps Script, pero `formacion-docente.html` solo existía en el working directory local — nunca se había hecho `git push`. La prueba E2E lo detectó de inmediato (el enlace del correo real abría el catálogo normal, no el formulario de carga). Corregido comiteando y pusheando ambos archivos (`331ee97`) — GitHub Pages tardó los ~5-10 min de siempre en servir la versión nueva (confirmado con `curl` contra el CDN, que ya traía el código nuevo, contra el navegador con caché vieja). |
| **Bug real #2 — Apps Script sin autorización de Drive** | Al llegar a la carga real, `subirConstancia_()` falló con "No cuentas con el permiso para llamar a DriveApp.getFoldersByName/createFolder" — este proyecto nunca antes había tocado `DriveApp`, y el redespliegue de una nueva versión no re-dispara el consentimiento de scopes por sí solo. Se resolvió agregando temporalmente una función sin guion bajo (los nombres con `_` no aparecen en el selector de "Ejecutar" del editor — hallazgo de tooling nuevo) que llamaba a `obtenerCarpetaConstancias_()`, corriéndola una vez desde el editor para aceptar el permiso de Drive, y quitándola de nuevo antes de guardar — el archivo real quedó verificado carácter por carácter contra la copia local antes de la prueba final. |
| **Verificación end-to-end real, sin modo de prueba** | Curso de prueba oculto (`Activo=FALSE`, para no aparecer en el catálogo público) dado de alta a mano en `Cursos`; registro real vía `curl` contra el endpoint desplegado (folio `OTDE-CAP-0348`); `enviarRecordatoriosWebinar()` corrido a mano desde el editor mandó el correo real (con las dos ligas) al Gmail personal de Jorge; clic real en "Subir mi constancia" desde ese correo, en la página ya publicada; subida real de un PDF de prueba. Verificado en la fila real de `Inscripciones`: `Constancia_recibida=Sí`, `Fecha_recepcion_constancia` con hora real, `Liga_constancia_drive` con el archivo real en Drive. Jorge borró los datos de prueba (fila de `Cursos`, fila de `Docentes`, fila de `Inscripciones`, archivo en Drive) al terminar. |
| **Verificación de código** | `git diff --stat`: `apps-script/formacion-docente.gs` (+329/-7), `formacion-docente.html` (+132/-0). `node --check` sobre ambos (extrayendo el `<script>` del HTML) antes de comitear. |
| **Commits** | `331ee97`, pusheado a `origin/main` a pedido explícito de Jorge (necesario para que la prueba E2E pudiera completarse contra producción real). |

---

## CHECKPOINT — 2026-09-21 (cont.) · SIGEE se modernizó: investigación en vivo + nuevo tipo "Cambio de Contraseña + Eliminar Autenticación" en Correo Institucional

| | |
|---|---|
| **Fecha** | 2026-09-21 |
| **Sesión** | Jorge pidió revisar a fondo `sigee.dee.edu.mx/modulos.php` (la plataforma externa de CoEEE donde se aprovisionan las cuentas de correo institucional) porque había sufrido modificaciones, y proponer cómo empatar el sistema propio para facilitar la gestión de solicitudes en todas sus modalidades. Se investigó en vivo con la sesión real de Jorge (Chrome ya autenticado como enlace de SEPRN) y en paralelo, con un agente en background, se auditó el código local existente (`apps-script/correo/`, `CLAUDE.md`, `docs/ARCHITECTURE.md`). |
| **Hallazgo: SIGEE migró de IP sin TLS a dominio propio, con panel completo** | SIGEE pasó de `189.206.211.185/sigid` (única referencia que tenía el repo, en `GenerarResumenSIGEE.gs`/`Alta.gs`) a `sigee.dee.edu.mx` v7.1 — dominio con TLS, panel "Correo Institucional" con `Nueva solicitud`/`Mis solicitudes`/`Solicitudes atendidas`/`Consultar cuentas`, folios propios autoincrementales, seguimiento en dos etapas (Pendiente/En revisión → Atendida → Pendiente/Enviado a sector) y un catálogo de cuentas exportable (6,399 cuentas en la unidad de Jorge, con CURP/Función/CCT/fecha de uso). Sigue **sin exponer ninguna API** — confirma lo que ya documentaba el repo (`ARCHITECTURE.md §16`/`Alta.gs`: sin API, "NP SIGEE" tecleado a mano por Marcos es el único hilo de trazabilidad). |
| **Hallazgo: SIGEE ahora tiene 6 tipos de solicitud, el webform propio solo cubría 4** | `Nueva solicitud` de SIGEE: Alta de cuenta, Cambio de contraseña, Eliminar método de autenticación, **Cambio de contraseña + eliminar método de autenticación** (nuevo, combinado), **Actualización de datos** (nuevo), **Baja/eliminar cuenta** (nuevo). El webform de OTDE solo tenía Alta/Cambio/Eliminar Autenticación/Incidencias (esta última, un catch-all propio del sitio sin equivalente en SIGEE). Presentado a Jorge vía `AskUserQuestion`: priorizó construir el tipo combinado ahora; dejó Actualización de datos y Baja/eliminar cuenta documentadas para después (ver `docs/ROADMAP.md` ítem 23); descartó explícitamente reconciliación automática por CURP contra "Consultar cuentas" y cualquier automatización tipo RPA sobre SIGEE. |
| **Construcción: 5º tipo "Cambio de Contraseña + Eliminar Autenticación"** | Nuevo `apps-script/correo/CambioYReset.gs` (278 líneas), calco exacto del patrón de `CambioContrasena.gs`: `HOJA_CAMBIO_RESET = 'Cambio y Eliminar Autenticación'`, folio `OTDE-CYR-NNNN`, mismos 6 campos requeridos que Cambio/Eliminar Autenticación por separado, correo de entrega que fusiona ambos (contraseña nueva + pasos de Authenticator) en un solo mensaje. Se conectó en los 4 archivos compartidos del proyecto (`WebApp.gs` — `case` en `doPost`, arreglo de `listarPendientesCorreo()`, mapa de `manejarConsultaCorreo()`; `OnEdit.gs` — `case` nuevo en el switch de `onEditWebform`; `Config.gs` — `configurarValidacionYSemaforo()`; `ResumenSemanal.gs` — arreglo `hojasValidas`) y en `correo.html` (5º botón en el switcher, panel completo con `crearCctAutocomplete('cyr')`, `toggleCyrForm`/`validarCyrForm`/`enviarSolicitudCambioReset`, reusando `CAMBIO_APPS_SCRIPT_URL` sin necesitar una constante ni un despliegue nuevo). |
| **Verificación** | `git diff --numstat` de esta sesión (antes de este checkpoint): `correo.html` (+166/-3), `apps-script/correo/Config.gs` (+11/-8), `apps-script/correo/OnEdit.gs` (+5/-2), `apps-script/correo/ResumenSemanal.gs` (+8/-8), `apps-script/correo/WebApp.gs` (+9/-3), `apps-script/correo/CambioYReset.gs` nuevo (278 líneas). Antes de comitear: braces/paréntesis balanceados en el `.gs` nuevo (verificado con Python), IDs `cyr-*` únicos y `<div>`/`</div>` balanceados en el panel nuevo de `correo.html` (verificado con `grep`/`awk`). Jorge pegó y redesplegó los 5 archivos de `apps-script/correo/` en el proyecto real. Verificado después por `curl` contra el endpoint real, siguiendo a mano la redirección 302 de Apps Script a `script.googleusercontent.com/macros/echo?...` (necesaria con GET, no repitiendo el POST — gotcha nuevo de tooling, no de código): `{"tipo":"cambioContrasenaYReset"}` sin más campos responde `{"status":"error","mensaje":"Campo requerido: cct"}` (confirma el `case` nuevo enrutando a `manejarCambioContrasenaYReset()`); agregar `cct` avanza el error a `escuela` (confirma el orden real de `cambioResetValidarCampos()`); un tipo de control inventado da el mensaje distinto de "no reconocido" — las tres pruebas fallan por diseño antes de escribir en el Sheet o disparar correos/Telegram, a propósito, para no ensuciar producción. **Pendiente real**: la prueba de extremo a extremo con datos completos (fila nueva real, correo combinado de entrega, aviso a Marcos) — ver `docs/ROADMAP.md` ítem 23. |
| **Commits** | Pendiente — sesión sin commitear todavía al momento de este checkpoint. |

---

## CHECKPOINT — 2026-09-21 · Mantenimiento: notificación de rechazo, fix de sobrescritura en reportes de 2ª visita, y botón CTA ilegible en 3 backends de correo

| | |
|---|---|
| **Fecha** | 2026-09-21 |
| **Sesión** | Jorge trajo feedback de una primera visita real de Mantenimiento en un solo mensaje: (1) las solicitudes rechazadas no avisan a nadie ni dicen el motivo; (2) un técnico llenó `reporte-visita.html` un día y, al llenarlo de nuevo al día siguiente para la misma intervención (mismo folio, 2ª visita), la segunda captura sobrescribió la primera en vez de agregarse como fila nueva; (3) propuso automatizar que "¿Requiere segunda visita? = No" marque `Estatus = Resuelto` solo. Se planeó en modo plan antes de tocar código. Jorge tuvo dudas sobre el punto (3) y pidió dejarlo anotado para decidir después, sin implementarlo. Más tarde, en la misma sesión, reportó un segundo problema no relacionado: el texto de los botones CTA de los correos automatizados se veía en azul/morado sobre el fondo, no en blanco. |
| **Rechazo silencioso → ahora notifica con motivo** | `manOnEditCierre` (`apps-script/mantenimiento.gs`) reaccionaba solo a `Estatus = Resuelto` — confirmado como decisión deliberada, no bug, en una nota ya existente de `docs/ARCHITECTURE.md §19.1`. Se extendió el mismo trigger para reaccionar también a `Rechazado`, con una función nueva `manNotificarRechazo()` (calco de `manNotificarCierre()`) que reutiliza la columna ya existente `Notas de revisión` como motivo del rechazo (sin agregar columna nueva) y pide al solicitante volver a llenar el formulario completo (`MAN_CTA_FORMULARIO`, nueva constante) para corregir y reenviar — Jorge confirmó que prefiere reenvío completo sobre un link de edición parcial. |
| **Bug real de sobrescritura en "Reportes de visita" — causa raíz y fix** | `manBuscarFilaReportePorFolio_()` emparejaba **solo por folio** para decidir sobrescribir vs. agregar fila — sin mirar la fecha, así que un segundo reporte del mismo folio en otro día encontraba (y sobrescribía) la fila del día anterior. Se agregó `manBuscarFilaReportePorFolioYFecha_()` (folio + mismo día calendario de "Fecha de atención", comparando año/mes/día para evitar falsos negativos por huso horario), usada solo dentro de `manDoPostReporteVisita_()` — la acción de menú manual (`manGenerarYEnviarReporteVisita()`) se dejó con la función original, sigue siendo correcta para ese caso. |
| **Punto (3) — auto-cierre en "No requiere segunda visita" — pendiente de decisión, no implementado** | Jorge tiene dudas (en particular, `manCrearSolicitudUrgente_()` ya fuerza `Estatus=Resuelto` en casos urgentes sin mirar esta respuesta, lo que podría chocar). El diseño explorado quedó documentado en el plan de la sesión para retomarlo después; **no se tocó código para este punto**. |
| **Botón CTA ilegible en Mantenimiento/Asesorías/Soporte** | Causa raíz: el `color:#ffffff` del botón vivía solo dentro de un bloque `<style>` en el `<head>` del correo (`.btn{...}`), que varios clientes de correo (Outlook, Gmail, Apple Mail) ignoran o pisan con su propio azul/morado de link por default — patrón que `formacion-docente.gs` y `apps-script/correo/` ya evitaban con estilos 100% inline. Se agregó el mismo `style="..."` directo sobre el `<a>` en los 3 backends afectados (`apps-script/mantenimiento.gs`, `apps-script/asesorias.gs`, `apps-script/soporte-remoto.gs` — código idéntico en los 3, mismo diff literal), documentado como `docs/QA-NOTES.md #34`. |
| **Verificación** | `git diff --numstat` de los 2 commits: `apps-script/mantenimiento.gs` (+106/-6), `apps-script/asesorias.gs` (+4/-1), `apps-script/soporte-remoto.gs` (+4/-1), `docs/ARCHITECTURE.md` (+13/-5), `docs/QA-NOTES.md` (+29/-0), `docs/manual-bases-tramites.html` (+3/-3). `node --check` sobre los 3 `.gs` modificados (sintaxis válida) antes de comitear. Jorge redesplegó los 3 backends; ya en producción se confirmó por `curl` (con User-Agent de navegador — el UA por defecto de `curl` recibía 403 de Google, no relacionado con el despliegue) que los 3 endpoints (`?action=consulta` con folio inventado) responden `{"status":"no_encontrado"}` con HTTP 200, confirmando que el redespliegue no rompió nada. **No verificado en vivo** (requeriría modo de prueba activado desde el editor de Apps Script, o inspección directa de la Hoja/un correo real, ninguno posible solo con `curl`): el contenido y disparo real del correo de rechazo, que el fix de sobrescritura efectivamente agregue una fila en vez de sobrescribir con datos reales, y el color del botón en un cliente de correo real. |
| **Commits** | `1bed393` (rechazo + fix de sobrescritura), `3c44d31` (botón CTA) — ambos pusheados a `origin/main` a pedido explícito de Jorge. |

---

## CHECKPOINT — 2026-09-20 · Ceremonias Cívicas: 4 etapas por pedido de Jorge (filtros de historial, cobertura 0/X, reagendar/cancelar, reportes con fecha de referencia, dashboard en la Sheet) + rediseño del reporte resumido tras feedback en vivo

| | |
|---|---|
| **Fecha** | 2026-09-20 |
| **Sesión** | Jorge trajo 6 pedidos sobre `ceremonias-civicas.html`/`apps-script/visitas-jefes.gs` en un solo mensaje: filtros en el historial de visitas, listar sectores sin visitar (0/X) en el panel de cobertura, botones de reagendar/cancelar con motivo obligatorio, seleccionar fecha/semana al generar el reporte PDF existente, un segundo reporte con columnas acotadas, y "materializar" la idea (pendiente) de un dashboard. Pidió abordarlo **por etapas, revisando cada una antes de seguir** — mismo criterio incremental ya usado en otras sesiones de este proyecto. Cada etapa se planeó en modo plan (Explorar → Diseñar → `ExitPlanMode`) antes de tocar código. |
| **Etapa 1 — filtros de historial + sectores 0/X (100% frontend, sin redeploy)** | Chips de estatus (incluido el pseudo-estatus "⚠ Vencida") + rango de fechas en `visRenderTabla()`; el Panel de cobertura ahora itera el catálogo completo de sectores (`totalPorSector`, ya calculado desde `CCT_DB`) en vez de solo los sectores con visitas, mostrando "⚠ 0 / X" ordenado por menor cobertura primero. Detalle completo en `docs/ARCHITECTURE.md §22`. |
| **Etapa 2 — reagendar/cancelar visita (backend + frontend)** | Dos acciones nuevas en `doPost` (`visDoPostReagendar_()`/`visDoPostCancelar_()`), columna nueva `Historial de cambios` (auto-heal), `visExisteReservaActiva_()` con un 4º parámetro opcional para excluir la propia fila al reagendar. Frontend: sección "Reagendar o cancelar mi visita", folio manual a propósito (no una lista — evita que cualquiera pueda tocar la reserva de otro). Probado con 15 casos de comportamiento en un arnés de Node (`vm` + mocks de `SpreadsheetApp`/`LockService`/`Utilities`) antes de entregarse, incluida la regresión del flujo de reserva original. |
| **Etapa 3 — fecha de referencia en el reporte + segundo reporte resumido (menú del Sheet, sin redeploy del Web App)** | `visPedirFechaReferencia_()` nuevo, reutilizado por ambos reportes. **Bug real encontrado en las pruebas, antes de producción**: los filtros de "recientes" solo tenían límite inferior — con una fecha de referencia pasada, una visita fechada *después* de esa referencia se colaba igual. Corregido (`&& f <= hoy`), ver `docs/QA-NOTES.md #33`. `visGenerarReporteResumen_()` es el segundo reporte (columnas C/D/G/H/I/J/O/Q/U/V/W), nombrado "resumido" y no "de difusión" a propósito (la columna Q es uso interno según `ficha-ceremonias-civicas.html`). 9 casos de prueba en Node. |
| **Etapa 4 — dashboard, con corrección de rumbo a mitad de sesión** | La primera pregunta de aclaración solo ofreció dos opciones de frontend; Jorge aclaró después que el dashboard que pedía desde el mensaje original vive **dentro de la Sheet**, no en el sitio — corregido explícitamente antes de replanear (ver `docs/ARCHITECTURE.md §22`, nota de "corrección de rumbo"). Nueva pestaña "Dashboard" en la misma Sheet (`visActualizarDashboardHoja_()`, menú "SEPRN Visitas"), estilo `panel-otde.gs` pero sin `UrlFetchApp`/token (los datos ya viven en la misma Sheet): mezcla de estatus, visitas por persona/sector, tendencia semanal. 8 casos de prueba en Node, incluido correr el refresh dos veces seguidas sin romperse por celdas combinadas. |
| **Rediseño del reporte resumido, mismo día, tras feedback de Jorge en producción** | Jorge desplegó y probó: "el reporte resumido funciona, pero no me gusta el diseño... es difícil leerlo". La tabla de 11 columnas a 8.5px aplastaba texto libre largo (Convocados/Descripción/Operatividad). Se presentaron 2 propuestas de tarjeta con mockups ASCII vía `AskUserQuestion` (tarjetas simples vs. con separación "Para difusión"/"Uso interno"); Jorge eligió tarjetas simples. Rediseñado con el mismo patrón visual que la sección "Realizadas" del reporte de seguimiento. Verificado con 3 pruebas de comportamiento + una captura de pantalla real (Playwright, HTML renderizado con datos de ejemplo) antes de entregarse. |
| **Corrección de documentación encontrada de paso** | `CLAUDE.md` tenía una nota vigente ("Ceremonias Cívicas — `visitas-jefes.gs` con fixes sin desplegar, 6 sep 2026") que contradecía a `docs/ROADMAP.md` ítem 17, que ya tenía esos mismos fixes marcados como desplegados desde el 6 sep 2026. Corregido en esta sesión — ver `CLAUDE.md` §"Pendientes vigentes". |
| **Verificación** | `git diff --numstat` de los 2 commits combinados: `apps-script/visitas-jefes.gs` (+364/-9), `ceremonias-civicas.html` (+400/-9). Casos de prueba de comportamiento corridos antes de cada entrega (15 para reagendar/cancelar, 9 para los reportes con fecha de referencia, 8 para el dashboard, 3 para el rediseño del reporte) contra arneses de Node que simulan `SpreadsheetApp`/`LockService`/`Utilities`/`DriveApp`, más pruebas de UI con Playwright headless contra un servidor local (nunca contra producción). Nombres de función/columna citados en esta sesión y en los docs actualizados confirmados con `grep` contra el código real antes de escribirlos. Jorge desplegó `visitas-jefes.gs` dos veces (tras la Etapa 2-4, y tras el rediseño del reporte) y confirmó ambas. **Pendiente real**: verificación en vivo de reagendar/cancelar, la fecha de referencia y el dashboard contra datos reales del ciclo — ver `docs/ROADMAP.md` ítem 20. |
| **Commits** | `3ba779c` (Etapas 1-4), `9fe8720` (rediseño del reporte) — ambos ya en `origin/main`, pusheados durante la sesión a pedido explícito de Jorge. |

---

## CHECKPOINT — 2026-09-17 (cont. 3) · Prueba en vivo del caso urgente sin folio contra producción real, y descarte del trigger nocturno de fotos como pendiente

| | |
|---|---|
| **Fecha** | 2026-09-17 |
| **Sesión** | Arrancó con una auditoría pedida por Jorge de los pendientes reales relacionados a Mantenimiento. Se detectó que la memoria de la sesión anterior tenía desactualizado el estado de la Fase 2 (marcada "sin probar" cuando ya llevaba desde el 25 ago desplegada) — corregido. El pendiente genuino identificado fue el caso urgente sin folio (desplegado por Jorge la sesión anterior, nunca probado contra el backend real). Jorge pidió atenderlo. |
| **Prueba en vivo del caso urgente sin folio, contra producción real** | Ejecutada de punta a punta vía navegador (esta sesión de Chrome ya estaba autenticada como `otde.nezahualcoyotl@gmail.com`, dueña del Sheet real y con acceso al editor de Apps Script, así que no requirió ningún paso manual de Jorge). Se activó `manActivarModoPrueba('otde.nezahualcoyotl@gmail.com')` vía función temporal en el editor (gotcha ya documentado en `docs/QA-NOTES.md #14`). **Escenario folio normal** (`OTDE-MAN-0014`, 2 casillas de Instalación realizada): nueva fila en `Reportes de visita` sin sobrescribir el reporte previo del mismo folio (el 25 ago), PDF y foto en Drive, correo `[PRUEBA]` con destinatarios correctos en el banner (escuela+técnico+OTDE). **Escenario caso urgente sin folio** (CCT real `15DPR0860F`, autocomplete confirmado, sin mensajes de error pegados al alternar Sí/No — regresión de `docs/QA-NOTES.md #32` sin encontrar el bug otra vez): `manCrearSolicitudUrgente_()` dio de alta el folio `OTDE-MAN-0016` en `Solicitudes` con `Estatus=Resuelto` desde el alta y la nota automática esperada; se confirmó que llegó **un solo correo** (el del reporte de visita) y no el de "solicitud recibida", que es el comportamiento diseñado. Instalación realizada con selección múltiple quedó cubierta dentro de ambos escenarios (2 y 3 casillas respectivamente). Se desactivó `manDesactivarModoPrueba()` al terminar (confirmado en Script Properties) y se dejaron las filas/PDF/fotos de prueba sin limpiar, mismo criterio que el 25 ago (no existe función de limpieza en el backend). **Sin bugs encontrados en esta ronda.** |
| **Hallazgo que reduce la Fase 3 del retiro de v8.5** | Jorge preguntó, al ver que las fotos ya quedan organizadas por carpeta y por folio automáticamente, si el trigger nocturno de organización de fotos de v8.5 seguía siendo necesario. Se leyó el código real de v8.5 (`OTDE Reportes.gs`, función `organizarFotosNocturno()`, ~línea 386): ese trigger corre a las 11pm y empareja por fecha de creación las fotos que Google Forms dumpó en una sola carpeta plana (`CONFIG.CARPETA_FOTOS_FORMS_ID`) contra las carpetas de reporte creadas ese mismo día, porque un Form no puede enrutar un archivo subido por CCT/folio al momento del envío. `reporte-visita.html`/`manSubirFotosReporte_()` no tienen ese problema — escriben cada foto directo a su subcarpeta por folio en el mismo envío. **Conclusión: ese ítem de la Fase 3 no aplica a la arquitectura nueva y se retira del roadmap** — el único pendiente real de Fase 3 queda siendo el reporte mensual. |
| **Verificación** | `git diff --stat` de esta sesión: `CLAUDE.md` (+39/-23, corrige la entrada de caso urgente a "verificado en vivo" y reescribe el pendiente de Fase 3), `docs/ROADMAP.md` (+28/-6, mismo ajuste de Fase 3 e ítem 9). Nombres de función/constante citados arriba confirmados contra el código real (`grep` en `apps-script/mantenimiento.gs` y lectura directa del editor de Apps Script de v8.5) antes de escribirlos. |
| **Commits** | Pendiente — sesión sin commitear todavía al momento de este checkpoint. |

---

## CHECKPOINT — 2026-09-17 (cont. 2) · `reporte-visita.html`: caso urgente sin folio (auto-alta de Solicitud), Conectividad "WiFi y Cable" e Instalación realizada a selección múltiple

| | |
|---|---|
| **Fecha** | 2026-09-17 |
| **Sesión** | Arrancó con una auditoría pedida por Jorge: revisar `mantenimiento.html` y todo lo que se le vincula (backend, `oficina-virtual.html`, `asesorias.html`, `correo.html`, `soporte.html`, `reporte-visita.html`) para detectar pendientes. Hallazgo principal: la Fase 2 (reporte técnico + PDF) que la memoria tenía anotada como "construida pero sin desplegar" en realidad ya estaba desplegada y verificada en vivo desde el 25 ago 2026 — corrección de una nota desactualizada, no un pendiente real. De ahí, Jorge pidió 3 ajustes a `reporte-visita.html`: (1) mover "¿Atención sin solicitud previa registrada?" al principio del formulario y hacer el Folio condicional a esa respuesta; (2) agregar una opción combinada "WiFi y Cable" en Conectividad; (3) permitir varias opciones en Instalación realizada. Confirmó también querer atender después Fase 3/Fase 4 (retiro completo de v8.5), pendiente para otra sesión. |
| **Hallazgo de diseño antes de construir** | El cambio 1 no era un simple toggle de UI: todo el flujo de `reporte-visita.html` dependía del folio para identificar la escuela (`manBuscarSolicitudPorFolio_()` lee CCT/escuela/sector/zona/correo de `Solicitudes`) — sin folio no había de dónde sacar esos datos para el PDF ni el correo. Jorge confirmó la solución: cuando la visita es "sin solicitud previa" y no hay folio, el backend da de alta una Solicitud nueva automáticamente a partir de datos que el técnico captura en ese momento (CCT con autocomplete, Turno, Función, Nombre, Correo), en vez de dejar el reporte "suelto" sin folio ni registro. |
| **Frontend (`reporte-visita.html`)** | "¿Atención sin solicitud previa?" ahora es el primer campo del fieldset "Identificación". Folio pasó a condicional (`CAMPOS_SIMPLES` ya no lo valida sin condición): oculto y no obligatorio si la respuesta es "Sí", visible y obligatorio si es "No" (comportamiento de siempre). Nuevo sub-bloque "Identificación de la escuela" (visible solo con "Sí"): CCT con autocomplete real contra `js/cct-db.js` (agregado a este archivo, no lo cargaba antes) replicando el patrón `manSeleccionarCct`/`manResetCct`/`manActualizarZonas`/`manActualizarTipoCct` de `mantenimiento.html` con prefijo propio `urg`, fallback manual de Tipo de CCT/Sector/Zona/Escuela, Turno, Función/Cargo (repoblada vía `otdePoblarFuncion()`, ya disponible por `js/tramites-shared.js`), Nombre y Correo de contacto. Conectividad ganó la opción "Sí — WiFi y Cable". Instalación realizada pasó de `<select>` único a checkboxes (mismo patrón `check-list`/`check-item` ya usado en Actividades preventivas/correctivas, con "Otro" + texto libre), uniendo las opciones marcadas con `"; "` al enviarlas — igual que "Temas de Excel" en `asesorias.gs`. |
| **Backend (`apps-script/mantenimiento.gs`)** | Nueva función `manCrearSolicitudUrgente_(datos)`: valida los campos mínimos (CCT/Turno/Función/Nombre/Correo), genera folio con `manGenerarFolio()`, da de alta la fila en `Solicitudes` con `Estatus = 'Resuelto'` desde el inicio y una nota ("Generada automáticamente desde reporte de visita urgente...") — deja en blanco WhatsApp/Equipos con falla/Oficio/Tipo de equipo, que no aplican retroactivamente. Devuelve el mismo shape que `manBuscarSolicitudPorFolio_()` para que `manDoPostReporteVisita_()` no tenga que distinguir el camino más adelante. Como el alta ocurre por script, no dispara `manOnEditCierre`/`manOnEditProgramacion` (los triggers `onEdit` instalables no se activan con ediciones hechas por Apps Script) — sin riesgo de correo duplicado, el único aviso que sale es el propio correo del reporte de visita. |
| **Bug real encontrado y corregido durante la prueba en navegador** | Al marcar "Sí", dejar el fallback manual a medias (zona/escuela sin llenar), cambiar a "No" y volver a "Sí", los mensajes de error de esos campos manuales quedaban visibles en rojo desde antes de que el técnico tocara nada — el listener de `sin-solicitud` solo limpiaba errores de los campos "felices" (CCT/Turno/Función/Nombre/Correo), no los del fallback manual. Corregido ampliando la lista de campos que se limpian al cambiar a "No". Detalle completo en `docs/QA-NOTES.md #32`. |
| **Verificación** | `git diff --numstat`: `apps-script/mantenimiento.gs` +89/-7, `reporte-visita.html` +451/-41. `node -e "new Function(...)"` sobre ambos archivos (sintaxis válida) y conteo de `<div>`/`</div>`/`<fieldset>` balanceados en el HTML antes de probar. Probado en navegador contra un servidor estático local (`python3 -m http.server`, sin tocar el backend real): reordenamiento confirmado, toggle folio↔identificación en ambas direcciones, autocomplete de CCT real (encontrado y no encontrado, con fallback manual y zonas pobladas por sector), Función repoblada por tipo de CCT, Conectividad con la opción nueva, checkboxes de Instalación con selección múltiple + "Otro" + limpieza de error en vivo, payload final inspeccionado directamente (`folio` vacío + campos `urg*` + `instalacionRealizada` unida con `"; "`) sin depender de un envío real. Sin errores de consola en ningún momento. Jorge desplegó el `.gs` actualizado a producción (Administrar implementaciones → Nueva versión) tras la prueba; **queda pendiente correr `manActivarModoPrueba('correo')` y probar los 3 escenarios en vivo contra producción** antes de confiar el flujo con datos reales — ver `docs/ROADMAP.md`. |
| **Commits** | Pendiente — sesión sin commitear todavía al momento de este checkpoint. |

---

## CHECKPOINT — 2026-09-17 (cont.) · Rediseño de `reporte-visita.html`: todo obligatorio, campos enriquecidos y fotos de evidencia

| | |
|---|---|
| **Fecha** | 2026-09-17 |
| **Sesión** | Arrancó con una pregunta informativa de Jorge (sin cambios de código): una escuela se atendió el mismo día en calidad de urgente/extraordinaria sin haber pasado por la solicitud web, y quería saber si el sistema se rompía. Respuesta verificada contra `manDoPostReporteVisita_()`/`manValidarDatosReporte_()`: el reporte técnico exige que el folio ya exista en "Solicitudes" pero no valida el orden cronológico, así que basta con llenar la solicitud (aunque sea después de la atención) antes del reporte. A partir de ahí, Jorge pidió rediseñar `reporte-visita.html` de fondo: (1) que todos los campos sean obligatorios — hoy solo 3 de ~18 lo eran —, (2) enriquecer el contenido para capturar información realmente útil para OTDE, (3) mantener la captura fácil y sin fricción, comparando contra el Google Form viejo (PDF que Jorge adjuntó) que el webform había perdido en estructura al digitalizarse (catálogos de actividades, selects categóricos, conteos numéricos, fotos obligatorias, un campo narrativo "qué se hizo hoy"). |
| **Decisiones confirmadas antes de construir** | Incluir fotos de evidencia obligatorias reutilizando el patrón ya probado de `ficha-ceremonias-civicas.html`/`apps-script/visitas-jefes.gs` (compresión en canvas + Drive, con el fix de rendimiento de compartir la carpeta una sola vez). Agregar un campo "¿Esta visita se atendió sin solicitud previa registrada?" (Sí/No) — da trazabilidad al caso planteado al inicio de la sesión. Los campos narrativos que a veces no tienen nada que decir (Seguimiento, Observaciones) quedan obligatorios pero con un chip "Ninguna"/"Ninguno" que los llena con un toque. |
| **Formulario: de 5 secciones/3 obligatorios a 8 secciones/~24 obligatorios** | Identificación (Inicio/Fin de visita pasan de opcional a obligatorio; nuevo select "Atención sin solicitud previa"; "Responsable" gana opción "Otro" con texto libre) · Aula al llegar (Aula en uso y Mobiliario pasan de textarea libre a select categórico, con descripciones de ayuda) · Equipos (conteos numéricos en vez de texto; nuevo campo "Modelo/marca del equipo"; Conectividad a select) · Equipos administrativos (Sí/No condicional, cantidad solo si aplica) · Actividades realizadas (Preventivas/Correctivas a checkboxes de catálogo con checkbox exclusivo "Ninguna"; Instalación realizada con catálogo de 7 valores; nuevo select "Toolwiz Time Freeze") · Resultado (nuevo "¿Requiere segunda visita?") · Evidencia fotográfica (nueva, obligatoria) · Descripción y observaciones (nuevo campo narrativo "Descripción de la atención realizada", que no existía en el formulario digital). `reporte-visita.html` pasó a cargar `js/tramites-shared.js` (antes tenía su propia copia de `fetchJsonConTimeout` sin el parámetro `timeoutMs`) para reutilizar `leerArchivoBase64()` y el patrón de timeout extendido en envíos con fotos. |
| **Backend: 7 columnas nuevas al final de "Reportes de visita" (U-AA), mismo criterio que "Solicitudes"** | Atención sin solicitud previa · Modelo/marca del equipo · ¿Se atendieron equipos administrativos? · ¿Se instaló Toolwiz Time Freeze? · ¿Requiere segunda visita? · Descripción de la atención realizada · Fotografías de evidencia (URLs, una por línea). Las 20 columnas existentes no cambiaron de posición — solo el contenido de varias pasó de texto libre a valor de catálogo. `manValidarDatosReporte_()` ahora exige las ~24 columnas capturables (antes solo Responsable/Fecha/Estado del aula). Funciones nuevas `manSubirFotosReporte_()`/`manObtenerCarpetaFotosReporte_()`/`manObtenerCarpetaFotosPorFolio_()`, calco de `visSubirFotos_()`/`visObtenerCarpetaFotos_()` de `visitas-jefes.gs` — carpeta "Fotos de Reportes de Visita" con subcarpeta por folio (más simple que "por semana" porque el folio ya es único), compartida una sola vez por envío. `manGenerarPDFReporte_()` ganó las secciones nuevas (incluido un aviso destacado si la atención fue sin solicitud previa) y sigue cabiendo en una sola página. `manConfigurarValidacionYSemaforo()`/`manAsegurarHojaReportes()` extendidas con dropdowns para las columnas Sí/No nuevas. |
| **Verificación en vivo end-to-end contra producción** | Jorge ya había pegado y redesplegado el `.gs` (paso 1); esta sesión hizo los pasos 2-3: activó `manActivarModoPrueba('otde.nezahualcoyotl@gmail.com')` vía función temporal en el editor (mismo patrón de `docs/QA-NOTES.md #14`), y envió un reporte real desde una copia local de `reporte-visita.html` (servida por `python3 -m http.server`, apuntando al endpoint real) usando el folio real más reciente de la hoja (`OTDE-MAN-0014`, escuela Cuauhtémoc) y una foto de prueba subida vía `mcp__claude-in-chrome__file_upload`. Confirmado contra el Sheet real: fila con las 27 columnas correctas; PDF en Drive con las secciones nuevas, "Página 1 de 1"; foto comprimida en la carpeta `Fotos de Reportes de Visita/OTDE-MAN-0014`; correo recibido en Gmail con prefijo `[PRUEBA]`, asunto y PDF correctos. Modo de prueba desactivado al terminar (`manDesactivarModoPrueba()`, confirmado "Se completó la ejecución"). **A diferencia de rondas de prueba anteriores, la fila/PDF/foto de prueba de esta sesión se dejaron sin limpiar a petición explícita de Jorge** (quedan marcados "PRUEBA — ignorar" en Descripción/Observaciones). |
| **Verificación** | `git diff --numstat`: `apps-script/mantenimiento.gs` +193/-32, `reporte-visita.html` +575/-79. `node --check` sobre ambos archivos (sintaxis válida) antes de la prueba en vivo. Validación de formulario probada en navegador sin datos reales (campos obligatorios, mostrar/ocultar condicionales, checkboxes exclusivos, chips) antes de la prueba contra producción. |
| **Commits** | `41c1b68` (código: `reporte-visita.html` + `apps-script/mantenimiento.gs`), `700fb82` (documentación: este checkpoint + `docs/ARCHITECTURE.md` + `CLAUDE.md`) — ambos en `main` local, no pusheados a `origin/main` (sin pedirse explícitamente esta sesión). |

---

## CHECKPOINT — 2026-09-17 · Confirmado en vivo: validez USICAMM/PROEEB ya desplegada en producción

| | |
|---|---|
| **Fecha** | 2026-09-17 |
| **Sesión** | El checkpoint anterior (16 sep, cont. 3) había dejado sin verificar si el código de validez USICAMM/PROEEB (`aa26bf7`, mismo archivo `.gs` que los Pasos 2-4 de doble registro) había quedado publicado en producción como efecto colateral de esos despliegues. Jorge pidió confirmarlo directamente contra el sistema real en vez de asumir cualquiera de las dos cosas. |
| **Verificación** | `curl` al `doGet` real (`APPS_SCRIPT_URL` de `formacion-docente.html`) — el JSON de respuesta ya trae `valida_usicamm`, `valida_proeeb`, `estado_inscripcion` y `cursos_pasados`, campos que solo existen con el código de `aa26bf7`. **Confirmado: ya está en producción.** `docs/ROADMAP.md` ítem 18 y las 3 menciones correspondientes en `CLAUDE.md` se actualizaron de "código listo, sin desplegar" a "desplegado y confirmado". |
| **Estado real capturado en el catálogo** | 2 cursos vigentes (`ACF-2627-002` "Gamificación como metodología para los proyectos de aula" y `ACF-2627-003` "Uso de la IA para el diseño de material educativo interactivo", ambos con `Valida_USICAMM`/`Valida_PROEEB=TRUE`, 1 inscrito cada uno) y 5 en el historial de "Cursos anteriores" (`AUT-2627-001` a `005`, ninguno válido para USICAMM/PROEEB, entre 16 y 39 inscritos cada uno). Jorge ya dio de alta estos cursos y capturó las columnas nuevas por su cuenta, sin que quedara registrado en una sesión previa. |
| **Hallazgo sin resolver, no asumido** | El curso original `ACF-2627-001` ("Formación Docente en Inteligencia Artificial", Fundación Televisa — el único que existía en el checkpoint del 10 sep) **no aparece ni en vigentes ni en el historial** de esta respuesta. No se investigó la causa (¿`Activo=FALSE` a propósito, curso eliminado de la hoja, otro motivo?) — queda pendiente confirmar con Jorge antes de asumir cualquiera. |
| **Commits** | Pendiente — sesión sin commitear todavía al momento de este checkpoint. |

---

## CHECKPOINT — 2026-09-16 (cont. 3) · Flujo guiado de doble registro (OTDE + plataforma externa) en Formación Docente, con recordatorio automático y confirmación de un toque

| | |
|---|---|
| **Fecha** | 2026-09-16 |
| **Sesión** | Continuación de la sesión del mismo día (checkpoint anterior: validez USICAMM/PROEEB). La preocupación central de Jorge: muchos docentes se registran en el formulario de OTDE pero nunca completan su inscripción real en la plataforma externa del curso (Aula Digital u otra), o al revés, se inscriben directo en la plataforma sin pasar por OTDE — hueco de seguimiento en ambos sentidos. |
| **Paso 2 — flujo guiado de 3 pasos (commit `d0ae2a9`)** | Cuando el curso exige inscripción externa, el wizard agrega un indicador de 3 pasos (Elige → Inscríbete → Avisa a OTDE) con guía visual de 4 íconos y la pregunta "¿Ya te llegó el correo de bienvenida del curso?" — esa confirmación de bienvenida de la plataforma externa es la prueba visual y ligera acordada con Jorge (sin subir archivos), con salida "Todavía no" que no bloquea el registro. Confirmación final con checklist por curso (OTDE / plataforma) y CTA "Ir a inscribirme" si quedó pendiente. Bug bloqueante corregido: tras un registro parcialmente fallido, "Volver a elegir cursos" dejaba el botón `btn-submit` congelado en "Enviando…" — ver `docs/QA-NOTES.md #30`. Botón Atrás navega entre pasos con `history.pushState` sin perder datos capturados. `instructivo-formacion-docente.html` actualizado al nuevo flujo. Verificado contra un backend simulado local (mock), no contra producción. |
| **Paso 3 — `Registro_externo` persistido en `Inscripciones`, columnas por bloques (commit `c236975`)** | `Inscripciones` pasó de 15 a 21 columnas, reorganizadas en bloques (Registro → Participante → Centro de trabajo → Curso → Seguimiento — ver `ENCABEZADOS_INSCRIPCIONES` en `apps-script/formacion-docente.gs`), y el código ahora lee/escribe **siempre por nombre de encabezado**, nunca por posición — reordenar columnas ya no rompe recordatorios ni estadísticas. Columnas de vista `Correo`/`Telefono` nuevas (VLOOKUP en vivo). El servidor decide `Registro_externo` en 3 valores (`Confirmado por docente` / `Pendiente` / `No aplica`, según si el curso tiene `Liga_convocatoria`) y un reenvío del mismo folio solo puede **mejorar** el estado (Pendiente → Confirmado, nunca al revés, nunca duplica folio — `mejorarRegistroExterno_()`). Menú "Reordenar columnas de Inscripciones" ya corrido en producción (369 filas, copia verificada contra respaldo `Inscripciones_respaldo_20260916`). `doPost` ahora toma `LockService` (antes la colisión solo se evitaba enviando los registros en serie desde el cliente) — cierra la ventana de carrera real que produjo el folio duplicado `OTDE-CAP-0089` el 10 sep 2026, ver `docs/QA-NOTES.md #31`. Página: liga directa `?curso=ID` y entrada "¿Ya te inscribiste? Avísanos aquí" para quien se inscribió directo en la plataforma sin pasar primero por OTDE. |
| **Paso 4 — recordatorio automático + confirmación de un toque (commit `c15dd11`)** | A quien quedó en `Registro_externo=Pendiente` se le manda un correo individual (agrupado por docente, máx. 2 por inscripción, nunca el mismo día del registro) al día siguiente del registro y de nuevo a ≤2 días del cierre de inscripción, con un botón firmado (HMAC-SHA256 del folio, `tokenConfirmacion_()`) que abre `formacion-docente.html?confirmar=<folio>&t=<firma>` → `doGet ?action=confirmar` → `Pendiente → Confirmado` sin exponer datos personales, protegido con el mismo `LockService`. Columnas nuevas `Recordatorios_pendiente`/`Fecha_ultimo_recordatorio` (segundo reorden de la hoja → respaldo `Inscripciones_respaldo_20260916_2318`). Solo cursos `Activo=TRUE` con inscripción abierta; `RESERVA_CUOTA_CORREO=20` deja cupo diario para el resto de los Apps Script de la cuenta. Menú nuevo: modo de prueba de correo, recordatorio de prueba por folio, envío manual a pendientes (en modo de prueba no se marcan las columnas). Verificado end-to-end en producción con Jorge tocando el botón real (fila de prueba → Confirmado); modo de prueba desactivado y filas de prueba borradas con su autorización, hoja de vuelta a 11 cursos / 283 docentes / 369 inscripciones. |
| **Verificación** | `git diff --stat` de los 3 commits combinados: 3 archivos, 1,298 inserciones, 225 eliminaciones. Paso 3 y Paso 4 verificados en vivo contra el Sheet y el Apps Script real; Paso 2 solo contra un mock local. |
| **Commits** | `d0ae2a9`, `c236975`, `c15dd11` — los 3 ya en `origin/main`, pusheados durante la sesión a pedido explícito de Jorge. |
| **Pendiente** | Decidir si se renumera una de las dos filas del folio duplicado `OTDE-CAP-0089` (datos no tocados); Paso 5 (reconciliar con la lista de inscritos de Aula Digital cuando CoEEE la tenga disponible); borrar las 2 hojas de respaldo (`Inscripciones_respaldo_20260916` y `..._2318`) cuando ya no se necesiten. La duda sobre si el código de validez USICAMM/PROEEB también quedó publicado al desplegar Pasos 3-4 se confirmó **sí** el 17 sep 2026 — ver el checkpoint de arriba. |

---

## CHECKPOINT — 2026-09-16 · Validez USICAMM/PROEEB e historial "Cursos anteriores" en Formación Docente — código en el repo, pendiente de pegar en Apps Script real

| | |
|---|---|
| **Fecha** | 2026-09-16 |
| **Sesión** | Jorge pidió dos mejoras al catálogo de `formacion-docente.html`: (1) destacar con una etiqueta los cursos que cuentan para USICAMM y/o PROEEB; (2) que el catálogo nunca se vea vacío sin cursos vigentes, mostrando en su lugar los cursos ya ofrecidos (en gris/bloqueados) — y que ese historial aparezca siempre, incluso con cursos vigentes disponibles, para generar el hábito de estar atentos a futuras convocatorias. Al perfilar el segundo punto en una pregunta de seguimiento, Jorge afinó el criterio de "pasado" a un modelo de 3 estados en vez de 2: un curso puede tener su periodo de inscripción cerrado sin que su periodo de desarrollo haya terminado — en ese caso debe seguir visible con una leyenda, no desaparecer del catálogo. |
| **Esquema de datos: 3 columnas nuevas en `Cursos`, retrocompatibles** | `Fecha_limite_inscripcion` (V, opcional — con fallback a `Fecha_inicio` si se deja vacía), `Valida_USICAMM` (W, TRUE/FALSE) y `Valida_PROEEB` (X, TRUE/FALSE, independiente de la anterior — un curso puede tener una, otra, ambas o ninguna). Se agregaron al final de `ENCABEZADOS_CURSOS`, mismo patrón que `Descripcion`/`Dirigido_a` (sep 2026): `obtenerHojaCursos()` las completa solas en la hoja real la próxima vez que corra, sin migrar nada a mano — el único curso real hoy (`ACF-2627-001`) sigue mostrándose igual hasta que Jorge llene las columnas nuevas. |
| **Modelo de 3 estados, calculado 100% por fecha en `doGet()`** | `evaluarEstadoCurso_(row, hoy)` compara `Fecha_fin` (fin del periodo de desarrollo) contra `Fecha_limite_inscripcion` (o `Fecha_inicio` si está vacía): **abierta** (normal, sin cambios visuales) → **cerrada** (venció el límite de inscripción pero el curso sigue en desarrollo — se queda en el catálogo vigente con la leyenda "Inscripciones cerradas · Curso en desarrollo" y sin botón de registro) → **pasado** (también venció `Fecha_fin` — sale del catálogo vigente y entra al historial). Fail-open ante fechas no parseables (`parseFechaSegura_()`, texto libre tipo "Por definir" ya soportado por `formatearFecha()`): un dato incompleto nunca oculta ni bloquea un curso por error. |
| **`doGet()` ahora regresa dos listas** | `cursos` (vigentes, misma lógica de `Activo`/`dentroDeVentanaVisible()` de siempre) y `cursos_pasados` (tope `MAX_CURSOS_PASADOS=6`, ordenado por `Fecha_fin` descendente — sin pasar por `dentroDeVentanaVisible()`, esa ventana es para controlar la aparición de vigentes, no aplica a historial ya cerrado). El tope/orden se calculan en el backend porque `fecha_fin` llega al frontend ya formateada como texto `dd/MM/yyyy`, no ordenable. |
| **Frontend: badge de validez + leyenda de cerrado + sección "Cursos anteriores"** | `.cc-valida-badge` (esquina superior derecha de la tarjeta, espejo de `.cc-sel-badge`) muestra "USICAMM", "PROEEB" o "USICAMM · PROEEB" combinado, siempre visible cuando aplica. `.cc-cerrado-tag` (tono neutro, distinto de `--warn` que ya significa "registro externo") reemplaza el CTA de selección en cursos con inscripción cerrada. Sección nueva `#cursos-pasados-wrap` (`renderCursosPasados()`, tarjetas `.curso-card-historial` en gris con `pointer-events:none`) se muestra siempre que `cursos_pasados.length > 0`, coexistiendo con `#catalogo-vacio` en vez de reemplazarlo. Se cambió el contrato de caché de `sessionStorage` (`{cursos, cursos_pasados}` en vez del arreglo directo de antes) en `cargarCatalogo()`/`actualizarCatalogoEnSegundoPlano()`. |
| **Capa de seguridad en `doPost()` (agregada a pedido explícito de Jorge tras revisar el plan)** | La UI ya deja de ofrecer el clic en cursos pasados, pero `doPost()` no lo validaba del lado servidor — un `curl` directo o una caché vieja del cliente podían colar un registro a un curso ya concluido. `obtenerFilaCurso_()` (reemplaza a `existeCurso()`, que solo regresaba un booleano) ahora regresa la fila completa, y `doPost()` la evalúa con el mismo `evaluarEstadoCurso_()` de `doGet()` — rechaza con un mensaje explícito si `esPasado`. |
| **Verificación** | `node --check` sobre una copia del `.gs` (sintaxis válida) y `new Function()` sobre el `<script>` inline de `formacion-docente.html` (sin errores de sintaxis). Cada cambio se releyó línea por línea contra el archivo real antes de escribirse (no se asumió nada del reporte de exploración previo). **No se probó contra el Apps Script real ni contra datos reales de la hoja** — el código quedó listo en el repo pero aún sin pegar/desplegar en el proyecto real (ver `docs/ROADMAP.md` ítem 18 para el checklist paso a paso pendiente). `git diff --stat`: 2 archivos, 229 inserciones, 36 eliminaciones (commit único, incluye ya la validación de `doPost`). |
| **Commits** | `aa26bf7` — ya en `origin/main`, pusheado durante la sesión a pedido explícito de Jorge. |

---

## CHECKPOINT — 2026-09-10 (cont. 2) · Cierra hueco de avisos duplicados en Ceremonias Cívicas — trigger diario instalado en producción, cascada de avisos y fricción real en el frontend

| | |
|---|---|
| **Fecha** | 2026-09-10 |
| **Sesión** | Jorge detectó con evidencia real (captura de pantalla) que la escuela "Revolución Mexicana" (CCT `15DPR0509L`) acumuló 3 reservas en semanas consecutivas por 3 personas distintas — la primera (31-ago, Erika) nunca llenó la ficha y se quedó "Reservada" para siempre, y ninguna de las 2 siguientes (Janet 14-sep, Nallely 21-sep) vio ningún aviso de que ya había actividad reciente en esa escuela. |
| **Diagnóstico: dos causas combinadas** | (1) `visInstalarTriggerValidacion()` nunca se había instalado en el proyecto real de Apps Script — confirmado por Jorge en vivo revisando la pestaña "Activadores" — así que `visMarcarNoRealizadas_()` nunca corrió y la reserva de Erika nunca "envejeció". (2) Aunque el trigger hubiera corrido a tiempo, el hueco de diseño seguía ahí: todos los avisos de `ceremonias-civicas.html` (badge del autocomplete, `visRevisarConflicto()`, contador de cobertura) solo comparaban contra `estatus === 'Realizada'` — una fila "Reservada" de otra semana, completada o no, era invisible. Causa raíz completa en `docs/QA-NOTES.md #28`. |
| **Fix operativo (Jorge, en el editor real de Apps Script)** | Instaló `visInstalarTriggerValidacion()` y lo confirmó en "Activadores"; corrió `visMarcarNoRealizadas_()` una vez para limpiar lo atorado (la fila de Erika pasó a "No realizada"; Janet/Nallely siguieron "Reservada" por estar dentro de su ventana de 3 días — correcto). Encontró en el camino que esa función no aparecía en el desplegable de funciones del editor por terminar en guion bajo — se envolvió en una función temporal sin guion bajo para poder ejecutarla. Causa raíz y fix documentados en `docs/QA-NOTES.md #29`. |
| **Fix de frontend, todo en `ceremonias-civicas.html` sin tocar el `.gs`** | `visRevisarConflicto()` pasó a una cascada de 3 niveles al reservar — "Reservada" **vencida** (+3 días sin ficha, calculado 100% en el cliente con `visEsVencida()`, sin depender de que el trigger del backend esté corriendo) > "Reservada" **pendiente** (otra semana, dentro de su ventana) > "Realizada" (rama original). Motivo de revisita pasó de opcional a obligatorio; se agregó un checkbox de confirmación explícita que se resetea con cada cambio de CCT/fecha — fricción real, no un candado duro. `enviarReserva()` ahora refresca `visReservasCache` justo antes de validar. |
| **Refinamiento tras ver el resultado en vivo (feedback de Jorge, mismo día)** | La etiqueta de cobertura se movía a una función compartida (`visEtiquetaCobertura()`, con fecha y nombre de quien reservó) y se trasladó de la sugerencia del autocomplete (que desaparece al elegir la escuela) a la caja de estatus verde que sí queda persistente tras seleccionar el CCT. La tabla de historial marca "⚠ Sin ficha" en cualquier "Reservada" vencida. "Ceremonia cívica semanal" quedó preseleccionada en Tipo de visita (única visita real que resta el ciclo), sin quitar "Inicio de ciclo escolar" del `<select>` — se necesitará de nuevo en el ciclo 2027-2028. |
| **Verificación** | Servidor local + datos reales de producción (solo lectura, ninguna reserva de prueba se envió): badge y cascada de avisos citando a Erika/Nallely por nombre y fecha real; `validarReservaForm()` confirmado en consola devolviendo `false` sin motivo/checkbox y `true` con ambos; fila de Erika confirmada "No realizada" en el Sheet real tras la limpieza retroactiva; recarga de la tabla de historial confirmando "⚠ Sin ficha" solo en la fila vencida, no en las de Janet/Nallely. `git diff --stat` del primer commit: `ceremonias-civicas.html`, 115 inserciones, 10 eliminaciones. |
| **Commits** | `05d6a8d` (cascada de avisos + checkbox + badges) y `50db3ea` (badge persistente con fecha/nombre, motivo obligatorio, tipo de visita preseleccionado) — ambos ya en `origin/main`, pusheados durante la sesión a pedido explícito de Jorge en cada caso. |

---

## CHECKPOINT — 2026-09-10 (cont.) · Dos ajustes de UI en el catálogo de Formación Docente: descripción expandible y aviso de registro externo dinámico por curso

| | |
|---|---|
| **Fecha** | 2026-09-10 |
| **Sesión** | Continuación de la sesión anterior del mismo día (reactivación de la card). Jorge reportó dos problemas de UI al revisar el catálogo con el primer curso real: (1) descripciones de más de 3 líneas se veían cortadas sin forma de leerlas completas; (2) el aviso amarillo "Este formulario no es tu inscripción oficial al curso" aparecía siempre, aplicara o no al curso elegido. |
| **Descripción expandible ("Leer más")** | `.cc-desc` sigue recortando a 3 líneas, pero ahora se mide con `scrollHeight > clientHeight` (dentro de `requestAnimationFrame`, tras insertar la tarjeta en el DOM) si el texto realmente se cortó — solo entonces se agrega el botón "Leer más"/"Leer menos" (`.cc-desc-toggle`, quita `-webkit-line-clamp` con la clase `.cc-desc.expanded`). El botón usa `stopPropagation()` para no disparar la selección de la tarjeta. |
| **Aviso de registro externo, de estático a dinámico y en tres señales** | Primer intento: solo ocultar/mostrar el aviso general (`#aviso-legal`, oculto por defecto) según si algún curso seleccionado tiene `Liga_convocatoria` (`actualizarResumenSticky()`). Jorge preguntó si se vería "más pro" si el aviso fuera específico al curso en vez de solo general arriba — se acordó agregar, además del aviso general: un badge ámbar en la tarjeta del catálogo (`.cc-externo-tag`, "Registro en plataforma externa") y una nota por curso en el panel "Resumen de selección" (`.rsb-nota-externo` en escritorio, ícono `.rs-chip-warn` en el chip móvil por espacio). Las tres señales comparten el mismo ícono (`ICON_EXTERNO`) y la misma condición (`liga`/`Liga_convocatoria`, no `Registro_previo_requerido` — ese campo solo fuerza el paso intermedio de registro, es un condicional distinto). Detalle completo de ambos patrones en `docs/DESIGN_SYSTEM.md`. |
| **Verificación** | El catálogo real solo tiene un curso (sin liga), así que no bastaba para probar el badge/nota/aviso en combinación — se armó un catálogo de prueba (`mock.json` con 2 cursos, uno con `liga_convocatoria` lleno) en un servidor local aparte, con `js/cct-db.js` copiado para que la página cargara sin errores, todo en el directorio de trabajo temporal (no en el repo). Verificado visualmente: el badge aparece solo en la tarjeta con liga; al seleccionar ambos cursos, la nota "Requiere registro externo" aparece solo bajo el curso correcto en el panel de escritorio y el ícono de advertencia aparece solo en su chip (probado también en viewport de 700px de ancho); el aviso general aparece al tener cualquiera de los dos seleccionados. Carpeta de prueba y servidor local eliminados al terminar. `git diff --stat`: 2 archivos (`formacion-docente.html`, `docs/DESIGN_SYSTEM.md`), 117 inserciones, 4 eliminaciones. |
| **Commits** | Pendiente — sesión sin commitear todavía al momento de este checkpoint. |

---

## CHECKPOINT — 2026-09-10 · Card de Centro de Formación Docente reactivada en Oficina Virtual, con primer curso real y fix de `formatearFecha`

| | |
|---|---|
| **Fecha** | 2026-09-10 |
| **Sesión** | Jorge pidió habilitar la card de Formación Docente en `oficina-virtual.html`. Verificación en vivo contra el `doGet` de `formacion-docente.gs` confirmó que el catálogo seguía vacío (`{"status":"ok","cursos":[]}`, mismo estado que el 1 sep 2026) — se acordó dar de alta un curso real primero en vez de reactivar la card con la tienda vacía. |
| **Primer curso dado de alta, con dos problemas reales encontrados al revisarlo en la hoja `Cursos`** | Jorge agregó "Formación Docente en Inteligencia Artificial" (Fundación Televisa, Virtual Sincrónica) directo en el Sheet, pero le faltaba `ID_Curso` (por eso no aparecía en el catálogo — `doGet` exige esa columna no vacía) y su `Categoria` era "Taller", que no está en las 7 categorías oficiales de `PREFIJOS_CATEGORIA`. Con su confirmación se cambió a "Acción formativa" y se generó el ID (`ACF-2627-001`) desde el menú "OTDE Formación → Generar ID de cursos faltantes". |
| **Columnas nuevas `Descripcion`/`Dirigido_a` en `Cursos`** | A pedido de Jorge, para mostrar propósito/objetivo del curso y público objetivo en la tarjeta del catálogo — dos columnas separadas (no una sola de texto libre combinado), agregadas al final de `ENCABEZADOS_CURSOS` (columnas T/U) para no correr los índices posicionales que ya usa `doGet`/los recordatorios. Frontend: `.cc-desc` (descripción truncada a 3 líneas) y una pill nueva con ícono de audiencia (`ICON_AUDIENCE`) para `dirigido_a`, ambas condicionales a que el campo venga lleno. Detalle en `docs/ARCHITECTURE.md §12`. |
| **Bug real encontrado y corregido: `formatearFecha()` con texto libre** | Jorge quería poder poner "Por definir" en `Fecha_inicio` para este curso sin fecha confirmada aún. Primer intento mostró `"fecha_inicio":"31/12/1969"` en vez del texto — `Utilities.formatDate()` no lanza excepción con un `Invalid Date`, lo formatea en silencio como época 0. Fix: `formatearFecha()` ahora revisa `isNaN(fecha.getTime())` antes de formatear y devuelve el texto tal cual si no es una fecha real. Detalle completo con causa raíz en `docs/QA-NOTES.md #27`. |
| **Reactivación de la card** | `oficina-virtual.html` revertido exactamente al estado previo al commit `da968f6` (que la había deshabilitado el 1 sep 2026): `<a href="formacion-docente.html" class="area-card">` con `<div class="ver-mas">Ver catálogo →</div>`, en vez de la `<div class="area-card ov-proximamente">` con texto "Próximamente". |
| **Verificación** | Cambios de `.gs` desplegados en vivo dos veces vía Chrome automatizado (versión 13: columnas nuevas; versión 14: fix de `formatearFecha`) — cada una confirmada con `curl` contra el endpoint real antes de seguir. Servidor local (`python3 -m http.server`, detenido al terminar) usado para revisar visualmente `oficina-virtual.html` y `formacion-docente.html` antes de tocar producción: la card se ve activa ("SERVICIO" · "Ver catálogo →"), enlaza al catálogo, y la tarjeta del curso muestra correctamente descripción, "Por definir", modalidad y "Docentes y directivos". `git diff --stat` de código+docs (sin contar este mismo archivo, que se sigue editando): 6 archivos, 75 inserciones, 14 eliminaciones. |
| **Commits** | Pendiente — sesión sin commitear todavía al momento de este checkpoint, a la espera de confirmación explícita de Jorge. |

---

## CHECKPOINT — 2026-09-08 · Presentación "Evaluación Institucional Final 2025-2026" publicada en Planeación, con vista previa sin convertir a PDF

| | |
|---|---|
| **Fecha** | 2026-09-08 |
| **Sesión** | Jorge tenía en la raíz del repo `Evaluación Final 25-26 080926.pptx` (mensaje de la Dra. Avelina Galindo Celix invitando a la transmisión en vivo de la Evaluación Institucional Final del ciclo) y pidió subirla al sitio, proponiendo él mismo que viviera en `planeacion.html` — confirmado: el "Objetivo" de esa página ya menciona explícitamente "informes de evaluación" y "Planeación Institucional" (SSII). |
| **Hallazgo que cambió el enfoque: el archivo pesaba 96.9MB** | Rozaba el límite de 100MB por archivo de GitHub (mismo límite que ya hizo excluir del repo un PDF de 248MB, ver `.gitignore`). Inspección con `python-pptx`/Pillow (sin LibreOffice/`soffice` instalado localmente) mostró que 44 de sus 50 imágenes internas eran fotos guardadas como PNG sin comprimir — se recodificaron a JPEG calidad 85 editando el ZIP OOXML directamente (renombrado interno + fix de referencias en todos los `.xml`/`.rels`), bajando el archivo a **13.9MB** sin pérdida perceptible. Detalle técnico completo, incluida la verificación de que ninguna otra extensión del paquete podía referenciar un nombre de imagen, en `docs/ARCHITECTURE.md §25`. |
| **Nueva sección "Documentos" en `planeacion.html`** | Primera sección de este tipo en una página de área simple (antes solo `protocolos.html`, un hub aparte, tenía tarjetas de documento). `.documento-card` con ícono, título, descripción adaptada del mensaje original (en tiempo pasado/evergreen, ya que la transmisión en vivo ya había ocurrido) y botón "Descargar presentación" — grid listo para más documentos futuros de esta Subjefatura. Archivo en `pdfs/planeacion/Evaluación Final 25-26 080926.pptx` (nombre conservado tal como se entregó, mismo criterio que `pdfs/cte/...`). |
| **Vista previa sin convertir a PDF, en un segundo momento de la misma sesión** | Jorge preguntó si era viable una vista previa antes de descargar. Inspección con `python-pptx` reveló que las 45 diapositivas son, cada una, una sola imagen a pantalla completa (los demás shapes son placeholders vacíos) — se extrajeron esas 45 imágenes (`images/planeacion/evaluacion-institucional-2025-2026/slide-01.jpg`…`slide-45.jpg`, 7.1MB totales) y se armó un lightbox vanilla-JS (miniatura clicable + botón "Vista previa", imagen grande, flechas, contador "N/45", teclado, clic-fuera para cerrar) sin cargar las 45 imágenes por adelantado. Esta técnica no generaliza a cualquier `.pptx` — solo aplica cuando cada diapositiva es de verdad una sola imagen; ver la salvedad completa en `docs/ARCHITECTURE.md §25`. |
| **Verificación** | `python-pptx` confirmó que el `.pptx` comprimido abre sin errores y conserva 45 diapositivas con sus imágenes resolviendo correctamente; revisión visual de una diapositiva extraída sin artefactos perceptibles. La sección "Documentos" sí se probó en Chrome real (servidor local, desktop y verificación del enlace de descarga) antes del primer commit; el lightbox del segundo commit **no se probó visualmente** porque la extensión Claude in Chrome estaba desconectada — se verificó en su lugar con `tidy -e` (sin errores estructurales reales), `node --check` sobre el `<script>` del lightbox (sintaxis limpia) y balance de `<div>`/IDs únicos; publicado a pedido explícito de Jorge sin esperar esa verificación visual — **queda pendiente que él mismo confirme en producción** que el lightbox funciona como se espera. |
| **Commits** | `6186123` (sección Documentos + `.pptx` comprimido) y `36d7e69` (vista previa lightbox + 45 imágenes) — ambos ya en `origin/main`, pusheados durante la sesión a pedido explícito de Jorge en cada caso. |

---

## CHECKPOINT — 2026-09-07 · Videotutorial de Microsoft Authenticator + avisos de multi-dispositivo en `correo.html`

| | |
|---|---|
| **Fecha** | 2026-09-07 |
| **Sesión** | Jorge reportó que configurar la app de autenticación de Microsoft (Authenticator) sigue siendo un dolor de cabeza recurrente para directores y docentes, pese a que OTDE ya grabó y compartió un videotutorial en YouTube por fuera del sitio. Pidió ubicar dónde incorporarlo (¿`otde.html`, `oficina-virtual.html`, o directo en el espacio de correos institucionales?) y, aparte, documentar que Authenticator admite 2 o más dispositivos por cuenta — un cuello de botella real: normalmente solo el director(a) lo configura en su celular, y al cambiar de centro de trabajo se lleva el único dispositivo autorizado, dejando a la escuela sin acceso al correo. |
| **Ubicación decidida: `correo.html`, no `otde.html` ni `oficina-virtual.html`** | Exploración previa confirmó que `otde.html` ya no tiene contenido de correo desde la migración del 27 ago 2026, y `oficina-virtual.html` es solo un hub de tarjetas sin espacio para contenido explicativo. `correo.html` es el único lugar donde ya se habla de "verificación en dos pasos"/autenticación (panel "Eliminar Método de Autenticación"). |
| **Bloque nuevo insertado antes del switcher de los 4 trámites** | Entre el `.highlight-box` de "Sobre tus datos" y `.correo-panel-switcher` — visible sin importar cuál de los 4 trámites (Alta/Cambio/Eliminar método/No puedo acceder) esté viendo el usuario, porque el problema de Authenticator no es exclusivo de ninguno. CSS `.video-wrapper`/`.video-wrapper iframe` copiado inline desde `otde.html` (líneas 140-155 ahí — no vive en `styles.css`), video embebido (`https://www.youtube.com/embed/afue5xqv-sQ`, id compartido por Jorge). |
| **Aviso de multi-dispositivo, como caja destacada (no párrafo suelto)** | A petición explícita de Jorge (no solo texto corrido): `.highlight-box` reutilizado tal cual, mismo patrón guinda ya usado en toda la página para callouts — explica que se pueden configurar 2+ dispositivos con la misma cuenta y recomienda que, además del director(a), otra persona de la escuela también configure la app. |
| **Segunda nota agregada tras feedback en vivo: "información muy digerida" para docentes** | Jorge revisó el bloque en el servidor local y pidió una nota adicional simple para el caso de quien ya tenía la app configurada pero ya no tiene acceso a ese dispositivo — segundo `.highlight-box` que los dirige directo al botón "Eliminar Método de Autenticación" ya existente en el switcher, en vez de repetir el video. |
| **Verificación** | `git diff --stat correo.html`: 1 archivo, 38 inserciones, 0 eliminaciones — solo contenido nuevo, sin tocar formularios/JS/backend. Servidor local (`python3 -m http.server 8000` en `seprn-sitio/`, detenido al terminar) usado por Jorge para revisar visualmente el bloque completo en `http://localhost:8000/correo.html`; confirmó "se ve bien" antes de pedir la segunda nota. No se probó en dispositivo móvil real ni se verificó que el ID de video (`afue5xqv-sQ`) sea el definitivo — Jorge lo compartió directo desde el embed de YouTube. |
| **Commits** | Pendiente — sesión sin commitear todavía al momento de este checkpoint. |

---

## CHECKPOINT — 2026-09-06 · Feedback de una semana en producción de Ceremonias Cívicas: bug de fichas duplicadas, UX de navegación, paginación, panel de cobertura y fotos por semana

| | |
|---|---|
| **Fecha** | 2026-09-06 |
| **Sesión** | Jorge trajo feedback real tras una semana de `ceremonias-civicas.html`/`ficha-ceremonias-civicas.html` en producción: un bug de datos (fichas duplicadas) y una lista de hallazgos de UX. Se trabajó una pieza a la vez, empezando por el bug más urgente. |
| **Bug crítico: envíos duplicados de la ficha (hasta 5x fotos/datos)** | Causa raíz confirmada en el propio código: el timeout de 120s del cliente (`FICHA_TIMEOUT_ENVIO_MS`) podía vencer mientras el servidor seguía guardando de fondo (hasta 68.7s con 20 fotos, ya documentado en `visitas-jefes.gs` desde el fix de rendimiento del 31 ago), y el mensaje de error invitaba a "volver a intentarlo" — cada reintento repetía la subida completa porque `visDoPostFicha_()` no tenía `LockService` ni ninguna verificación de "esto ya se guardó". Fix: `LockService.getScriptLock()` envolviendo toda la función (mismo patrón que `visDoPostReservar_()`), chequeo de `estatusActual === 'Realizada'` que responde `status:'ya_enviada'` sin volver a tocar Drive, y la validación de campos obligatorios movida *antes* de `visSubirFotos_()` (antes subía fotos aunque faltaran campos, dejándolas huérfanas). Frontend (`ficha-ceremonias-civicas.html`) trata `ya_enviada` como éxito, no como error. Ver `docs/QA-NOTES.md #26`. |
| **CTA de reservar + buscador del historial, confundidos entre sí** | Jorge reportó que el público (no siempre nativo digital) no intuía que el botón "Reservar visita" había que abrirlo, y confundía el buscador de historial (`#vis-buscar`, pide CCT/escuela/sector) con el lugar para reservar. Fix: el formulario de reserva ahora empieza **expandido por defecto** (ya no hay que descubrir que es clicable) y el buscador ganó una etiqueta ("Buscar en el historial de abajo") más una aclaración explícita debajo ("Esta casilla solo filtra la tabla de abajo — no reserva una visita"). |
| **Paginación del historial** | La tabla de disponibilidad/histórico crecía sin límite (todo `visReservasCache` se renderizaba de una sola vez). Se agregó paginación 100% cliente (`VIS_TABLA_PAGE_SIZE`=20, `visPaginaActual`, controles "← Anterior"/"Siguiente →" con clamp defensivo si la página pedida queda fuera de rango), sin tocar el backend — los datos ya se cargaban completos en memoria. La búsqueda resetea a la página 1 (`visBuscarCambio()`) para no dejar al usuario viendo una página vacía. |
| **Estadísticas ocultas → reactivadas, y prueba temporal de panel público** | El "Panel de cobertura" (visitas por persona/sector) estaba comentado a propósito desde el 30 ago para no mostrarlo en las primeras semanas del ciclo — con una semana de datos reales ya en producción, Jorge pidió reactivarlo. Además pidió probar unos días sin el candado de clave: nueva `visConfigurarDashboardPublico(activo)` en el backend (Script Property `DASHBOARD_PUBLICO`, revisada por `visObtenerDashboard_()` antes de exigir `DASHBOARD_TOKEN`) y se quitó el campo de clave del HTML, dejando solo un botón directo "Ver panel". Diseñado para revertirse fácil: `visConfigurarDashboardPublico(false)` regresa el candado sin perder el valor de la clave ya guardada (aunque el campo de clave del HTML habría que regresarlo aparte si se revierte del todo). |
| **Toasts de confirmación/error, poco notorios** | El bug de fichas duplicadas también venía en parte de que el usuario no estaba seguro de si su envío ya había funcionado. En `ceremonias-civicas.html` y `ficha-ceremonias-civicas.html`, el mensaje de éxito/error pasó de texto plano a un bloque con ícono circular (✓/!), sombra, borde lateral grueso y `scrollIntoView` automático al mostrarse — acotado a estas dos páginas vía selectores por ID (`#reservar-msg`/`#ficha-msg`), sin tocar la clase compartida `.soporte-submit-msg` de `styles.css` que usan los otros 4 trámites. |
| **Guía paso a paso agregada; banner principal se mantiene completo (decisión de Jorge)** | Se agregó un bloque "Cómo funciona, paso a paso" (4 pasos: reservar → anotar folio → visitar y tomar fotos → llenar la ficha) antes del formulario. Se probó primero resumir el párrafo largo del `.servicio-header` (`docs/ROADMAP.md` ítem 14) a una sola oración, pero Jorge pidió explícitamente restaurar el texto institucional completo — la mitigación real del problema de UI/UX de ese ítem terminó siendo el formulario expandido por defecto + la guía nueva, no acortar el contenido. |
| **Fotos de Drive organizadas por semana** | A pedido de Jorge: `visSubirFotos_()` ya no sube todo a la raíz de "Fotos de Visitas Jefes" — nueva `visObtenerCarpetaSemana_()` crea/reutiliza una subcarpeta `Semana YYYY-MM-DD` (lunes de la semana de la fecha planeada, mismo criterio que la columna "Semana (lunes)" de la hoja) y comparte esa subcarpeta una sola vez por envío (mismo criterio de rendimiento que la carpeta base). Las fotos subidas antes de este cambio se quedan donde estaban — esto solo organiza los envíos nuevos. |
| **Confirmado sin cambios: duplicados de reserva** | Jorge preguntó si ya se bloqueaban reservas duplicadas pasadas o futuras — confirmado que sí, sin tocar código: `visExisteReservaActiva_()` ya bloquea por CCT + semana ISO (no por fecha exacta), sin importar si es pasada o futura. |
| **Verificación** | Edge case testing: 20/20 pruebas de lógica pura (matemática de paginación, cálculo de lunes-de-semana, tabla de decisión de idempotencia) corridas con Node fuera del navegador, más `node --check` sobre los 3 archivos editados (sintaxis limpia). E2E: navegador real contra un servidor local (`python3 -m http.server 8080` en `seprn-sitio/`, cerrado al terminar) con los **44 registros reales** que ya existían en producción (vía el GET de solo lectura `?action=disponibilidad`, sin escribir nada) — paginación (20/20/4), toggle del formulario, filtro de búsqueda, y los dos toasts confirmados visualmente; el botón "Ver panel" respondió correctamente "no disponible" porque el backend en producción **todavía no tiene este código desplegado** (comportamiento esperado, sin fuga de datos). Regresión: `git diff --stat` confirma que `styles.css`/`js/cct-db.js`/`js/tramites-shared.js`/los demás `.gs` no se tocaron, y grep confirma que los IDs/clases nuevos no existen en ninguna otra página del sitio. **No se probó el envío real (POST) de una reserva o una ficha** — habría escrito una fila falsa en la Sheet de producción real, y el backend con los fixes aún no está desplegado ahí. `git diff --numstat`: `apps-script/visitas-jefes.gs` +110/-48, `ceremonias-civicas.html` +167/-31, `ficha-ceremonias-civicas.html` +42/-0. |
| **Pendiente real para Jorge** | Pegar `apps-script/visitas-jefes.gs` completo en el editor de Apps Script del proyecto real y **redesplegar nueva versión** (sin esto, el bug de duplicados sigue vivo en producción); correr una vez `visConfigurarDashboardPublico(true)` para activar el panel público; probar de punta a punta con un folio real (enviar ficha, reenviar el mismo folio, confirmar `ya_enviada` sin fotos duplicadas en Drive); decidir cuándo revertir el panel a acceso restringido. |
| **Commits** | Pendiente — sesión sin commitear todavía al momento de este checkpoint. |

---

## CHECKPOINT — 2026-09-03 (cont.) · Ejecución y verificación en vivo del "planchado" de Soporte y Formación Docente (código del checkpoint anterior, mismo día)

| | |
|---|---|
| **Fecha** | 2026-09-03 |
| **Sesión** | Continuación directa del checkpoint anterior (mismo día): Jorge pegó `soporte-remoto.gs` y `formacion-docente.gs` completos en sus dos proyectos reales de Apps Script pero no pudo correr `sopConfigurarValidacionYSemaforo()`/`fdConfigurarValidacionYSemaforo()` ni confirmar visualmente el resultado, y pidió que se hiciera por él dándole acceso a su cuenta. |
| **Sin acceso a credenciales — resuelto con Chrome automatizado, no con la cuenta de Jorge** | Se explicó que no es posible ni conveniente operar con las credenciales de Jorge directamente; en su lugar se usó la extensión **Claude in Chrome** sobre el navegador de Jorge, ya con su sesión de Google autenticada — cada acción quedó visible en pantalla para él, sin que ninguna credencial pasara por esta sesión. |
| **`sopConfigurarValidacionYSemaforo()` corrida en el proyecto real "Soporte Técnico Remoto"** | Seleccionada desde el dropdown de funciones del editor de Apps Script y ejecutada — "Se completó la ejecución" sin errores en el Registro de ejecución (confirmado también en "Descripción general" del proyecto: última ejecución con hora exacta, sin errores en el resumen de 7 días). |
| **`fdConfigurarValidacionYSemaforo()` corrida en el proyecto real "Formacion Docente - Backend"** | Mismo procedimiento — "Se completó la ejecución" sin errores. |
| **Verificación visual en vivo contra las dos hojas reales** | `Solicitudes_Soporte_2026`: el dropdown de `Estatus` abre la lista real de 5 valores (Pendiente de validar/Validado/En atención/Resuelto/Rechazado) y una fila ya resuelta se pinta de verde — semáforo confirmado, no solo el dropdown —, más flechas de dropdown visibles en `Urgencia`/`Tipo de ayuda`. `Formacion_Docente_2026_2027` (hoja `Cursos`): flechas de dropdown visibles en las 5 columnas esperadas (`Categoria`/`Modalidad`/`Requiere_codigo_asistencia`/`Activo`/`Registro_previo_requerido`). No se probó en esta ronda el diálogo de protección de solo aviso en ninguna de las dos hojas (a diferencia de Mantenimiento, donde sí se intentó borrar una columna protegida) — queda como el único punto de este ítem sin verificar en vivo. |
| **Sin cambios de código ni de datos** | Sesión puramente operativa: no se tocó ningún archivo del repo (`git status` limpio antes y después) ni se modificó ningún valor real en las hojas — el único acercamiento a editar una celda (`Estatus` de una fila real de Soporte) se abrió para ver las opciones del dropdown y se cerró con Escape sin seleccionar nada. |
| **Verificación** | `git status`/`git diff` confirmando árbol de trabajo limpio antes de tocar documentación. Ejecuciones confirmadas por el propio "Registro de ejecución" de Apps Script (texto exacto "Se completó la ejecución"). Dropdown y semáforo de Soporte confirmados abriendo la lista real y observando el color de fondo de una fila ya resuelta, no solo inferidos del código. |
| **Commits** | Pendiente — sesión sin commitear todavía al momento de este checkpoint. |

---

## CHECKPOINT — 2026-09-03 · "Planchado" de Soporte Técnico y Formación Docente (cierra el ítem 16 con los 5 trámites cubiertos)

| | |
|---|---|
| **Fecha** | 2026-09-03 |
| **Sesión** | Jorge pidió retomar `docs/ROADMAP.md` ítem 16 — los dos trámites que faltaban del piloto de "planchado" (Mantenimiento/Asesorías/Correo, 1 sep 2026): Soporte Técnico Remoto y Centro de Formación Docente. Se investigó primero el patrón exacto ya implementado 3 veces (código real de `manConfigurarValidacionYSemaforo()`/`aseConfigurarValidacionYSemaforo()`/`configurarValidacionYSemaforo()`) antes de replicarlo. |
| **`sopConfigurarValidacionYSemaforo()` nueva en `apps-script/soporte-remoto.gs`** | `Estatus` ya tenía dropdown **duro** desde antes (a diferencia de Mantenimiento/Asesorías) — esta ronda solo le agregó el semáforo de color (mismos 5 colores, `ESTADOS_SOP_VALIDOS` es idéntico). Dropdowns suaves nuevos en `Urgencia` (Alta/Media/Baja) y `Tipo de ayuda` (7 valores), catálogos confirmados contra los `<select>` reales de `soporte.html`. Protección de solo aviso en `Fecha`/`Folio`/`Notificación de cierre enviada` — sin `Contactos_Zona_Sector` ni Oficio en este trámite, nada más que proteger. Item nuevo en el menú "OTDE Soporte" → "Aplicar validación y semáforo". |
| **`fdConfigurarValidacionYSemaforo()` nueva en `apps-script/formacion-docente.gs`** | Alcance reducido a la hoja `Cursos` — `Docentes`/`Inscripciones` son datos transaccionales/computados, no captura manual. Sin campo tipo "Estatus": `Activo` ya cumple ese papel, sin semáforo de color. Dropdowns suaves en `Categoria` (catálogo de `PREFIJOS_CATEGORIA`), `Modalidad`, `Requiere_codigo_asistencia`, `Activo`, `Registro_previo_requerido`. Protección de solo aviso en `ID_Curso` y las 3 columnas `Recordatorio_*_enviado`. Item nuevo en el menú "OTDE Formación" → "Aplicar validación en Cursos". |
| **Dos catálogos sin fuente de verdad en el código, confirmados con Jorge en vez de inferirse** | `Modalidad` (Formación Docente) no tiene ningún `<select>` ni catálogo cerrado en el repo — Jorge confirmó Virtual/Presencial/Híbrido. `Requiere_codigo_asistencia`/`Activo`/`Registro_previo_requerido` se leen con `String(row[x]).trim().toUpperCase() === 'TRUE'`, que funciona igual con checkbox nativo de Sheets o con texto escrito a mano — no era deducible del código cuál de los dos es. Jorge confirmó que son texto a mano, así que sí se les agregó la lista suave `['TRUE','FALSE']` (de haber sido checkbox nativo, un dropdown de lista se lo habría quitado). |
| **Sin verificación en vivo contra las hojas reales, a diferencia de las 3 rondas anteriores** | Esta sesión no tuvo acceso a los Sheets reales de Soporte/Formación Docente — los encabezados se confirmaron contra los arreglos `ENCABEZADOS_SOPORTE`/`ENCABEZADOS_CURSOS` ya trackeados en el repo (fuente de verdad de lo que el auto-heal de cada proyecto ya escribió en producción), y los catálogos cerrados contra los `<select>` reales de `soporte.html`/`PREFIJOS_CATEGORIA` — pero no se abrió ninguna hoja en el navegador. **El código quedó listo en el repo, sin pegar ni correr en los proyectos reales de Apps Script** — a diferencia de Mantenimiento/Asesorías/Correo (desplegados y verificados en vivo en sus propios checkpoints), aquí el primer chequeo visual (dropdowns/semáforo/protección donde se espera) lo hace Jorge al desplegar. |
| **Manual visual — secciones 3 y 5 de `docs/manual-bases-tramites.html` completadas** | Reemplazados ambos placeholders "pendiente" por sus tablas columna-por-columna (Soporte reusa el semáforo de 5 colores de Mantenimiento/Asesorías; Formación Docente sin tabla de semáforo, por no tener columna tipo Estatus). Índice del documento actualizado — ya no queda ningún `estado-pendiente`. |
| **`docs/ARCHITECTURE.md §24` y `docs/ROADMAP.md` ítem 16 actualizados** | §24 ganó dos párrafos nuevos (mismo formato que Mantenimiento/Asesorías/Correo) más una nota explícita sobre la falta de verificación en vivo de esta ronda. Ítem 16 de `docs/ROADMAP.md` tachado como resuelto, con el pendiente real (desplegar y verificar en vivo) dejado explícito en vez de darlo por cerrado sin más. |
| **Verificación** | `node --check` sobre copias `.js` de ambos `.gs` editados (Apps Script no es un runtime que se pueda ejecutar localmente) — sintaxis correcta en los dos. Balance de `<div>` verificado en `docs/manual-bases-tramites.html` (46 apertura / 46 cierre) tras las ediciones. Sin pruebas contra Apps Script/Sheets reales — ver hallazgo de arriba. `git diff --stat`: `apps-script/soporte-remoto.gs` +57 líneas, `apps-script/formacion-docente.gs` +39 líneas, `docs/manual-bases-tramites.html` +64/-4, `docs/ARCHITECTURE.md` +61/-4, `docs/ROADMAP.md` +28/-11, `CLAUDE.md` +25/-1. |
| **Commits** | Pendiente — sesión sin commitear todavía al momento de este checkpoint. |

---

## CHECKPOINT — 2026-09-01 (cont. 3) · "Planchado" de Correo Institucional (4 tipos) y su sección del manual visual

| | |
|---|---|
| **Fecha** | 2026-09-01 |
| **Sesión** | Jorge pidió "el mismo tratamiento para Correos" — tercer trámite del piloto (`docs/ROADMAP.md` ítem 16), el más grande de los tres por tener 4 hojas (Alta/Cambio de Contraseña/Reset 2FA/Incidencias) en un solo proyecto de Apps Script en vez de uno por trámite. |
| **Hallazgo antes de escribir código: `Estado general` ya tenía dropdown** | Al leer `Alta.gs`/`CambioContrasena.gs`/`Reset2FA.gs`/`Incidencias.gs` completos se confirmó que `Estado general` en las 4 hojas ya tenía su propio dropdown desde agosto (`altaAplicarValidacionEstado()` y equivalentes, documentado en `docs/ARCHITECTURE.md §20`) — el ítem 16 de `docs/ROADMAP.md`, escrito en el checkpoint anterior de esta misma sesión, decía lo contrario ("texto libre, no convertirlo en dropdown"), confundiendo cómo se *muestra* el estatus en `oficina-virtual.html` (sin badge de color, por ser 4 vocabularios distintos) con si la *hoja* tenía dropdown. Corregido ese ítem antes de continuar. |
| **`configurarValidacionYSemaforo()` nueva en `apps-script/correo/Config.gs`** | Utilidades compartidas (`aplicarValidacionListaSuave_()`, `aplicarSemaforoPorValor_()`, `protegerColumnaAutomatica_()`) en un solo lugar — a diferencia de Mantenimiento/Asesorías (proyectos separados, cada helper duplicado), aquí las 4 hojas viven en el mismo proyecto. Cada `Xxx.gs` ganó su propia `xxxAplicarSemaforoYProteccion_()`: semáforo de color sobre el dropdown ya existente de `Estado general`; dropdowns suaves nuevos en `Tipo de Cuenta`/`¿Cuenta lista?` (Alta), `¿Cuenta lista?` (Cambio), `¿Reset listo?` (Reset — el disparador real de ese tipo, marcado en amarillo en el manual en vez de verde), `¿Cuenta lista?` (Incidencias); protección de solo aviso en `Fecha`/`Folio`/`Usuario enviado`/`Fecha de entrega`/`Estado general` de las 4, más `Dominio` en Alta/Cambio. `Usuario asignado`/`Contraseña asignada` (los campos que Marcos llena a mano para disparar el correo de credenciales) y `NP SIGEE` se dejan libres a propósito. Detalle completo en `docs/ARCHITECTURE.md §24`. |
| **Verificado en vivo contra la hoja real antes de escribir código** | `Solicitudes_Correo_2026_2027` abierta en el navegador: las 4 pestañas (Alta, Cambio de Contraseña, Reset 2FA, Incidencias) confirmadas columna por columna contra los `ENCABEZADOS_*` de cada archivo. Reset 2FA ya tenía una fila real (`OTDE-2FA-0001`) — no se creó ninguna fila de prueba en ninguna de las 4 hojas. |
| **Ejecución y verificación en el proyecto de Apps Script real** | Los 5 archivos (`Config.gs`, `Alta.gs`, `CambioContrasena.gs`, `Reset2FA.gs`, `Incidencias.gs`) pegados completos en el editor del proyecto "Webform Correo 2026-2027 - Backend". **Dos pegados fallaron en silencio** (Reset2FA.gs e Incidencias.gs no se reemplazaron en el primer intento pese a que el editor mostraba "cambios sin guardar" — detectado buscando el nombre de la función nueva con Cmd+F antes de dar por buena la copia, no solo mirando el número de líneas) — reintentados y confirmados con la misma búsqueda. `configurarValidacionYSemaforo()` corrida desde el editor: "Se completó la ejecución" sin errores (mismo hallazgo del diálogo de alert en la pestaña del Sheet que en Asesorías). Verificado visualmente: dropdown nuevo en `Tipo de Cuenta` (Alta) y `¿Reset listo?` (Reset 2FA); semáforo gris confirmado en vivo sobre la fila real de Reset 2FA (`Estado general` = "Solicitud recibida"). |
| **Manual visual — sección 4 de `docs/manual-bases-tramites.html` completada** | Reemplazado el placeholder "pendiente" por 4 sub-tablas (una por tipo de Correo) más una tabla de semáforo de `Estado general` (3 valores finales distintos: Cuenta entregada / Reset notificado / Incidencia resuelta, todos verdes sobre el mismo gris de "Solicitud recibida"). Índice del documento actualizado. |
| **Verificación** | Ejecución de la función confirmada sin errores. Cada uno de los 5 archivos verificado con Cmd+F buscando el nombre de su función nueva antes de continuar (no solo el conteo de líneas, por el fallo silencioso de pegado detectado en Reset2FA.gs/Incidencias.gs). Semáforo y dropdown confirmados visualmente contra datos reales. Manual verificado renderizado en navegador (servidor local temporal, cerrado al terminar). `git diff --stat`: `apps-script/correo/` 5 archivos, 151 líneas agregadas; `docs/manual-bases-tramites.html` +58/-3 líneas. |
| **Commits** | Pendiente — sesión sin commitear todavía al momento de este checkpoint. |

---

## CHECKPOINT — 2026-09-01 (cont. 2) · "Planchado" de Asesorías (dropdowns + semáforo + protección) y su sección del manual visual

| | |
|---|---|
| **Fecha** | 2026-09-01 |
| **Sesión** | Jorge pidió "el mismo tratamiento para Asesorías" — repetir el piloto de Mantenimiento del checkpoint anterior en el segundo de los 5 trámites (`docs/ROADMAP.md` ítem 16). |
| **`aseConfigurarValidacionYSemaforo()` nueva en `apps-script/asesorias.gs`** | Mismo patrón exacto que `manConfigurarValidacionYSemaforo()`, adaptado a las columnas de Asesorías (sin `Reportes de visita` ni `Técnico asignado`, no existen en este trámite): dropdown suave en `Tipo de Asesoría`/`Turno`/`Confirmó Mantenimiento Previo`/`Tipo de solicitante`; semáforo de color en `Estatus` (mismos 5 colores); protección "solo aviso" en `Fecha`/`Folio`/`Oficio (link Drive)`/las 2 columnas de Notificación. No toca la validación de `Estatus` que ya existía. Corrida desde el menú "OTDE Asesorías" → "Aplicar validación y semáforo" (item nuevo). Detalle completo en `docs/ARCHITECTURE.md §24`. |
| **Verificado en vivo contra la hoja real antes de escribir código** | `Solicitudes_Asesorias_2026` abierta en el navegador: headers confirmados columna por columna contra `ENCABEZADOS_ASE_SOLICITUDES`, catálogos cerrados de Tipo de Asesoría/Turno/Tipo de solicitante confirmados contra los `<select>` reales de `asesorias.html`. Hallazgo de paso: la hoja sí tiene 3 filas reales — las del smoke test del checkpoint "Notificaciones de Telegram + correo por persona" (🧪, CCT `99ZZZ9999Z`) que seguían sin borrar; no se tocaron, Jorge las borrará él mismo. |
| **Ejecución y verificación en el proyecto de Apps Script real** | Código pegado completo en el editor del proyecto "Asesorias - Backend (asesorias.gs)", guardado, y `aseConfigurarValidacionYSemaforo()` corrida desde el editor. **Hallazgo operativo nuevo**: el Registro de ejecución se quedó mostrando el spinner de "Se inició la ejecución" mucho más de lo esperado (~30s) — causa real: el `SpreadsheetApp.getUi().alert()` final se despliega en la pestaña del Sheet, no en la del editor, y bloquea el fin de la ejecución hasta que alguien lo cierra ahí; una vez cerrado el diálogo en la pestaña del Sheet, confirmado que la función sí había corrido completa y sin errores. Verificado visualmente: los 4 dropdowns nuevos muestran flecha de selección; `Estatus` de las 3 filas existentes ("Pendiente de validar") se colorea con el gris esperado. No se probaron los otros 4 valores de `Estatus` con una fila de prueba (a diferencia de Mantenimiento) — ya había filas reales con dropdown+color visibles, no hacía falta. |
| **Manual visual — sección 2 de `docs/manual-bases-tramites.html` completada** | Reemplazado el placeholder "pendiente" de Asesorías por la tabla columna-por-columna completa (mismo criterio de colores que Mantenimiento) y una nota aclarando que Asesorías no tiene Técnico asignado ni hoja de Reportes de visita. Índice del documento actualizado (ya no dice "pendiente" para Asesorías). |
| **Verificación** | Ejecución de la función confirmada sin errores (alert final desplegado con el mensaje de éxito). Dropdowns y semáforo confirmados visualmente contra las 3 filas reales ya existentes. Manual verificado renderizado en navegador (servidor local temporal, cerrado al terminar). `git diff --stat`: `apps-script/asesorias.gs` +91 líneas; `docs/manual-bases-tramites.html` +50/-2 líneas. |
| **Commits** | Pendiente — sesión sin commitear todavía al momento de este checkpoint. |

---

## CHECKPOINT — 2026-09-01 (cont.) · "Planchado" de Mantenimiento (dropdowns + semáforo + protección) y manual visual de bases para el equipo

| | |
|---|---|
| **Fecha** | 2026-09-01 |
| **Sesión** | Jorge pidió dos cosas para el equipo (Alejandro/Nancy/Marcos): un manual mayormente visual de qué mover/llenar/no tocar en las hojas de los 5 trámites de Oficina Virtual, y dejar esas hojas "bien planchadas" con listas desplegables y semaforización por color donde aplique. Alcance de esta sesión, decidido para planear una pieza a la vez: piloto completo en Mantenimiento; Asesorías/Soporte/Correo/Formación Docente quedan para sesiones siguientes repitiendo el mismo patrón (`docs/ROADMAP.md` ítem 16). |
| **`manConfigurarValidacionYSemaforo()` nueva en `apps-script/mantenimiento.gs`** | Dropdown "suave" (marca con aviso, no bloquea) en `Turno`/`Estado de instalación`/`Tipo de solicitante`; semáforo de color (`setConditionalFormatRules`) en `Estatus` (Solicitudes, 5 colores) y `Estado del aula` (Reportes de visita, ya traía emoji semáforo en el texto); protección "solo aviso" (`Range.protect().setWarningOnly(true)`, no bloquea a nadie) en las columnas 100% automáticas (`Fecha`/`Folio`/`Oficio`/las 3 de Notificación). No toca la validación de `Estatus`/`Técnico asignado` que ya existía ni ningún trigger. Corrida una vez desde el menú "OTDE Mantenimiento" → "Aplicar validación y semáforo" (item nuevo del menú). Detalle completo en `docs/ARCHITECTURE.md §24`. |
| **Verificado en vivo contra la hoja real antes de escribir código** | `Solicitudes_Mantenimiento_2026` abierta en el navegador: headers confirmados columna por columna, catálogos cerrados de Turno/Estado de instalación/Tipo de solicitante confirmados contra los `<select>` reales de `mantenimiento.html` (no inferidos de la documentación). Hallazgo de paso: la hoja `Solicitudes` seguía sin ninguna fila real de datos (`Contactos_Zona_Sector` sí tiene sus 88 filas reales) — nadie ha usado el webform público todavía. |
| **Ejecución y verificación en el proyecto de Apps Script real** | Código pegado completo en el editor del proyecto "Mantenimiento - Backend (mantenimiento.gs)" (mismo patrón de siempre: copiar y pegar el `.gs` completo), guardado, y `manConfigurarValidacionYSemaforo()` corrida desde el editor — "Se completó la ejecución" sin errores. Verificado visualmente en la hoja real: los 3 dropdowns nuevos muestran flecha de selección; se probó temporalmente cada uno de los 5 valores de `Estatus` en una fila y el color cambió correctamente para los 5; se confirmó en vivo que intentar borrar una columna protegida (`Oficio`) dispara el diálogo nativo de Sheets de "solo aviso". La fila de prueba se borró antes de cerrar la sesión, la hoja quedó igual de vacía que al empezar. |
| **Manual visual — `docs/manual-bases-tramites.html` (nuevo, 373 líneas)** | Mismo lenguaje visual institucional (guinda/midnight) que el resto del sitio, imprimible (salto de página por sección para exportar a PDF vía Cmd+P). Leyenda de 3 colores (🟢 llenar libre / 🟡 tocar con cuidado / 🔴 no tocar) y tabla columna-por-columna completa para Mantenimiento. Asesorías/Soporte/Correo/Formación Docente quedan como placeholder explícito "pendiente" — el documento es entregable tal cual sin prometer contenido no verificado de los otros 4 sistemas. |
| **Verificación** | Ejecución de la función confirmada sin errores en el Registro de ejecución de Apps Script. Semáforo de `Estatus` probado en vivo con los 5 valores reales. Protección probada en vivo (diálogo de aviso disparado al intentar borrar `Oficio`). Manual verificado renderizado en navegador (servidor local temporal, `python3 -m http.server`, cerrado al terminar). `git diff --stat`: `apps-script/mantenimiento.gs` +99 líneas; `docs/manual-bases-tramites.html` nuevo, 373 líneas. |
| **Commits** | Pendiente — sesión sin commitear todavía al momento de este checkpoint. |

---

## CHECKPOINT — 2026-09-01 (cont.) · QA pre-difusión pública de Oficina Virtual + catálogo de Formación Docente ocultado + verificación de drift repo↔Apps Script

| | |
|---|---|
| **Fecha** | 2026-09-01 |
| **Sesión** | Jorge, a horas de difundir el link de `oficina-virtual.html` a toda la estructura educativa, preguntó qué pruebas correr antes. Se auditó el repo y `docs/BITACORA.md` para armar un plan de QA con riesgos concretos (no un checklist genérico), y se ejecutó en vivo lo que era seguro hacer sin crear solicitudes reales. |
| **Catálogo de Formación Docente confirmado vacío — tarjeta ocultada** | Verificado en vivo contra el `doGet` real de `formacion-docente.gs`: `{"status":"ok","cursos":[]}` — confirma lo ya visto en el UAT del 31 ago (`docs/BITACORA.md`, checkpoint de ese día), y corrige una nota desactualizada de `CLAUDE.md` que aún decía "2 webinars". La tarjeta "Centro de Formación Docente" en `oficina-virtual.html` (la más visible del hub) pasó de `<a href="formacion-docente.html">` a `<div class="area-card ov-proximamente">`, mismo patrón visual que la card "Más servicios en camino" ya existente — reactivarla es un cambio de una línea en cuanto haya cursos dados de alta. |
| **QA en vivo (sin crear solicitudes reales)** | Probado localmente (servidor estático) y luego confirmado en producción tras el deploy: las 4 páginas de trámite cargan y su navegación de vuelta al hub funciona; el buscador de folio (`GET ?action=consulta`, de solo lectura) responde correctamente tanto a un folio con formato inválido como a uno con prefijo válido pero inexistente; viewport móvil (390px) sin desbordamiento en el hub ni en Mantenimiento; `og-image.png`/`favicon.svg` cargan con 200; tag de Google Analytics presente. No se probó el 404 personalizado (el servidor estático local no replica el comportamiento de GitHub Pages) ni la vista previa real de WhatsApp — quedan para que Jorge las confirme directo en producción/WhatsApp. |
| **Verificación de código desplegado vs. repo (los 4 backends)** | A petición de Jorge, comparación byte a byte (SHA-256, calculado en el propio navegador contra `monaco.editor.getModels()`, sin descargar ni pegar el código en texto plano) del código real en el editor de Apps Script contra cada `.gs`/`.js` del repo: Mantenimiento, Asesorías, Soporte y 8 de los 9 archivos de `apps-script/correo/` coinciden exactamente. Único hallazgo: `WebApp.gs` desplegado tiene 4 líneas extra ausentes del repo — detalle completo y causa raíz en `docs/QA-NOTES.md #25`. |
| **Hallazgo de seguridad — `PANEL_TOKEN` hardcodeado y débil (`1234`)** | Derivado de investigar la discrepancia de `WebApp.gs` de arriba: la función temporal encontrada fija `PANEL_TOKEN` a `'1234'`, y ese es efectivamente el valor real configurado en las Propiedades del script de Mantenimiento, Asesorías y Soporte — no un placeholder. Ese token protege `?action=pendientes&token=...`, el endpoint que usa el Panel OTDE para leer las solicitudes abiertas de los 4 trámites. No bloquea la difusión de hoy (no es el endpoint que se comparte públicamente), queda como pendiente de higiene — ver `docs/QA-NOTES.md #25`. |
| **`MODO_PRUEBA_CORREO` reconfirmado apagado** | Revisado directo en las Propiedades del script de Mantenimiento y Asesorías: la propiedad no aparece en ninguno de los dos — sigue apagada desde que Jorge la desactivó el 31 ago. |
| **Limpieza de filas de prueba** | Jorge confirmó haber limpiado ya las filas 🧪 del smoke test de la sesión anterior en los Sheets de producción (fuera del alcance de esta sesión de código). |
| **Prueba de envío real de los 4 trámites — deliberadamente no realizada** | Jorge decidió mantener su elección de la sesión del 31 ago: no forzar un envío de prueba marcado justo antes de la difusión (evita notificar en falso a Alejandro/Nancy/Marcos), y en su lugar dejar que las primeras solicitudes reales sirvan de validación. |
| **Verificación** | `curl -sL` contra el `doGet` real de `formacion-docente.gs` (catálogo vacío confirmado). Pruebas de navegador (local y producción) descritas arriba. Hash SHA-256 in-browser de 13 archivos `.gs`/`.js` contra sus equivalentes del repo (`sha256sum` normalizado, CRLF→LF y sin salto de línea final, para evitar falsos positivos por formato). `git diff --stat` del fix de la tarjeta: 1 archivo, 4 inserciones, 5 eliminaciones. `git push` verificado sin conflicto (`git log --oneline -3 origin/main` comparado antes de empujar, sin divergencia). |
| **Commits** | `da968f6` (fix de la tarjeta de Formación Docente, ya publicado en `origin/main`); pendiente el de esta actualización de documentación de cierre. |

---

## CHECKPOINT — 2026-09-01 · Notificaciones de Telegram + correo por persona (Marcos/Alejandro/Nancy) en los 4 backends de trámite

| | |
|---|---|
| **Fecha** | 2026-09-01 |
| **Sesión** | Jorge pidió configurar quién del equipo recibe qué notificación: Marcos (cambio de contraseña y eliminar método de autenticación de Correo), Alejandro (soporte técnico y mantenimientos) y Nancy (asesorías) — por Telegram y por correo. |
| **Contradicción con decisiones previas, señalada antes de proceder** | El 18 ago 2026 Jorge había acotado Telegram en Correo a solo `Incidencias.gs` y decidido explícitamente no agregar correo de respaldo a Marcos, para evitar ruido en el chat compartido de OTDE ante trámites rutinarios (ver checkpoint del 18 ago). Antes de implementar, se le presentó la contradicción directamente: lo pedido ahora revierte esa decisión. Jorge confirmó proceder — un DM personal dirigido no es el mismo "ruido" que un chat compartido de equipo, así que el motivo original ya no aplica. |
| **Código agregado (6 archivos, 149 líneas)** | `cambioNotificarEquipo()` (`CambioContrasena.gs`), `resetNotificarEquipo()` (`Reset2FA.gs`) e `incidenciaNotificarEquipoPorCorreo()` (`Incidencias.gs`, el Telegram de este ya existía) notifican a Marcos. `manNotificarEquipoPorCorreo()` (`mantenimiento.gs`) y `sopNotificarEquipoPorCorreo()` (`soporte-remoto.gs`) notifican a Alejandro. `aseNotificarEquipoPorCorreo()` (`asesorias.gs`) notifica a Nancy. Cada proyecto sigue usando el mismo bot de Telegram compartido de siempre — lo que cambia es que `TELEGRAM_CHAT_ID` de cada proyecto ahora apunta al chat_id personal de la persona correspondiente, no al chat de equipo. `Alta.gs` no se tocó (sin cambios, decisión ya tomada de que no notifica a nadie). |
| **Obtención de los 3 chat_id personales** | Jorge coordinó con Marcos/Alejandro/Nancy que cada uno le escribiera al bot existente y usó `getUpdates` para leer sus chat_id. Primer intento con `getUpdates` regresó vacío (`{"ok":true,"result":[]}`) — causa real: había apuntado a un bot distinto al que Marcos/Nancy le habían escrito (encontrado "desde su chat de Telegram", no verificado contra el `TELEGRAM_BOT_TOKEN` real de Propiedades del script). Corregido comparando `getMe` del token real contra el bot correcto. |
| **Smoke test en producción (12 solicitudes de prueba, marcadas 🧪)** | Contra los 4 endpoints reales (`correo.html`/`mantenimiento.html`/`soporte.html`/`asesorias.html`), con CCT `99ZZZ9999Z` y Sector/Zona inventados (para que Mantenimiento/Asesorías no le hicieran CC a ninguna Zona/Sector real) y el correo del propio Jorge como "solicitante". Primeros 6 envíos por `curl`: llegaron al servidor (`HTTP 302`, confirmando que `doPost` sí corrió) pero sin poder leer el JSON de confirmación — mismo falso negativo de `curl` con el redirect de Apps Script ya documentado (checkpoint 6 ago 2026). Verificación posterior vía `fetch()` de navegador real (`javascript_tool` contra las páginas reales) sí devolvió el JSON: `OTDE-2FA-0003`, `OTDE-ASE-0002`, `OTDE-ASE-0003`, `OTDE-MAN-0002` confirmados con `status:ok`. |
| **Bug real encontrado y corregido — Telegram de Mantenimiento/Asesorías fallaba en silencio** | El Telegram a Nancy (Asesorías) no llegó en 2 intentos pese a folio válido y correo de equipo entregado correctamente; una llamada directa a la API de Telegram con el mismo token/chat_id sí funcionó, descartando mala configuración. Causa raíz: `aseNotificarTelegram()`/`manNotificarTelegram()` insertan la URL del oficio de Drive sin pasarla por `aseEscapeMarkdown_()`/`manEscapeMarkdown_()` — un `_` sin parear en el ID del archivo de Drive rompe el `parse_mode: 'Markdown'` de Telegram (`400`, "can't parse entities"), y como la llamada usa `muteHttpExceptions:true` sin revisar el código de respuesta, el error no deja ningún rastro. A Alejandro (Mantenimiento) le había funcionado por pura suerte con esa URL en particular — mismo bug latente, sin detonar todavía. Corregido en ambos archivos; Jorge redesplegó y se confirmó Telegram entregado a Nancy y a Alejandro en una segunda ronda de verificación. Detalle completo en `docs/QA-NOTES.md #24`. |
| **Documentación desactualizada, corregida de paso** | Las secciones de Mantenimiento y Asesorías en `CLAUDE.md` seguían diciendo "Telegram deliberadamente sin configurar (decisión de Jorge, 6 ago 2026)" — desactualizado desde el 18 ago 2026, cuando sí se configuró (con el bot/chat compartido) pero solo se actualizó la sección de Correo Institucional, no estas dos. Corregido de paso al tocar estas secciones. |
| **Pendiente** | Las 10 filas de prueba del smoke test (Cambio de Contraseña ×1, Reset 2FA ×2, Incidencias ×1, Mantenimiento ×2, Soporte ×1, Asesorías ×3 — todas marcadas "🧪 PRUEBA SMOKE TEST"/"🧪 VERIFICACION #N", CCT `99ZZZ9999Z`) siguen en los Sheets de producción; Jorge las borrará cuando tenga tiempo. |
| **Verificación** | `node --check` sobre los 6 `.gs` tras cada edición (sintaxis válida). 4 folios de prueba confirmados con `status:ok` vía `fetch()` real. Confirmación manual de Jorge de entrega real: Telegram + correo a Marcos (Cambio de Contraseña, Reset 2FA, Incidencias), a Alejandro (Mantenimiento, Soporte) y a Nancy (Asesorías, tras el fix). `git diff --stat`: 10 archivos, 240 líneas agregadas, 27 eliminadas. |
| **Commits** | Pendiente — sesión sin commitear todavía al momento de este checkpoint. |

---

## CHECKPOINT — 2026-08-31 (cont.) · Performance fix en la ficha de Ceremonias Cívicas + reconciliación de `main`/`origin`

| | |
|---|---|
| **Fecha** | 2026-08-31 |
| **Sesión** | Jorge reportó que le avisaban problemas al cargar/enviar la ficha de Ceremonias Cívicas, con un mensaje de que el servidor tardó en responder — pidió un performance test. Se diagnosticó, corrigió, verificó en vivo y publicó en la misma sesión; después Jorge pidió resolver la divergencia de git entre `main` local y `origin/main` que arrastraba el repo desde hacía meses. |
| **Performance test en vivo (diagnóstico, sin tocar código todavía)** | Con autorización explícita de Jorge, se creó una reserva de prueba real y se envió la ficha vía `curl` contra el endpoint desplegado, midiendo tiempos con distinto número de fotos: 20.2s (5 fotos), 30.5s (10, ya por encima del timeout de 30s del cliente) y 68.7s (20, el máximo). Causa raíz confirmada: `visSubirFotos_()` en `apps-script/visitas-jefes.gs` hacía `createFile()` + `setSharing()` por cada foto, en serie, sin ninguna concurrencia posible en Apps Script — y el timeout fijo de `fetchJsonConTimeout()` (30s) no distinguía este envío pesado del resto de llamadas del sitio. Jorge confirmó por separado que en algunos casos reales la ficha sí se había guardado (Sheet + Drive) pese al mensaje de error visto por el usuario — el aviso era falso, no un fallo real. |
| **Fix aplicado y verificado en vivo** | Dos cambios: (1) `visObtenerCarpetaFotos_()` comparte la carpeta de Drive completa una sola vez por envío en vez de compartir cada foto por separado (`archivo.setSharing()` eliminado del loop de `visSubirFotos_()`) — bajó a 35.4s con 20 fotos y 21.5s con 10, mismo test repetido tras el redeploy de Jorge; verificado con `curl` sin sesión que el link de una foto real sigue abriendo. (2) `fetchJsonConTimeout()` (`js/tramites-shared.js`) ganó un tercer parámetro opcional `timeoutMs` (retrocompatible, ~12 llamadas del sitio sin tocar); el envío de la ficha usa `FICHA_TIMEOUT_ENVIO_MS`=120000 con avisos progresivos en pantalla. Detalle completo en `docs/ARCHITECTURE.md §22` y `docs/QA-NOTES.md #23`. |
| **Publicación a producción** | Rama `publish-fix-fotos-ceremonias` creada desde `origin/main`, cherry-pick del commit local, `git diff origin/main -- otde.html` confirmado vacío antes de pushear — mismo mecanismo de siempre. |
| **Limpieza de datos de prueba** | Jorge eliminó a mano las filas y fotos de prueba creadas durante el diagnóstico y la verificación (Sheet "Reservas" y carpeta "Fotos de Visitas Jefes" en Drive). |
| **Reconciliación de `main` local y `origin/main`** | A petición de Jorge, se investigó la divergencia (57 vs. 22 commits) que el repo arrastraba desde hacía meses por el freeze de `otde.html`. `git diff --stat origin/main main` mostró contenido idéntico salvo 3 `.DS_Store` — el freeze ya había terminado (commit `c8f1252`, sesión anterior, publicó `otde.html` por completo). Se hizo `git merge origin/main` (no un `reset --hard`, para no perder ninguno de los 57 commits locales) con una rama de respaldo local creada antes por seguridad, y se empujó — ambas ramas quedaron sincronizadas. `CLAUDE.md` actualizado para reflejar que el `git push origin main` directo vuelve a ser seguro (con la salvedad de siempre: verificar antes de asumir, si vuelve a acumularse trabajo no listo). |
| **Verificación** | `curl -w` contra el endpoint real (antes y después del fix, mismos números de foto para comparar directo); `curl` sin sesión confirmando el link de Drive de una foto real; `git diff --stat` de los 3 archivos tocados (51 líneas agregadas, 10 eliminadas); `git diff origin/main main` vacío (salvo `.DS_Store`) antes y después de la reconciliación. |
| **Commits** | `16a7f60` (fix, local `main`), `7a0127d` (mismo fix, publicado a `origin/main` vía `publish-fix-fotos-ceremonias`), `3a8d218` (merge de reconciliación, en ambas ramas). Pendiente el de esta actualización de documentación de cierre. |

---

## CHECKPOINT — 2026-08-31 · Correo de trámites rediseñado, `apps-script/correo/` integrado al repo, UAT de Oficina Virtual

| | |
|---|---|
| **Fecha** | 2026-08-31 |
| **Sesión** | Arrancó con Jorge preguntando si Oficina Virtual estaba lista para difusión al público. Auditoría en vivo encontró que sí funciona técnicamente, pero `otde.html` de producción sigue sin enlazar a Oficina Virtual (pendiente, decisión ya conocida de Jorge) y que `MODO_PRUEBA_CORREO` podía seguir activo en Mantenimiento/Asesorías desde el 25 ago — Jorge lo desactivó. De ahí se derivó el resto de la sesión: correo de confirmación de Soporte, rediseño completo del correo institucional, mover `Correos-institucionales/webform-2026-2027/` a este repo, y un UAT de navegación/validación de los 6 flujos. |
| **Correo de confirmación en Soporte (`docs/ROADMAP.md` ítem 11, resuelto)** | Nueva `sopNotificarSolicitudRecibida(folio, d)` en `apps-script/soporte-remoto.gs`, llamada desde `doPost` junto a `notificarTelegram`. Soporte ya confirma de inmediato al solicitante, como los demás trámites. Sin CC a Zona/Sector — Soporte no tiene `Contactos_Zona_Sector`, decisión ya tomada. |
| **Rediseño de correo institucional (`docs/ROADMAP.md` ítem 12, resuelto)** | A petición explícita de Jorge de "empezar de cero" tras rechazar 3 propuestas iniciales, se revisó el sistema real `Correos-institucionales` (`Code.gs`/`OnFormSubmit.gs`, el que usa Marcos a diario) como fuente de identidad — Jorge ya lo reconocía como "institucional, con marca, limpio". Se adoptó ese lenguaje visual (header guinda con eyebrow y barra de acento, caja de resumen en `<table>` en vez de `<div>` para sobrevivir en Outlook, firma completa nombre/cargo/teléfono/dirección/correo, redes sociales, pie con copyright) en `mantenimiento.gs`, `asesorias.gs`, `soporte-remoto.gs` (funciones `man`/`ase`/`sopCorreoHtml_` nuevas, un color de marca único sin variación por trámite, siguiendo el precedente real) y `formacion-docente.gs` (header pasó de azul marino `#0C1A2E` a guinda `#9F2241`, ganó firma completa que antes no tenía). Se agregó un botón "Consultar estatus de tu solicitud" hacia `oficina-virtual.html#buscar-folio` en los correos de los 4 trámites con folio (no en Formación Docente — no tiene endpoint de consulta, decisión ya documentada de que se sigue por Looker Studio). Jorge pegó y redesplegó los 4 `.gs` de `seprn-sitio` en sus proyectos reales. |
| **`apps-script/correo/` movido a este repo (antes `Correos-institucionales/webform-2026-2027/`)** | A petición de Jorge, tras notar que ese trámite seguía fuera del repo unificado por inercia histórica. 9 archivos (`Config.gs`, `WebApp.gs`, `Alta.gs`, `CambioContrasena.gs`, `Reset2FA.gs`, `Incidencias.gs`, `OnEdit.gs`, `GenerarResumenSIGEE.gs`, `ResumenSemanal.gs`) copiados y verificados byte a byte antes de borrar el original (repo sin git). De paso recibieron el mismo rediseño de correo: `altaCajaHTML` de `<div>` a `<table>`, firma con teléfono/dirección agregados, nuevo botón `altaBotonSeguimientoHTML()` en las 8 funciones de correo de los 4 tipos. El sistema viejo (`Code.gs`/`OnFormSubmit.gs`/`OnEditTrigger.gs`/`ResumenSemanal.gs` de la raíz de `Correos-institucionales`, sin folio, el que usa Marcos) se dejó explícitamente sin tocar — Jorge confirmó que está por retirarse con la migración completa a webforms, no vale la pena invertir ahí. Jorge pegó y redesplegó los 5 archivos tocados en el proyecto real. Docs actualizados: `Correos-institucionales/CLAUDE.md` (nota de migración), `README.md`, `CLAUDE.md`, `docs/ARCHITECTURE.md` (§16, §19.4, §20) — `docs/BITACORA.md` no se reescribe, es historia. |
| **UAT de Oficina Virtual (navegación/validación, sin envíos reales — alcance elegido por Jorge)** | Verificado en producción: hub (folio search con 2 prefijos distintos, incluyendo el de Correo recién movido), Mantenimiento (autocompletado CCT + función por tipo de CCT + validación por campo), Asesorías (toggle Banco de Materiales ↔ Excel básico), Soporte (validación), Correo (switcher de 4 paneles + regla de dominio verificada en vivo con CCT real `15DPR0006T`: sin Función es `@aulamexiquense.mx`, con Director(a) cambia a `@dee.edu.mx`), Formación Docente (carga bien, pero el catálogo real está vacío — nadie puede inscribirse hoy, pendiente ya conocido de dar de alta cursos). Sin hallazgos de bugs nuevos. Jorge decidió no completar el UAT de punta a punta (POST reales con modo de prueba) esta sesión — probará con personas reales e irá ajustando sobre la marcha. |
| **Verificación** | `node --check` en los 4 `.gs` de `seprn-sitio` y los 9 de `apps-script/correo/` — todos sin errores de sintaxis. Copia de `apps-script/correo/` verificada byte a byte (`diff`) contra el original antes de borrarlo. `git diff --stat`: 13 archivos vía `git add` directo (1,679 líneas agregadas, 32 eliminadas — incluye los 9 nuevos de `apps-script/correo/`) más 4 archivos de rediseño de correo (406 líneas agregadas, 123 eliminadas). |
| **Commits** | Pendiente — todo el trabajo de esta sesión sigue sin comitear en el working tree, a la espera de este cierre. |

---

## CHECKPOINT — 2026-08-30 (cont.) · Ajustes de Ceremonias Cívicas + favicon/og:image en las 26 páginas del sitio

| | |
|---|---|
| **Fecha** | 2026-08-30 |
| **Sesión** | Continuación de la sesión anterior del mismo día (sistema de Ceremonias Cívicas recién construido). Tres pedidos puntuales de Jorge: agregar opciones al select de Subjefatura/Oficina, revisar un desborde en móvil del campo de fecha, y diagnosticar por qué solo el link de `index.html` mostraba ícono al compartirse por WhatsApp. |
| **`ceremonias-civicas.html` — select "Subjefatura/Oficina"** | Se agregaron dos opciones: "Encargada del Despacho" (a petición de Jorge, reubicada después como la primera opción real del select, antes de las Subjefaturas) y "Secretaría Particular" (entre Recursos y OTDE). Sin cambios en `visToggleOtroCargo()`. |
| **`ceremonias-civicas.html` — "Fecha planeada" desbordada en móvil** | No se logró reproducir en Chrome: probado a 371px de viewport real vía iframe same-origin (sin overflow), y en headless Chrome de este equipo (que resultó forzar un mínimo de ventana de 500px sin importar los flags usados, inútil para simular anchos de celular). El patrón encaja con un bug conocido de Safari/iOS donde `<input type="date">` ignora el `width` del contenedor. Se aplicó el fix estándar en `#vis-fecha` (`max-width:100%; min-width:0; -webkit-appearance:none; appearance:none`) sin poder confirmarlo contra Safari real — pendiente que Jorge confirme en su teléfono. |
| **Causa raíz — favicon ausente en WhatsApp salvo en `index.html`** | Ninguna de las 26 páginas HTML del sitio tenía `og:image` (ni las 14 que ya traían el resto del bloque `og:`), y el único favicon existente era `favicon.svg` — WhatsApp (como la mayoría de crawlers de redes sociales) no rasteriza SVG para la miniatura de vista previa. El ícono que Jorge veía en el link de `index.html` casi seguro era una vista previa vieja cacheada por WhatsApp, de antes de que el sitio quedara solo con favicon SVG. |
| **Fix — favicon PNG + `og:image` en las 26 páginas** | Nuevos `favicon-192.png` (raíz) e `images/og-image.png` (512×512), ambos renderizados del mismo logomark NE/ZA de `favicon.svg`. Las 14 páginas con bloque `og:` existente ganaron solo la línea `og:image`; las 12 sin ningún bloque `og:` (`ceremonias-civicas.html`, `ficha-ceremonias-civicas.html`, `soporte.html`, `mantenimiento.html`, `asesorias.html`, `correo.html`, `formacion-docente.html`, `instructivo-formacion-docente.html`, `reporte-visita.html`, `asistencia.html`, `charla-ia.html`, `404.html`) recibieron el bloque completo, derivando `og:title` del `<title>` existente y `og:description` del `<meta name="description">` (4 páginas sin meta description recibieron una redactada nueva). Detalle completo en `docs/ARCHITECTURE.md §23`; checklist de páginas nuevas (`docs/ARCHITECTURE.md §10`) actualizado para no repetir el bug en páginas futuras. |
| **Fuera del código — mensaje de WhatsApp para autorización de la jefa** | A pedido de Jorge, se redactó (solo en el chat, no en un archivo del repo) un mensaje breve para que la Dra. Galindo autorice difundir `ceremonias-civicas.html`/`ficha-ceremonias-civicas.html` a los jefes de área. No requiere seguimiento en este repo. |
| **No verificado en esta sesión** | El fix de favicon/`og:image` no se probó contra una vista previa real de WhatsApp (requiere producción + compartir un link real) — pendiente que Jorge confirme después del siguiente deploy, considerando que WhatsApp cachea vistas previas ya generadas. |
| **Verificación** | `git diff --stat` sobre las 26 páginas: 115 líneas agregadas, 0 eliminadas. Página de prueba (`ceremonias-civicas.html`) renderizada en Chrome tras los cambios, sin errores visuales. |
| **Commits** | Ninguno todavía — sigue pendiente junto con el resto del código sin comitear de la sesión anterior del mismo día (sistema de Ceremonias Cívicas). |

---

## CHECKPOINT — 2026-08-30 · Sistema nuevo: Ceremonias Cívicas (reserva + ficha + cobertura + reporte PDF) — SEPRN-wide, no solo OTDE

| | |
|---|---|
| **Fecha** | 2026-08-30 |
| **Sesión** | La jefa de SEPRN instruyó a ~20 jefes de área + docentes que se sumen a visitar escuelas el lunes 1 sep (inicio de ciclo 2026-2027) y, después, a acompañar semanalmente las ceremonías cívicas de distintas escuelas. Jorge pidió sistematizar la selección de escuela (evitar duplicados la misma semana), el histórico de quién visitó qué y cuándo, y la validación de que la visita sí ocurrió. Sesión larga, construida y afinada por fases en conversación directa con Jorge (no un plan monolítico), con smoke/QA/UI/UX test al final. Es el primer sistema de este repo que no es un trámite de OTDE — lo usan jefes de toda la Subdirección. |
| **Backend nuevo — `apps-script/visitas-jefes.gs` (710 líneas, 26 funciones)** | Proyecto Apps Script propio, Sheet real `Seguimiento_Ceremonias_Cívicas_26-27` (tab "Reservas", 24 columnas). Folio `SEPRN-CC-NNNN` (no `OTDE-`, porque participan jefes de toda la Subdirección). **Sin notificaciones a propósito** (ni correo ni Telegram) — decisión explícita de Jorge; el folio se muestra en pantalla al reservar, sin respaldo por correo. `doGet` con 3 acciones (`disponibilidad`, `consulta`, `dashboard`); `doPost` ramificado por `datos.accion` (`'ficha'` vs. reserva por default). Reserva bloqueada por CCT+semana vía `LockService`. Menú del Sheet "SEPRN Visitas" con 4 acciones (reporte PDF, instalar/desinstalar trigger de validación, marcar "No realizadas" a mano). |
| **`ceremonias-civicas.html` (758 líneas)** | Reserva (autocomplete de CCT con fallback manual + cascade Sector→Zona, campo "Subjefatura/Oficina" sin opción "Docente", sin correo/teléfono en el formulario — no tenía caso pedirlos sin notificaciones) + tabla de disponibilidad/histórico con filtro de búsqueda + contador de cobertura ("N de 417 escuelas visitadas este ciclo") + panel de cobertura por persona/sector (barras CSS, sin librería nueva) protegido con clave de acceso propia (`DASHBOARD_TOKEN`, mismo patrón `PANEL_TOKEN` que `panel-otde.gs`) — **panel comentado/oculto a petición de Jorge**, no visible las primeras semanas del ciclo, el código queda intacto para reactivarlo cambiando un comentario HTML. |
| **`ficha-ceremonias-civicas.html` (560 líneas)** | Ficha post-visita localizada por folio (`?action=consulta`, precarga automática si llega por `?folio=...`), campos pensados para el Community Manager (Nombre de la actividad, Propósito, Convocados/Participantes con nombre de autoridades, Descripción general, Cantidad de asistentes) separados visualmente ("Para difusión" vs. "Uso interno") de la Operatividad de la escuela (interna, no se difunde). Hasta 20 fotos comprimidas en el navegador (canvas, 1600px/JPEG q0.8) antes de subir. Guía de qué fotografiar + aviso de protección de menores, repetida también en el formulario de reserva (ver fila de UX abajo). |
| **Decisión de arquitectura — Fase 2 (reporte general de actividad para el CM) queda separada, no construida** | Jorge notó que el folio de Ceremonias Cívicas no puede ser la llave de un futuro reporte general para el CM (Facebook) porque ese caso de uso no tiene preselección (cualquier oficina reporta su propia actividad, sin reserva previa). Resuelto con dos flujos independientes: Ceremonias Cívicas mantiene folio obligatorio (hace trabajo real: bloqueo + validación); el reporte general (no construido, `ROADMAP.md` ítem 13) no pedirá folio de entrada, clonando el patrón de campos de la ficha sin el paso de reserva — mismo criterio que `asesorias.gs` clonado de `mantenimiento.gs`. Nombre de archivo `ficha-informativa-visita.html` reservado para esa pieza futura. |
| **Riesgo de cobertura desigual (misma escuela revisitada, otras nunca) — resuelto con avisos, no candados** | El sistema solo bloqueaba duplicar escuela+semana; nada evitaba que la misma escuela recibiera varias visitas en semanas distintas mientras otras nunca se visitaban. Jorge aclaró que una revisita legítima (seguimiento a compromisos acordados) sí debe ser posible, así que no cabe bloqueo duro. Se agregó: badge "Ya visitada" en las sugerencias del autocomplete, y un aviso no bloqueante (con fecha + nombre + cargo de quien visitó antes) si se elige una escuela con visita "Realizada" en otra semana, con un campo opcional "Motivo de revisita" que se guarda en la Sheet (columna nueva `COL_VIS_MOTIVO_REVISITA`). |
| **Acceso para la persona de seguimiento sin habilidades digitales** | No usa activamente ni Sheets ni correo — la solución no fue una interfaz nueva para ella, sino que alguien más (Jorge u otra persona) le imprima o le platique un resumen periódico. `visGenerarReporteSeguimiento_()` en el menú del Sheet genera un PDF (próximas visitas, realizadas recientes en tarjetas legibles con la Operatividad completa, pendientes "No realizada"), mismo técnica HTML→PDF que `manGenerarPDFReporte_()` de `mantenimiento.gs`, sin la pleca institucional (vive en el otro proyecto, no se duplicó). Guarda en Drive, carpeta "Reportes de Seguimiento — Ceremonias Cívicas". No requiere redeploy del Web App — solo pegar el código y guardar. |
| **Renombrados de archivos (mismo día, dos rondas)** | `visitas-escolares.html` → `ceremonias-civicas.html`; `ficha-visita-jefe.html` → `ficha-informativa-visita.html` (nombre genérico, transitorio) → `ficha-ceremonias-civicas.html` (nombre final, coincide con el trámite) — el nombre genérico se liberó y quedó reservado para la Fase 2. Título/`<h1>`/meta description de `ceremonias-civicas.html` actualizados a juego. |
| **QR institucional para compartir el link** | `images/qr-ceremonias-civicas.png`/`.pdf` — generado con Python (`qrcode`+Pillow, `RoundedModuleDrawer`, `ERROR_CORRECT_H`), mismo estilo que `images/qr-protocolos.png` (logomark NE/ZA superpuesto al centro, tarjeta guinda/off-white, paleta institucional). Verificado con `cv2.QRCodeDetector` que decodifica exactamente a la URL real pese al logo superpuesto. |
| **Smoke + QA + UI + UX test (vía navegador real, Chrome)** | Reserva, bloqueo de duplicado (cliente y servidor), CCT manual con cascade Sector→Zona, validaciones de campos vacíos, folio inexistente, ficha completa con fotos reales — sin bugs ni errores de consola. De la pasada de UI/UX salieron 3 fixes: el link dentro de la caja guinda de `ceremonias-civicas.html` no tenía color propio (heredaba el azul/morado del navegador, ahora caramelo claro); "Datos de la actividad" y "Operatividad de la escuela" en la ficha tenían el mismo tratamiento visual (ahora badges "Para difusión"/"Uso interno"); el bloque "Localiza tu visita" se quedaba visible aunque la precarga por folio ya hubiera funcionado (ahora se colapsa a una línea). Un hallazgo de UX adicional (Jorge): el aviso de qué fotografiar + protección de menores solo aparecía en la ficha, *después* de la visita, cuando ya no sirve para planear — se agregó también al formulario de reserva, antes de ir a la escuela. Hallazgo sin corregir, anotado para decisión de Jorge: el párrafo institucional de `.servicio-header` en `ceremonias-civicas.html` es más largo que el de páginas equivalentes, a anchos medios (~900-1100px) se vuelve una columna muy alta de texto. |
| **Gotcha de testing local documentado** | `docs/QA-NOTES.md #21` — el servidor `python3 -m http.server` no manda cabeceras de caché, y Chrome puede servir una versión vieja del HTML/JS tras editar el archivo aunque se navegue de nuevo a la misma URL; hace falta hard-reload (`Cmd+Shift+R`) o un parámetro de query nuevo para confirmar cambios reales durante pruebas. |
| **Verificación** | `node --check` sobre el `.gs` y sobre cada `<script>` inline de las 2 páginas HTML, balance de tags verificado por script. Reserva de prueba end-to-end contra el backend real desplegado por Jorge (vía `curl` primero, después por navegador con `browser_batch`): folio real generado, bloqueo de duplicado confirmado en cliente y servidor, ficha completa enviada con 2 fotos reales subidas a Drive y comprimidas correctamente, estatus y contador de cobertura actualizados en el histórico tras el envío. Sin errores de consola en ninguna prueba. |
| **Commits** | Ninguno todavía — todo el código de esta sesión sigue sin comitear (`apps-script/visitas-jefes.gs`, `ceremonias-civicas.html`, `ficha-ceremonias-civicas.html`, `images/qr-ceremonias-civicas.png`/`.pdf`). Pendiente el commit de cierre de esta sesión. |

---

## CHECKPOINT — 2026-08-28 (cont. 2) · Integration test de todo el sitio: navegación completa + 7 submits reales verificados contra producción

| | |
|---|---|
| **Fecha** | 2026-08-28 |
| **Sesión** | A petición del usuario ("realiza un Integration test de todo el sitio"). Se dividió en 2 fases: navegación estática local, y submits reales contra los backends de producción con datos de prueba — esta segunda fase requirió autorización explícita del usuario dos veces (el clasificador de modo automático de Claude Code bloqueó dos intentos de delegarla a un subagente; se ejecutó en el hilo principal con `curl`). |
| **Fase 1 — navegación local** (`python3 -m http.server`) | 26 páginas HTML probadas (24 en raíz + 2 manuales en `docs/`), todas 200. Un "link roto" detectado por el crawler resultó falso positivo: el `href` a `pdfs/cte/cuarta-sesion/cuarta-sesion-completa.zip` en `cte.html` vive dentro del bloque HTML comentado "Sesión de Ejemplo (COLAPSADA) — Copia este bloque para agregar más sesiones" (plantilla de referencia, nunca se renderiza) — no requirió fix. Sin errores JS propios del sitio en `index.html`/`cte.html`/`formacion-docente.html`/`oficina-virtual.html`. Nav/footer consistentes en las páginas que comparten header institucional. |
| **Fase 2 — submits reales contra producción** | 7 flujos probados con datos marcados "PRUEBA — Integration Test 2026-08-28" y el correo personal del usuario como solicitante — CCT `15FJS0015D` (Jefatura Sector I, tipo `jefatura`) usado en Mantenimiento/Asesorías para que el propio código dejara el CC vacío (`aseFiltrarContactosPorTipo`/`manFiltrarContactosPorTipo`) y no se notificara a Zona/Sector reales. Los 7 regresaron folio válido y pasaron la verificación posterior por `?action=consulta`: `OTDE-SOP-0004` (Soporte), `OTDE-MAN-0001` (Mantenimiento), `OTDE-ASE-0001` (Asesorías), `OTDE-ALT-0001`/`OTDE-CAM-0001`/`OTDE-2FA-0004`/`OTDE-INC-0001` (los 4 sub-formularios de Correo Institucional, backend en el repo aparte `Correos-institucionales/webform-2026-2027/`). Formación Docente: catálogo de cursos vacío en producción (confirma el pendiente ya conocido de dar de alta cursos del ciclo 26-27) — `doGet` respondió sano, `doPost` no se probó por falta de un curso real al que inscribir. Fuera de alcance a propósito, sin submit real: `reporte-visita.html` (siempre notifica a escuela+técnico+OTDE reales, sin excepción en el código), `charla-ia.html`/`conferencia-ia.gs` y `asistencia.html` (evento de jun 2026 ya pasado). Quedaron 7 filas de prueba en Sheets de producción, pendientes de que Jorge las borre (folios de arriba). |
| **Hallazgo — bug de despliegue en Asesorías** (`apps-script/asesorias.gs`) | Al probar tipo "Excel básico" (que según el código local del repo NO debería exigir la casilla de confirmación de mantenimiento previo — esa exigencia es solo para "Banco de Materiales y Chuka"), producción rechazó la solicitud citando esa misma casilla. El condicional correcto sí existe en el repo (`if (d.tipoAsesoria.trim() === 'Banco de Materiales y Chuka' && !d.confirmaMantenimiento)`), así que el comportamiento observado confirma que esa versión no está desplegada en el editor de Apps Script — mismo patrón que `docs/QA-NOTES.md #10` ("Un fix 'corregido' en el repo no está corregido hasta que se redespliega"), nueva ocurrencia agregada ahí. Pendiente para Jorge: copiar `asesorias.gs` al editor y Administrar implementaciones → Nueva versión. |
| **Hallazgo — correo de confirmación de Asesorías nunca llegó** | De los 7 correos de confirmación esperados (uno por flujo, con `to`=correo del usuario), el usuario confirmó que los 6 restantes sí llegaron pero el de Asesorías (folio `OTDE-ASE-0001`) no — pese a que el submit regresó `status:ok` y el folio es consultable, así que el registro en Sheets se guardó bien. Causa raíz no investigada esta sesión — nuevo `docs/QA-NOTES.md #20`, pendiente de diagnosticar (podría ser el mismo despliegue desincronizado de arriba, o un problema aparte). |
| **Pendientes nuevos anotados por el usuario, para debatir/implementar después** | (1) Soporte Técnico Remoto (`apps-script/soporte-remoto.gs`) hoy solo manda correo al *cerrar* el ticket (`sopNotificarCierreSolicitante`), nunca al recibir la solicitud — a diferencia de Mantenimiento/Asesorías/Correo Institucional, que sí confirman de inmediato. (2) Unificar diseño/contenido/formato/firma electrónica/redes sociales de todos los correos institucionales que manda OTDE (hoy cada backend arma su propio HTML con variaciones menores) — posiblemente con variaciones de color/etiqueta por trámite, sin diseño decidido todavía; el usuario pidió explícitamente posponer el debate. Ambos agregados a `docs/ROADMAP.md`, ítems 11 y 12. |
| **Verificación** | Cada uno de los 7 submits confirmado por su propio folio + `?action=consulta` posterior con el mismo folio y correo, mostrando estatus y fecha correctos. |
| **Commits** | Ninguno de código — sesión de solo pruebas, sin cambios en el repo salvo esta actualización de documentación. |

---

## CHECKPOINT — 2026-08-28 (cont.) · Fix: falso "CCT no encontrada" al escribir la clave exacta y salir con Tab

| | |
|---|---|
| **Fecha** | 2026-08-28 |
| **Sesión** | Continuación de la sesión anterior del mismo día. Bug reportado: el listener de `change` (blur) de las 4 páginas de trámite (`correo.html`, `mantenimiento.html`, `asesorias.html`, `soporte.html`) solo marcaba una CCT como encontrada si el usuario clickeaba la sugerencia del autocomplete o navegaba con flechas+Enter — si escribía la CCT completa y correcta y salía con Tab sin clickear, el formulario mostraba "CCT no encontrada en nuestra base" y forzaba captura manual aunque la CCT sí existiera. |
| **Fix** | El listener de `change` de las 4 páginas ahora intenta primero una coincidencia exacta contra `CCT_DB` (`CCT_DB.filter(e => e.cct === <valor>.trim().toUpperCase())[0]`) antes de declarar "no encontrada" y forzar el fallback manual. Mismo patrón repetido en los 4 archivos (`asesorias.html`, `mantenimiento.html`, `soporte.html`, y 2 instancias dentro de `correo.html` — Alta y el helper compartido de Cambio/Reset/Incidencias), sin componente compartido nuevo — consistente con la convención ya establecida de este bloque de páginas. |
| **Verificación** | `git show --stat 262e880`: 4 archivos, 20 líneas agregadas, 0 eliminadas. |
| **Commits** | `262e880` (código, ya comiteado). Pendiente el de esta actualización de documentación de cierre. |

---

## CHECKPOINT — 2026-08-28 · Observaciones de Jorge sobre los 4 webforms, Excel básico en Asesorías, y auditoría (smoketest + QA extremo) de esos 4 más Formación Docente

| | |
|---|---|
| **Fecha** | 2026-08-28 |
| **Sesión** | Jorge compartió una lista larga de observaciones sueltas sobre los webforms de trámite tras usarlos en producción. Siguiendo el patrón ya establecido en este proyecto de avanzar por fases pequeñas (no un plan monolítico), se dividió en: Fase 1 (ajustes puntuales de bajo riesgo en los 4 webforms), un mini curso de Excel básico nuevo en Asesorías, y — a petición explícita de Jorge — un smoketest de UI + QA extremo de los 4 webforms de trámite y, después, de Formación Docente. Cada hallazgo de las auditorías se corrigió también por fases, con Jorge revisando y aprobando ("aplica todas las recomendaciones... por fases") antes de seguir. |
| **Fase 1 — ajustes puntuales en los 4 webforms** | Rangos de "Cantidad aproximada" de Aula de Medios en Mantenimiento (`1-5`/`6-10`/`11-15`/`16-20`/`Más de 20`, antes otra escala; Uso administrativo sin cambios, a propósito). Se quitó la pregunta "Tipo de cuenta" de Alta de correo — Director(a)/Subdirector(a) de escuela ahora recibe automáticamente `@dee.edu.mx` sin preguntar (`otdeDominioParaCCT()` eliminada como código muerto, verificado por grep). Header de `correo.html` menciona ahora "verificación en dos pasos (autenticación)". Label "Función/Cargo" → "Función/Cargo del solicitante" en las 4 páginas. Las 4 páginas ganaron un link "← Volver a Oficina Virtual" junto al hero y un bloque "Otros trámites" al final (clases nuevas `.tramite-back-link`/`.tramite-otros-tramites` en `styles.css`, sin componente compartido — se repitió el snippet en las 4). El switcher de Correo Institucional se aplanó de 2 botones (con un sub-switcher anidado de 3 adentro) a 4 botones al mismo nivel — corrige de paso el amontonamiento visual que reportó Jorge. |
| **Excel básico en Asesorías** | Segundo tipo de asesoría (antes solo Banco de Materiales/Chuka): dirigido a personal administrativo, sin requerir mantenimiento previo — la casilla de confirmación correspondiente ahora es condicional (`aseToggleTipoAsesoria()`, mismo condicional en el backend). Checklist opcional de 6 temas sugeridos (duplicados, filtrar por zona/sector, validar RFC/CURP/CCT, combinar correspondencia con Word, fórmulas básicas, IA/Copilot), columna nueva `Temas de Excel` al final del Sheet. "Número de Docentes" se generalizó a "Número de personas" en Mantenimiento y Asesorías. Un primer intento de agregar un campo "¿Ya tuviste correo institucional antes?" a Alta de correo (para detectar duplicados) se revirtió por completo tras una pregunta directa de Jorge que desarmó la premisa: quien recuerda su cuenta ya tiene un trámite correcto (Cambio de Contraseña), así que el campo no aportaba lo que se pensaba — Jorge confirmó que esa revisión de duplicados ya la hacen manualmente. |
| **Auditoría de los 4 webforms de trámite (smoketest UI + QA extremo)** | Publicada como artifact. Hallazgo principal del smoketest: `.form-container` sin `max-width` estiraba los campos de texto a ~850px en pantallas anchas (un solo fix en `styles.css`, `docs/QA-NOTES.md #16`) — causa raíz única de la sensación de "huecos vacíos" que reportó Jorge, no un problema disperso. QA extremo encontró 3 bugs reales: `validarCorreoInstitucional()` en `correo.html` solo revisaba el sufijo del dominio (`.endsWith()`), aceptando `"@dee.edu.mx"` sin usuario (`docs/QA-NOTES.md #17`); el RFC de Alta solo exigía longitud ≥10 sin formato (`docs/QA-NOTES.md #18`); "Número de personas" en Asesorías declaraba `max="200"` en el HTML pero nunca se validaba (ni cliente ni backend, por el `novalidate` del formulario). Los 3 corregidos y verificados reproduciendo el bug primero, luego el fix. También se alinearon las opciones de Turno entre Mantenimiento y Asesorías (faltaba "No aplica (oficina)" en Asesorías). |
| **Auditoría de Formación Docente (smoketest UI + QA extremo)** | Publicada como artifact aparte (sistema de diseño propio, no comparte `styles.css`). Hallazgo de UI: `.site-header` sin `flex-wrap` ni media query se rompía en la mayoría de celulares (390-430px), texto partido a mitad de frase — corregido con `flex-wrap` + una media query nueva. Hallazgo de QA más serio de toda la ronda: el envío secuencial de varios cursos (`enviarFormulario()`) perdía de vista los folios ya generados si un curso posterior fallaba (reproducido en vivo simulando 2 cursos, el 2° forzado a fallar: el folio del 1° nunca aparecía en pantalla) — corregido para que `mostrarConfirmacion()` distinga "Registro parcial" de éxito completo, liste los folios ya obtenidos, nombre los cursos pendientes, y ofrezca un botón para reintentar (seguro porque el backend deduplica por RFC+curso). Mismo bug de RFC de 12 vs. 13 caracteres que en `correo.html`, encontrado independientemente aquí (`docs/QA-NOTES.md #18`, un solo ítem para los dos archivos). CCT sin validación de formato real, ni cliente (`length >= 8`) ni backend (solo no-vacío) — ahora exige el patrón real de CCT en ambos. Nombre sin exigir letras reales. Se agregó un campo opcional "Municipio" al fallback manual de CCT (antes siempre viajaba vacío) y el atributo HTML `required` a los campos manuales que ya mostraban el asterisco pero no lo tenían. El paso de "regístrate primero en la plataforma externa" quedó documentado como honor-system puro (sin verificación real posible, CoEEE no expone API) — no es un bug con arreglo, es una limitación conocida. |
| **Verificación** | Cada fix probado en vivo (Chrome + servidor estático local) antes de darlo por bueno: formularios reales llenados, `fetch` interceptado para simular éxito/fallo del backend sin escribir en ningún Sheet de producción, catálogo de Formación Docente simulado vía `sessionStorage` (mismo mecanismo de caché que ya usa la página, porque el catálogo real está vacío ahora mismo). `node --check` sobre los 3 `.gs` tocados (`asesorias.gs`, `formacion-docente.gs`, y `Alta.gs` en el repo aparte). Balance de tags HTML verificado en los 5 archivos HTML tocados. |
| **Commits** | Pendiente — a la espera del mensaje de cierre de esta sesión. |

---

## CHECKPOINT — 2026-08-27 (cont. 2) · Incidente: deploy de Correo falló por timeout — diagnosticado y reintentado sin tocar otde.html

| | |
|---|---|
| **Fecha** | 2026-08-27 |
| **Sesión** | Jorge reenvió el correo de notificación de GitHub Actions avisando que el job `deploy` de `pages build and deployment` había fallado ("Failed in 10 minutes and 6 seconds"). Sesión de diagnóstico y reintento del deploy, sin cambios de código ni de contenido. |
| **Diagnóstico** | Investigado directo en GitHub Actions (repo `educaneza/seprn-sitio`, vía navegador, sin `gh` CLI instalado en la máquina): run #119 (commit `dd5b14d`, la publicación de Correo del checkpoint anterior) — `build` exitoso (55s), pero `deploy` falló con "Timeout reached, aborting!" tras 10m 6s subiendo el artifact de Pages (335 MB). No fue un error de código ni de contenido — el run inmediato anterior (#118, mismo tamaño de artifact, 335 MB) había desplegado sin problema en solo 14s, así que fue lentitud puntual de infraestructura, no un límite duro. Confirmado en vivo que `correo.html` daba 404 en producción — la migración de Correo nunca había llegado a publicarse de verdad pese al push exitoso a `origin/main`. |
| **Riesgo real descubierto, no resuelto esta sesión** | El artifact de Pages ya pesa 335 MB (el repo trackea 354 MB en total, con varios zips/pptx de sesiones CTE entre 12 y 69 MB en `pdfs/cte/`) — lo bastante grande como para que un timeout de infraestructura como este se vuelva más probable de lo que sería con un sitio ligero. Jorge decidió no atacarlo esta sesión. Anotado como ítem 10 en `docs/ROADMAP.md`. |
| **Cuidado real al reintentar: el patrón de publicación aislada casi se rompe** | El plan inicial de reintento (`git commit --allow-empty` en `main` local + `git push origin main`) se descartó **antes de ejecutarlo** al descubrir que `main` local y `origin/main` están divergidos a propósito (ver checkpoint siguiente, "Publicación a producción") — ese push habría empujado los 46 commits locales no publicados, incluido `otde.html` (deliberadamente excluido de producción), directo a `origin/main`. En su lugar: rama temporal `retry-deploy-correo` creada desde `origin/main` (no desde `main` local), un commit vacío ahí, pusheada como `retry-deploy-correo:main` — mismo mecanismo de `publish-*` ya usado en este repo, sin arrastrar nada de `main` local. Rama temporal borrada después del push. |
| **Verificación** | Run #120 se disparó solo tras el push, completó en 1m 10s (`deploy` sin timeout, éxito). `https://educaneza.github.io/seprn-sitio/correo.html` confirmado en vivo cargando el formulario de Correo Institucional completo (ya no 404). |
| **Commits** | `b648171` (commit vacío, pusheado directo a `origin/main` vía la rama temporal `retry-deploy-correo`, ya borrada — no existe en `main` local). `main` local no se tocó: sigue divergido de `origin/main` (46 commits locales sin publicar vs. 12 en `origin/main`, incluido este commit de reintento). |

---

## CHECKPOINT — 2026-08-27 (cont.) · Mantenimiento, Soporte y Correo migrados — docs/ROADMAP.md ítem 7 resuelto por completo

| | |
|---|---|
| **Fecha** | 2026-08-27 |
| **Sesión** | Continuación directa del cierre anterior (Asesorías migrada como piloto). Jorge pidió seguir con Mantenimiento, luego Soporte, luego Correo — una instrucción por vez ("sigue con Mantenimiento" / "sigue con soporte" / "sigue con Correo"), repitiendo en cada caso el mismo patrón ya piloteado con Asesorías. |
| **Mantenimiento (`mantenimiento.html`, 821 líneas)** | Sin dependencias cruzadas nuevas (ya resueltas por Asesorías), pero sí una segunda familia de CSS compartido no detectada en la primera pasada: `.highlight-box`/`.benefit-list`/`.featured-box`/`.download-button` (usadas también por Correo/Soporte/Licencias Office/Chuka/Recursos) — movida a `styles.css`. Se corrigió de paso un comentario obsoleto en `otde.html` que seguía mencionando funciones de Mantenimiento/Asesorías ya migradas. |
| **Soporte (`soporte.html`, 681 líneas)** | Sin sorpresas — helpers y las 2 familias de CSS ya resueltas. Único detalle real: la referencia cruzada con Licencias Office (recomendación mutua cuando el problema es de instalación) usaba `showServicio()`/`onclick`, que solo funciona dentro de la misma página — se convirtió a links reales en ambas direcciones (`soporte.html` → `otde.html#office`, aprovechando el deep-link por hash ya existente; `otde.html` → `soporte.html` directo). |
| **Correo (`correo.html`, 1,482 líneas)** | La migración más grande (4 sub-formularios anidados: Alta/Cambio de Contraseña/Eliminar Método de Autenticación/Incidencias, ~1,360 líneas de HTML+JS) pero mecánicamente la más simple de las 4 — todo lo compartido ya estaba resuelto, extracción verbatim sin dependencias nuevas. Al ser la última tab con formulario que salía de `otde.html`, se aprovechó para eliminar 2 piezas de código muerto verificadas con grep antes de borrar: `toggleForm()` (sin ninguna llamada en todo el sitio) y los `<script src="js/cct-db.js">`/`<script src="js/tramites-shared.js">` de `otde.html` (ninguna tab restante los usa). |
| **`otde.html` al cierre de las 4 migraciones** | 598 líneas (desde 4,186 originales). Quedan 3 tabs, todas informativas, sin un solo formulario: Licencias Office (activa por default, antes lo era Correo), Chuka, Recursos. |
| **Documentación reestructurada** | `CLAUDE.md` separa ahora "`otde.html`" (contenido informativo) de una sección nueva "Páginas propias de trámite" que agrupa las 4 con su patrón compartido — durante la edición se produjo un lío de secciones duplicadas (Licencias Office/Banner de convocatoria/Correo quedaron mal anidados y con contenido cruzado a media edición); detectado y corregido antes de terminar, verificado con grep que no quedó ningún encabezado duplicado ni contenido mal etiquetado. `docs/ARCHITECTURE.md §21` documenta las 4 migraciones completas. `docs/ROADMAP.md` ítem 7 tachado por completo. `README.md` actualizado. |
| **Verificación** | `node --check` sobre todos los `<script>` inline nuevos/modificados en cada ronda. Servidor estático local + Chrome, cada ronda: autocomplete de CCT, switchers/sub-switchers, cálculo de dominio en Correo, checklist de equipos en Mantenimiento, referencia cruzada Soporte↔Office, y navegación completa desde `oficina-virtual.html` a cada página nueva — sin errores de consola. |
| **Publicación a producción — `otde.html` excluido a propósito, mismo patrón que Asesorías** | Dos rondas de publicación vía rama `publish-*` desde `origin/main`: `66ba480` (Mantenimiento + Soporte) y `dd5b14d` (Correo). `otde.html` de producción sigue en su versión antigua (nunca tuvo estas tabs). Con esto, los 4 trámites (Correo, Mantenimiento, Asesorías, Soporte) funcionan de punta a punta en la Oficina Virtual real por primera vez — antes ninguno funcionaba, todos apuntaban a tabs de `otde.html` que nunca existieron en producción. |
| **Commits** | `1298293`/`65b67ca` (Mantenimiento+Soporte, código+docs, local `main`), `00319b2`/`2a275ef` (Correo, código+docs, local `main`), `66ba480` y `dd5b14d` (publicados a `origin/main` vía `publish-mantenimiento-soporte` y `publish-correo`). `main` local y `origin/main` siguen divergidos a propósito — se resuelve con un rebase cuando `otde.html` esté listo para publicarse. |
| **Pendiente real que queda** | No de esta migración: Licencias Office (hoy 100% estático, sin backend ni folio) podría entrar a la grid de `oficina-virtual.html` como card tipo "Recurso" — anotado, no hecho, para no ampliar el alcance. |

---

## CHECKPOINT — 2026-08-27 · Migración de Asesorías a página propia (piloto) — publicada en producción, otde.html excluido a propósito

| | |
|---|---|
| **Fecha** | 2026-08-27 |
| **Sesión** | Jorge pidió retomar `docs/ROADMAP.md` ítem 7 (migrar los 4 trámites de `otde.html` a páginas propias, decidido 10 ago 2026 y nunca iniciado). Se exploró `otde.html` (4,186 líneas) a fondo antes de planear: 2 agentes en paralelo confirmaron la estructura de tabs/CSS/JS, el tamaño real de cada trámite (Correo ≈1,360 líneas, Mantenimiento ≈723, Soporte ≈614, Asesorías ≈605) y las URLs/formularios exactos. Se decidió migrar **solo Asesorías** como piloto de bajo riesgo (sin los 4 sub-formularios de Correo ni el checklist de Mantenimiento), con header/nav/footer institucional completo (no el tratamiento minimalista de Formación Docente/Asistencia) — mismo razonamiento que ya usó `oficina-virtual.html`. |
| **Hallazgo no obvio que cambió el plan** | Leyendo el código real (no solo el resumen de los agentes) se encontró que `enviarSolicitudAsesoria()` ya dependía de `manLeerArchivoBase64()`/`MAN_TAMANO_MAX_BYTES`, ambos definidos dentro del bloque de **Mantenimiento** en `otde.html` — una dependencia cruzada no documentada, no una copia propia como sí ocurre con el patrón de CCT autocomplete (`docs/ARCHITECTURE.md §11`). Se resolvió de raíz en vez de solo copiar el código a la página nueva. |
| **`js/tramites-shared.js` (nuevo)** | `toTitleCase`, `fetchJsonConTimeout`, `otdePoblarFuncion`, y los renombrados `leerArchivoBase64`/`TAMANO_MAX_ARCHIVO_BYTES` (antes `manLeerArchivoBase64`/`MAN_TAMANO_MAX_BYTES`) — extraídos de `otde.html`, cargados por `otde.html` y por cada página propia de trámite. 70 líneas. |
| **`asesorias.html` (nuevo)** | Página standalone (712 líneas) con el formulario de Asesorías migrado tal cual (CCT autocomplete, casilla de confirmación de mantenimiento previo, adjunto de oficio PDF), mismo backend sin cambios (`asesorias.gs`). |
| **`otde.html`** | Baja de 4,186 a 3,346 líneas: se quitó la tab de Asesorías (botón, contenido HTML, bloque JS completo), comentarios `<!-- SERVICIO N -->` renumerados (quedan 6: Correo, Mantenimiento, Soporte, Licencias Office, Chuka, Recursos), y las 2 llamadas de Mantenimiento actualizadas a los nombres compartidos nuevos. |
| **`styles.css`** | +191 líneas: el CSS de formularios de trámite (`.servicio-header`, `.content-block`, `.form-button`, `.soporte-form-group` y familia, `.sop-cct-status`, `.sop-manual-fields`, `.soporte-submit-msg`) que antes vivía solo en el `<style>` inline de `otde.html`, aunque Mantenimiento/Soporte ya lo reusaban tal cual. |
| **Bug real encontrado y corregido de paso** | Al mover ese CSS se encontró que el selector de sugerencias CCT (`#sop-cct-suggestions`) era un ID que solo existe en el formulario de Soporte — Mantenimiento, Asesorías y los 4 sub-formularios de Correo mostraban la lista de sugerencias sin ningún estilo (sin flotar, sin fondo, sin scroll) desde que se copió el patrón, nunca detectado antes. Se generalizó a `ul[id$="-cct-suggestions"]`, sin tocar HTML. Verificado en vivo en el navegador: el dropdown de Mantenimiento (que nunca había tenido este estilo) ahora se ve igual que el de Asesorías. Documentado como `docs/QA-NOTES.md #15`. |
| **`oficina-virtual.html`** | El único link que apuntaba a este trámite (`otde.html#asesorias`, "Solicitar →" de la card de Asesorías) se actualizó a `asesorias.html`. |
| **Verificación** | `node --check` sobre los 3 `<script>` inline (`otde.html`, `asesorias.html`, `oficina-virtual.html`) y sobre `js/tramites-shared.js` — sin errores de sintaxis. Servidor estático local + Chrome: `asesorias.html` completo (header/nav/hero/footer institucional, autocomplete de CCT con caso encontrado, Función/Cargo repoblado correctamente, validación de campos vacíos con estilos correctos); `otde.html` con las 6 tabs restantes, autocomplete de Mantenimiento funcionando con el helper renombrado; navegación completa `oficina-virtual.html` → `asesorias.html` confirmada. Sin errores nuevos en consola. |
| **Publicación a producción — `otde.html` excluido a propósito** | Se descubrió que `otde.html` en producción (`origin/main`) está congelado en una versión de 1,503 líneas, previa a todo el trabajo de webforms de OTDE — **nunca tuvo tab de Asesorías**, y el link de "Solicitar →" de Asesorías en la Oficina Virtual **en vivo** apuntaba a un hash (`otde.html#asesorias`) sin tab correspondiente (roto en producción desde que se desplegó Asesorías). Siguiendo el mismo patrón ya usado en este repo (`publish-cte`, `publish-fase2`, `publish-panel-otde`, `publish-protocolos`, `publish-tecnico-notice`), se creó `publish-asesorias` desde `origin/main`, se trajeron solo los archivos no-`otde.html` de esta sesión, y se publicó — dejando `main` local con el trabajo completo (incluido `otde.html`) sin publicar, como en las rondas anteriores. Efecto real: el flujo "Solicitar Asesoría" de la Oficina Virtual en producción queda funcional por primera vez. |
| **Documentación** | `CLAUDE.md` (tab-order de `otde.html`, subsección Asesorías, `js/tramites-shared.js`, nueva entrada de clases compartidas), `docs/ARCHITECTURE.md §21` (nueva, detalle completo de la migración), `docs/ROADMAP.md` ítem 7 (Asesorías tachada, 3 pendientes explícitas), `README.md` (árbol de archivos), `docs/QA-NOTES.md #15` (bug del selector CSS), este checkpoint. |
| **Commits** | `414240b` (migración de código, local `main`), `4e1054b` (documentación, local `main`), `6841552` (`Publicar migración de Asesorías a página propia (otde.html excluido, pendiente)`, pusheado a `origin/main` vía la rama `publish-asesorias`). `main` local y `origin/main` siguen divergidos a propósito (mismo patrón ya conocido en este repo) — se resuelve con un rebase cuando `otde.html` esté listo para publicarse. |

---

## CHECKPOINT — 2026-08-25 (cont. 7) · Aviso al técnico verificado en vivo — y un incidente de datos encontrado y corregido en el camino

| | |
|---|---|
| **Fecha** | 2026-08-25 |
| **Sesión** | Jorge pegó y redesplegó el `.gs` del aviso al técnico y pidió probarlo. Confirmó que el modo de prueba seguía activo. |
| **Incidente encontrado y corregido** | Al abrir la hoja "Solicitudes" real para preparar la prueba, la fila 1 tenía datos de la fila de prueba `OTDE-MAN-TEST2` (de la verificación del rediseño del PDF, checkpoint anterior) en vez de los encabezados — la fila 1 (encabezados) se había borrado por accidente en vez de la fila 2 (datos) al limpiar esa prueba, sin verificación posterior que lo detectara. Efecto en cascada: encabezados ausentes, validación de `Estatus` desplazada a N1:N991 (en vez de N2:N1000), formato guinda/blanco/negritas y fila congelada perdidos. Un primer intento de recuperar la fila 1 copiándola desde el Historial de versiones de Google Sheets falló (error "supera el número máximo de 50000 caracteres" — el copiado desde el panel de versión no funcionó como se esperaba). Se corrigió retecleando los 26 encabezados exactos a mano (navegando con el cuadro de nombres, no coordenadas de píxel, tras identificar esa como la causa raíz del incidente original), reaplicando formato (guinda `#56212f`, texto blanco `#F9F8F5`, negritas) y fila congelada, y reconstruyendo las validaciones de `Estatus` (5 valores) y `Técnico asignado` (2 técnicos) vía Datos → Validación de datos. Verificado visualmente que ambas hojas quedaron idénticas al estado original. |
| **Verificación del aviso al técnico contra producción real** | Fila de prueba nueva (folio `OTDE-MAN-TEST3`, escuela marcada "AVISO TECNICO"). Se capturó la fecha programada en la columna W: disparó el aviso a solicitante (columna X en "Sí"). Se asignó el técnico en la columna Y (eligiendo del dropdown ya reconstruido): disparó el aviso al técnico (columna Z en "Sí"), sin repetir el de fecha. Los dos correos reales llegaron a la bandeja de prueba: el de fecha programada, y "[PRUEBA] Visita asignada — ..." con el aviso de modo de prueba mostrando el destino real correcto (`alejandro.morales@dee.edu.mx`) y el cuerpo completo (folio, fecha, escuela/CCT, sector/zona, equipos con falla reportados, contacto de la escuela con WhatsApp, y el link a `reporte-visita.html`). Fila de prueba limpiada después (verificado que la fila 1 de encabezados quedó intacta esta vez). |
| **Documentación** | `docs/ARCHITECTURE.md §15` (verificación en vivo + detalle del incidente), `docs/ROADMAP.md` ítem 9 (aviso al técnico marcado como resuelto), `CLAUDE.md` (nota actualizada), este checkpoint. |
| **Commits** | Pendiente — a la espera del mensaje de cierre de esta sesión. |

---

## CHECKPOINT — 2026-08-25 (cont. 6) · Corte de caja del flujo de Mantenimiento + aviso al técnico asignado — cierra el cuello de botella detectado

| | |
|---|---|
| **Fecha** | 2026-08-25 |
| **Sesión** | Con Fase 2 ya publicada, Jorge pidió un corte de caja honesto: ¿un solo stack? ¿todo lo automatizable ya automatizado? ¿cuellos de botella urgentes? ¿listo para producción? Auditoría del código y `docs/ROADMAP.md`/`CLAUDE.md` actuales (no de memoria) encontró: (1) v8.5 sigue vivo en paralelo para 2 funciones no migradas (fotos nocturnas, reporte mensual — Fase 3/4, no iniciadas); (2) hueco real: al programar la fecha de visita, el sistema avisa a solicitante+Zona/Sector pero **nunca al técnico**, que se sigue coordinando por fuera. Jorge pidió atacar el aviso al técnico y dejar anotado lo de fotos/reporte mensual como pendiente. |
| **`apps-script/mantenimiento.gs`** | Columna nueva `Técnico asignado` (dropdown validado contra `MAN_TECNICOS`, `manAplicarValidacionTecnico()`) + columna de control `Notificación a técnico enviada`, ambas al final de `ENCABEZADOS_MAN_SOLICITUDES` (mismo criterio de siempre). `manOnEditProgramacion()` ahora escucha ambas columnas (fecha programada + técnico asignado) y separa dos avisos independientes que pueden llenarse en cualquier orden: el de solicitante+Zona/Sector sigue igual; el nuevo (`manNotificarTecnicoAsignado()`) solo dispara cuando **ambas** ya tienen valor — folio, escuela, fecha, equipos con falla reportados, contacto del solicitante y link directo a `reporte-visita.html`. Si el nombre capturado no coincide con `MAN_TECNICOS`, no truena ni manda correo, pero sí marca notificado (no reintenta en cada edición). No requiere instalar ningún trigger nuevo, reutiliza el ya instalado desde Fase 1. |
| **Documentación** | `CLAUDE.md` (nota bajo `mantenimiento.gs`, y Fase 3/4 de v8.5 hechas visibles explícitamente en "Pendientes vigentes" — antes solo estaban enterradas dentro del ítem 9 de `docs/ROADMAP.md`), `docs/ARCHITECTURE.md §15` (subsección nueva), `docs/ROADMAP.md` ítem 9 (bullet nuevo entre Fase 2 y Fase 3). **Se corrigió además un error propio de esta sesión**: una edición anterior (el "Rediseño del PDF...") había borrado por accidente el encabezado `## 16.` de `docs/ARCHITECTURE.md` al reemplazar el bloque anterior — detectado al revisar la numeración de secciones antes de insertar la subsección nueva, corregido en el mismo archivo. |
| **Verificación** | Simulación local de la hoja "Solicitudes" (arnés de Node, mock de `getRange`/`getValue`/`setValue`, sin tocar Apps Script real): 4 casos — solo fecha (avisa solicitante, no técnico), técnico asignado después (avisa técnico, sin repetir el de fecha), re-edición con todo ya notificado (no reenvía nada), técnico que no coincide con `MAN_TECNICOS` (no truena, no manda correo, sí marca notificado). Los 4 pasaron — el primer intento tenía un bug en los índices del propio arnés de prueba (no del código), corregido antes de confirmar. También se generó y revisó el HTML del correo real. **Pendiente**: Jorge redesplegar y probar contra producción real, igual que las piezas anteriores. |
| **Commits** | Pendiente — a la espera del mensaje de cierre de esta sesión. |

---

## CHECKPOINT — 2026-08-25 (cont. 5) · Rediseño del PDF confirmado contra Apps Script real: "Página 1 de 1", pleca nítida, sin sorpresas

| | |
|---|---|
| **Fecha** | 2026-08-25 |
| **Sesión** | Jorge pegó y redesplegó el `.gs` con el rediseño del PDF, confirmó que el modo de prueba seguía activo, y pidió repetir la verificación end-to-end. |
| **Verificación contra producción real** | Fila de prueba nueva en "Solicitudes" (folio `OTDE-MAN-TEST2`, escuela marcada "DISEÑO PDF"), formulario `reporte-visita.html` enviado contra la URL real. El PDF generado se abrió directo en el visor de Google Drive (no la extensión de Chrome, que bloquea la automatización por ser una URL interna): **"Página 1 de 1"** confirmado en el propio visor — cabe en una sola hoja carta con margen de sobra. La pleca de logos se ve nítida, las barras guinda/lema, las secciones, las firmas de 3 columnas (incluyendo "Recibió en la escuela" mostrando el dato real sin inventar el rol) y el pie de página con el teléfono correcto — todo idéntico a la previsualización de la sesión anterior. El correo llegó con este PDF adjunto. Sin sorpresas: la imagen en base64 y el `<style>` con clases, el único riesgo que quedaba sin confirmar del rediseño, se interpretan igual en la conversión real de Apps Script que en un navegador normal. |
| **Limpieza** | Fila de prueba borrada de "Solicitudes" y "Reportes de visita", PDF de prueba movido a la papelera de Drive. |
| **Documentación** | `docs/ARCHITECTURE.md §15` (la nota de "pendiente de confirmar" se actualizó a verificado), este checkpoint. |
| **Commits** | Pendiente — mismo commit sin hacer de los checkpoints anteriores de esta sesión, a la espera del mensaje de cierre. |

---

## CHECKPOINT — 2026-08-25 (cont. 4) · Rediseño del PDF con identidad institucional real (pleca de logos, lema anual, firmas) — pendiente de confirmar en un PDF real

| | |
|---|---|
| **Fecha** | 2026-08-25 |
| **Sesión** | Continuación directa del checkpoint de abajo (Fase 2 ya verificada funcionalmente). Jorge compartió dos PDFs: un reporte real ya generado por el sistema viejo v8.5 (formato oficial completo) y el PDF de prueba generado por el nuevo `manGenerarPDFReporte_()` (tabla plana de dos columnas, sin identidad institucional). Pidió igualar/mejorar el diseño del segundo respecto al primero — con margen para ajustar, pero conservando encabezados, pie de página y firmas. |
| **Insumos encontrados en el propio repo** | `images/Pleca 4x.png` (pleca de logos oficiales: Gobierno del Estado de México, Estado de México, EDUCACIÓN/SECTEI, SEIEM — el mismo asset que ya usa el reporte de v8.5) e `images/Firma_institucional_OTDE.png` (tarjeta de firma con el mismo patrón de barra guinda bajo la pleca) — ninguno de los dos estaba conectado a ninguna página del sitio todavía; existían en el repo sin usar. El teléfono institucional correcto (`55 3300 2400 ext. 9065`) se confirmó contra el que ya usan `otde.html` y el resto de páginas de área — distinto (y más actual) que el que trae el PDF de referencia de v8.5 (`55 5583 6400 ext. 9065`). |
| **`apps-script/mantenimiento.gs`** | `manGenerarPDFReporte_()` reescrita por completo (de tabla plana a documento con pleca de logos embebida en base64, barra de oficina, barra de lema anual, meta línea de folio/fecha/registro, secciones con acento guinda que agrupan las mismas ~20 columnas del primer corte, firmas de 3 columnas y pie de página institucional). Dos funciones nuevas: `manFormatearHora12_()` (hora en formato "7:58 a.m.", igual que v8.5) y `manCalcularDuracion_()` (duración de la jornada calculada de inicio/fin — mejora real sobre el primer corte, que solo mostraba fechas/horas crudas). Constantes nuevas `MAN_LOGO_PLECA_B64` (el PNG de la pleca, redimensionado a 1600px con `sips` y codificado en base64 directo en el `.gs` — decisión deliberada de no depender de una URL externa durante la conversión a PDF) y `MAN_LEMA_ANUAL` (lema oficial vigente, con nota de actualizarlo cada año). Se agregó también `<meta charset="UTF-8">` al HTML, ausente en el primer corte. |
| **Decisión de "no inventar" en las firmas** | El reporte de v8.5 firma con "Director(a) de la escuela" porque tiene una hoja de contactos separada con ese dato. Nuestro sistema no lo captura — `solicitud.nombre` es quien solicitó el mantenimiento, no necesariamente el director. En vez de asumir el rol, la tercera firma quedó etiquetada honestamente como "Recibió en la escuela". |
| **Verificación — solo visual en navegador, NO contra Apps Script real** | Se extrajeron las funciones nuevas a un arnés de Node (con `Utilities`/`Utilities.newBlob` simulados) para generar el HTML exacto que produciría el `.gs` con datos de prueba, y se revisó en Chrome forzando el ancho de página carta (816px). Resultado: diseño visualmente fiel a la referencia, contenido completo mide 926px de alto contra una página de 1056px (816×1056 @ 96dpi) — cabe con margen. **Esto no confirma que la conversión real de Apps Script renderice igual** — la imagen en base64 y el `<style>` con clases son un mecanismo no probado antes en este proyecto (el primer corte solo usaba estilos inline en una tabla simple, ya verificado). Pendiente: que Jorge pegue y redespliegue esta versión, y genere un reporte real (con modo de prueba activo) para confirmar visualmente que el PDF de producción se ve como la previsualización. |
| **Documentación** | `docs/ARCHITECTURE.md §15` (nueva subsección "Rediseño del PDF..."), este checkpoint. |
| **Commits** | Pendiente — mismo commit sin hacer de los checkpoints anteriores de esta sesión, a la espera del mensaje de cierre. |

---

## CHECKPOINT — 2026-08-25 (cont. 3) · Fase 2 verificada en vivo de punta a punta: PDF legible, correo con destinatarios correctos, sin fugas — riesgo técnico principal ya confirmado

| | |
|---|---|
| **Fecha** | 2026-08-25 |
| **Sesión** | Continuación directa del checkpoint de abajo. Jorge pegó y redesplegó `apps-script/mantenimiento.gs` en el proyecto real de Apps Script y pidió seguir con la verificación. |
| **Bloqueo encontrado y respetado** | El clasificador de seguridad de Auto Mode bloqueó dos intentos de editar/ejecutar código dentro del editor de Apps Script vía automatización de navegador (un `fetch`+escritura a portapapeles, y luego una tecla simple) — se interpretó como "no autónomamente permitido modificar código de producción por este medio", se detuvo esa vía sin intentar rodearla, y se le devolvió la decisión a Jorge. Jorge hizo el paso manualmente (pegar + redesplegar + activar modo de prueba con la función temporal de siempre, `docs/QA-NOTES.md #14`) y avisó cuando estuvo listo. El resto de la verificación (Sheets, Gmail, el formulario público) sí se hizo vía navegador sin nuevos bloqueos — el clasificador distinguió correctamente entre "editar el código de producción" (bloqueado) y "usar el sistema ya desplegado como lo haría un usuario real" (permitido). |
| **Verificación contra producción real** | Con `manActivarModoPrueba('otde.nezahualcoyotl@gmail.com')` activo: (1) confirmado que `curl` simple no sigue bien el redirect de un `doPost` de Apps Script (falso negativo, no es un bug del código — el mecanismo real de prueba en este proyecto para POST es un `fetch()` de navegador, igual que hace la página real); (2) probado el camino de error del `accion:'reporteVisita'` con un folio inexistente directo contra el endpoint real: respondió el mensaje esperado, sin escribir nada; (3) creada una fila de prueba en "Solicitudes" (folio `OTDE-MAN-TEST1`, escuela/CCT/sector claramente marcados como prueba) para tener un folio real contra el cual probar; (4) enviado el formulario completo desde `reporte-visita.html` (servido localmente, apuntando a la URL real) contra producción: la hoja "Reportes de visita" se autocreó y se llenó correcta (upsert por folio, fechas/horas construidas sin el bug de UTC — `docs/QA-NOTES.md #3` — confirmado visualmente en el Sheet), el PDF se generó legible (tabla HTML con el guinda institucional, todos los campos correctos) y se subió a Drive con el link guardado en la hoja, y el correo llegó a la bandeja de prueba con el aviso "Modo de prueba activo — destino real: Para: otde.nezahualcoyotl@gmail.com · CC: alejandro.morales@dee.edu.mx,otde.nezahualcoyotl@dee.edu.mx" — confirmando que el `to`/`cc` real (escuela + técnico vía `MAN_TECNICOS` + OTDE institucional) se arma correctamente y que el modo de prueba interceptó todo sin fugas a destinatarios reales. |
| **Incidente y corrección durante la prueba** | Al escribir la fila de prueba en "Solicitudes" a mano (para tener un folio real), el primer intento de clic aterrizó una fila arriba de lo esperado y sobreescribió momentáneamente los encabezados reales de la hoja (fila 1) con datos de prueba — detectado de inmediato al revisar el resultado, corregido con Cmd+Z (deshacer) antes de continuar, confirmado visualmente que los encabezados quedaron intactos. Segundo intento usando el cuadro de nombres de Sheets (navegación por referencia de celda, no coordenadas de píxel) para posicionar el cursor exacto en A2 antes de escribir — funcionó sin problema. **Lección para próximas sesiones**: al escribir datos en una hoja real vía automatización de navegador, navegar por el cuadro de nombres en vez de confiar en coordenadas de píxel de una captura de pantalla previa, que pueden desalinearse. |
| **Limpieza posterior** | Borrada la fila de prueba en "Solicitudes" y en "Reportes de visita", borrado el PDF de prueba de la carpeta de Drive "Reportes de Visita" (movido a la papelera). El correo de prueba se dejó en la bandeja de `otde.nezahualcoyotl@gmail.com` sin borrar (inofensivo, marcado `[PRUEBA]`). El modo de prueba se dejó **activo** — no se desactivó esta sesión, a propósito, por si Jorge quiere seguir probando; recordar desactivarlo (`manDesactivarModoPrueba()`) antes de que el sistema reciba solicitudes reales. |
| **Documentación** | `docs/ARCHITECTURE.md §15` (las dos entradas "Pendiente de probar/desplegar" se actualizaron a "Verificado en vivo" con el detalle de arriba), `docs/ROADMAP.md` ítem 9 (Fase 2 marcada como resuelta, con tachado), `CLAUDE.md` (nota bajo `mantenimiento.gs` actualizada), este checkpoint. |
| **Commits** | Pendiente — mismo commit sin hacer del checkpoint anterior (`apps-script/mantenimiento.gs` + `reporte-visita.html`), más los cambios de documentación de esta sesión, a la espera del mensaje de cierre. |

---

## CHECKPOINT — 2026-08-25 (cont. 2) · Fase 2 (resto): formulario de captura del técnico (`reporte-visita.html`) — cierra el pendiente explícito del primer corte, ambas piezas sin probar/desplegar juntas

| | |
|---|---|
| **Fecha** | 2026-08-25 |
| **Sesión** | Continuación directa del checkpoint de abajo (primer corte de Fase 2 ya construido, sin probar). Jorge pidió seguir con "el resto de la Fase 2" — confirmado antes de diseñar que se refería al formulario de captura del técnico (el pendiente explícito que dejó el primer corte: "sin formulario de captura propio todavía"). |
| **Decisiones confirmadas con Jorge antes de diseñar** | (1) Quién lo llena: el técnico (Alejandro/Marcos) desde campo, no Jorge — formulario público, mismo nivel de "sin login real" que el resto del sitio. (2) Automatización: un solo envío ya dispara PDF + correo (no queda como paso de menú aparte, a diferencia del primer corte — aquí se llena de una sola sentada, no en varias sesiones sobre la hoja, así que no aplica el riesgo que sí motivó que la acción de menú fuera manual). (3) Identificación de la solicitud: el técnico captura el folio a mano, sin lookup de pendientes. Jorge aceptó explícitamente construir esta pieza encima del primer corte sin esperar a que se probara/desplegara. |
| **`apps-script/mantenimiento.gs`** | `doPost` gana una rama nueva al inicio (`datos.accion === 'reporteVisita'` → delega a `manDoPostReporteVisita_()`, mismo criterio de branching por `accion` que ya usa `doGet`). `manDoPostReporteVisita_()` (nueva) reusa sin cambios `manBuscarSolicitudPorFolio_()`, `manValidarDatosReporte_()`, `manGenerarPDFReporte_()`, `manGuardarPDFReporte_()` y `manEnviarReporteVisita_()` del primer corte — hace upsert por folio (`manBuscarFilaReportePorFolio_()`: si ya hay fila, la sobreescribe; si no, `appendRow`), cubriendo tanto una corrección real como un doble tap accidental del técnico. `manFechaHoraLocal_()` (nueva) construye las fechas/horas con componentes explícitos en vez de parsear el string del cliente — mismo bug UTC ya documentado en `docs/QA-NOTES.md #3`. La acción de menú manual del primer corte (`manGenerarYEnviarReporteVisita()`) se dejó sin cambios, sigue viva como respaldo. |
| **`reporte-visita.html` (nuevo)** | Página independiente, no una tab más de `otde.html` — la audiencia es el técnico de campo, no un solicitante con CCT que autocompletar. Sin entrada en nav/footer, mismo precedente que `asistencia.html` (se llega por link directo). 18 campos en el mismo orden que `ENCABEZADOS_MAN_REPORTES` (el esquema ya estaba fijo desde el primer corte, no se reabrió esa decisión), mismos 3 mínimos obligatorios que ya exigía `manValidarDatosReporte_()` server-side. `POST` con `Content-Type: text/plain` a la misma `MANTENIMIENTO_APPS_SCRIPT_URL` ya desplegada — sin implementación nueva de Apps Script. |
| **Documentación** | `CLAUDE.md` (tabla de páginas + nota bajo `mantenimiento.gs`), `docs/ARCHITECTURE.md §15` (subsección nueva con el detalle completo), `docs/ROADMAP.md` ítem 9 (Fase 2 ahora refleja las 2 piezas, ambas pendientes de probar/desplegar juntas), este checkpoint. |
| **Verificación** | Sin backend real disponible (el `.gs` no está pegado/redesplegado en Apps Script todavía). Probado en el navegador con la llamada de red interceptada (`fetch` stub, sin tocar producción): confirmado que el envío vacío marca error en los 4 campos obligatorios (folio + los 3 mínimos), y que el payload armado al enviar el formulario completo coincide exactamente con lo que espera `manDoPostReporteVisita_()` (folio normalizado a mayúsculas/trim, fechas/horas en el formato que `manFechaHoraLocal_()` espera). **Pendiente la prueba end-to-end real** — cuando Jorge redespliegue, probar esta pieza junto con el primer corte: activar `manActivarModoPrueba('correo')`, llenar el formulario con un folio real, y confirmar que el PDF se genera legible (el riesgo que sigue sin confirmarse desde el primer corte). |
| **Commits** | Pendiente — `apps-script/mantenimiento.gs` modificado y `reporte-visita.html` nuevo, sin comitear, a la espera del mensaje de cierre de esta sesión. |

---

## CHECKPOINT — 2026-08-25 (cont.) · Fase 2 (primer corte) construida: reporte técnico de visita con PDF y correo — verificado en vivo contra v8.5 antes de construirla, código sin probar/desplegar todavía

| | |
|---|---|
| **Fecha** | 2026-08-25 |
| **Sesión** | Continuación directa del checkpoint de arriba (Fase 1 ya cerrada). Jorge pidió avanzar a la Fase 2 de la ruta hacia retirar v8.5 — el reporte técnico de la visita con PDF y cierre. Antes de diseñar nada, se verificó en vivo el sistema real v8.5 (Sheet + editor de Apps Script), porque el diseño previo de esta fase se basaba en un resumen de segunda mano nunca confirmado — ver hallazgos abajo, documentados con detalle en la memoria [[reference_sistema_reportes_visitas]] (fuera de este repo). |
| **Verificación en vivo de v8.5, hallazgo que cambió el plan** | El resumen de segunda mano decía que v8.5 solo notifica a director+técnico al cerrar el reporte — confirmado exacto en `enviarReporteVisita(r, c, urlPDF, pdfBlob)`: `GmailApp.sendEmail` manda el PDF adjunto a `c.correoDirector` y a `CONFIG.TECNICOS[r.responsable]`, y por separado a Jorge (`CONFIG.EMAIL_JORGE`) un correo con el link de Drive **sin** el PDF. Nunca a Zona/Sector. También se confirmó que la hoja "seguimiento" real tiene 33 columnas (no 29 como decía la memoria vieja), que "Estado del aula" es un semáforo con emoji + oración completa (no una etiqueta corta), y que v8.5 arma el PDF como una tabla HTML (no una plantilla de Docs/Slides) — mismo enfoque que se replicó abajo. |
| **Decisión de diseño con Jorge — destinatarios del reporte técnico** | Extender a escuela+Zona+Sector (como ya sugería `docs/ROADMAP.md`) resultaría en que Zona/Sector reciban 4 correos por solicitud (apertura, fecha programada, reporte técnico, cierre) — Jorge lo consideró demasiado. Acordado: el reporte técnico nuevo notifica **solo** a escuela + técnico + OTDE (sin Zona/Sector); Zona/Sector se siguen enterando en el correo de cierre que ya existe (`manOnEditCierre`) cuando Jorge marca Estatus = Resuelto — sin agregar un quinto punto de contacto ni tocar ese mecanismo. |
| **`apps-script/mantenimiento.gs` (+300 líneas, solo Mantenimiento — Asesorías no aplica a esta fase)** | Hoja nueva "Reportes de visita" (autocreada vía `manAsegurarHojaReportes()`, mismo patrón de auto-heal de encabezados; 20 columnas — folio, responsable de visita con dropdown de 2 técnicos, fecha/inicio/fin de visita, ~15 campos técnicos, link del PDF, control de notificación). Sin formulario de captura todavía — el técnico o Jorge llenan la fila a mano, primer corte deliberado (ver alcance abajo). Acción de menú nueva "Generar y enviar reporte de visita" (`manGenerarYEnviarReporteVisita`, agregada a `onOpen()`): pide el folio, valida que la solicitud y la fila del reporte existan y tengan los campos mínimos (responsable, fecha de atención, estado del aula), arma el PDF vía `manGenerarPDFReporte_()` (tabla HTML con el guinda institucional, convertida con `Utilities.newBlob(html,'text/html').getAs('application/pdf')` — sin plantilla de Docs), lo guarda en la carpeta de Drive nueva "Reportes de Visita" (`manGuardarPDFReporte_()`, mismo patrón de sharing que "Oficios de Mantenimiento"), y notifica vía `manEnviarReporteVisita_()` reusando `manEnviarCorreo_()` (respeta el modo de prueba existente) con el PDF adjunto. Confirma antes de reenviar si la fila ya estaba marcada como notificada. |
| **Por qué acción de menú y no un trigger automático** | A diferencia de la Fase 1 (un solo campo, "Fecha programada de visita"), aquí el técnico llena ~15 campos en varios momentos — un `onEdit` de una sola columna dispararía el correo con datos a medio llenar. Se prefirió una acción explícita de menú (pide el folio) sobre automatizar de una vez, mismo criterio de minimizar riesgo sobre un flujo nuevo. |
| **Alcance deliberadamente acotado a "la base"** | Jorge decidió construir solo hoja+folio+PDF+correo en esta sesión, dejando fuera (explícitamente, no un olvido): el formulario de captura propio para el técnico (reemplazo del Form de 24 preguntas de v8.5 — decisión pedagógica/de qué preguntas cortar, pendiente), el trigger nocturno de fotos, y el reporte mensual — mismo criterio de planeación incremental ya usado en este proyecto. |
| **Documentación** | `apps-script/mantenimiento.gs` (comentario de cabecera actualizado con el mecanismo nuevo y las columnas de "Reportes de visita"), este checkpoint, y `docs/ROADMAP.md`/`docs/ARCHITECTURE.md` (ver abajo). |
| **Verificación** | Solo `node --check` sobre una copia temporal del archivo (sintaxis válida, sin errores). **Nada probado en vivo todavía** — Jorge decidió probarlo después. El riesgo técnico principal sin verificar: si `Utilities.newBlob(html,'text/html').getAs('application/pdf')` genera un PDF legible (es una técnica conocida de Apps Script, pero nunca corrida en este proyecto) — a diferencia de la Fase 1, que solo reutilizaba mecanismos ya probados en producción. Pendiente, en orden: pegar y redesplegar en el proyecto de Apps Script real; correr `manObtenerHojaSolicitudes()` una vez para crear la hoja nueva; llenar una fila de prueba con un folio real; activar `manActivarModoPrueba(...)`; correr el menú nuevo y confirmar que el PDF adjunto se ve bien y el correo combinado llega correctamente; desactivar el modo de prueba. |
| **Commits** | Pendiente — `apps-script/mantenimiento.gs` modificado sin comitear (300 líneas agregadas), a la espera del mensaje de cierre de esta sesión. |

---

## CHECKPOINT — 2026-08-25 · Fase 1 construida y desplegada: coordinación de fecha de visita en Mantenimiento/Asesorías

| | |
|---|---|
| **Fecha** | 2026-08-25 |
| **Sesión** | Retomó directamente el plan pausado el 24 ago 2026 (`~/.claude/plans/vamos-a-revisar-el-wiggly-eclipse.md`) — construir la Fase 1 de la ruta hacia retirar v8.5: la coordinación de fecha de visita en Mantenimiento/Asesorías. Sesión de código, no solo planeación. |
| **`apps-script/mantenimiento.gs` (+142/-2 líneas) y `apps-script/asesorias.gs` (+145/-2 líneas)** | Mismo cambio en paralelo, prefijos `man`/`ase`: dos columnas nuevas al final de `ENCABEZADOS_MAN_SOLICITUDES`/`ENCABEZADOS_ASE_SOLICITUDES` ("Fecha programada de visita" + "Notificación de fecha programada enviada", `COL_MAN_FECHA_PROGRAMADA`/`COL_MAN_NOTIFICACION_PROGRAMADA` y sus equivalentes `ASE`), completadas solas por el auto-heal de encabezados ya existente; segundo trigger `onEdit` instalable e independiente del de cierre (`manOnEditProgramacion`/`aseOnEditProgramacion`, mismo esqueleto que `manOnEditCierre`/`aseOnEditCierre`, evita doble aviso con su propia columna de control); `manNotificarFechaProgramada()`/`aseNotificarFechaProgramada()` reutilizan el mismo patrón de correo combinado (`to`=solicitante, `cc`=Zona/Sector vía `manBuscarContactosZonaSector()`/`manFiltrarContactosPorTipo()` tal cual existen) que las notificaciones de apertura/cierre; `manInstalarTriggerProgramacion()`/`manDesinstalarTriggerProgramacion()` (y equivalentes `ase`) agregados al menú `onOpen()` de cada Sheet; `fechaProgramada` expuesto en `?action=consulta`/`?action=pendientes` de ambos backends. No se tocó `otde.html` — el cambio vive del lado de Jorge (columnas del Sheet + notificación), no de la captura del solicitante. |
| **Documentación** | `docs/ROADMAP.md` (ítems 8-9, +33/-14 líneas: ítem 8 pasa de "plan escrito, no iniciado" a "código construido, pendiente de desplegar"; ítem 9 lista la Fase 1 ya construida dentro de la ruta de 4 fases) y `docs/ARCHITECTURE.md` (+50 líneas: nueva subsección en §15 con el detalle de código de arriba, y el diagrama de §19.1 actualizado — nodos `L`/`M` sólidos para la captura/notificación de fecha, que ahora ocurre dentro del sitio; el nodo externo `I` se redujo a "visita técnica, atención, reporte técnico con PDF", lo único que sigue 100% en v8.5). No se tocó `CLAUDE.md` — ninguna convención de desarrollo cambió, y la entrada de `mantenimiento.gs`/`asesorias.gs` ahí ya resume "nuevo (ago 2026)" sin desglosar cada campo de columna. |
| **Verificación** | Revisión de código a mano (nombres de función/columna confirmados con `grep` contra ambos `.gs` antes de citarlos en la documentación), más despliegue real por Jorge y verificación en vivo. Bug real encontrado y resuelto en el camino: `manActivarModoPrueba('correo')` tronaba `Exception: Invalid argument: value` al correrla desde ▶️ Ejecutar directo — mismo problema ya documentado en `docs/QA-NOTES.md #14` (▶️ Ejecutar no pasa argumentos), resuelto envolviéndola en una función temporal sin parámetros. Las columnas nuevas no aparecían en el Sheet real hasta correr manualmente `manObtenerHojaSolicitudes()`/`aseObtenerHojaSolicitudes()` desde el editor (el auto-heal solo corre dentro de esa función, que antes solo se disparaba vía `doPost` real). Confirmado por Jorge en ambos proyectos: las 2 columnas nuevas aparecen en el Sheet, el trigger `manOnEditProgramacion`/`aseOnEditProgramacion` quedó registrado en la pestaña "Activadores" (no solo que la instalación no arrojó error), el correo de prueba se disparó una sola vez al capturar la fecha y no al editar otras columnas, y el modo de prueba se desactivó después. Confirmado además vía `curl -L` contra ambos endpoints reales en producción: `{"status":"ok","servicio":"..."}` sin `action`, y `{"status":"no_encontrado"}` con un folio inventado en `?action=consulta` — mismo mensaje genérico de siempre, sin errores, en los 2 backends. No se verificó `fechaProgramada` en una respuesta JSON real (no se compartió un folio+correo de prueba real fuera del Sheet). |
| **Commits** | `80305a6` (código: `apps-script/mantenimiento.gs`/`asesorias.gs`) y `094c5ec` (documentación: `docs/ROADMAP.md`/`ARCHITECTURE.md`/`BITACORA.md`) en `seprn-sitio` (`main`, local, no publicado). |

---

## CHECKPOINT — 2026-08-24 (cont. 3) · Revisión de flujo de Mantenimiento y decisión de retirar v8.5 en fases

| | |
|---|---|
| **Fecha** | 2026-08-24 |
| **Sesión** | Jorge pidió revisar el ciclo de vida completo de una solicitud de Mantenimiento (solicitante → OTDE coordina fecha de visita → visita → reporte técnico con PDF → cierre) y un plan para pulir/completar lo que falta. Sesión de auditoría + planeación — no se tocó código, solo documentación de la decisión y un plan de implementación guardado fuera del repo. |
| **Auditoría en vivo (código real, no memoria)** | Confirmó lo ya documentado en `ARCHITECTURE.md §15/§19.1`/`ROADMAP.md` ítems 8-9: la mitad del flujo (intake → folio → oficio → correo combinado → estatus → cierre por correo al marcar "Resuelto") está construida y en producción; la coordinación de fecha de visita (ítem 8) sigue sin construir; el reporte técnico + PDF + auto-cierre vive completo en el sistema aparte v8.5, sin ninguna integración automática con el webform nuevo. Se confirmó también que el patrón de fallback manual de CCT (autocomplete + Tipo de CCT/Sector/Zona/Escuela a mano si el CCT no está en el catálogo, sin bloquear el envío) es correcto para Mantenimiento tal cual está — Jorge no lo quiere más estricto. |
| **Decisión de arquitectura — se revierte el descarte de reemplazar v8.5 (ítem 9 de `ROADMAP.md`)** | La evaluación del 11 ago 2026 había descartado reconstruir v8.5 por "beneficio mayormente cosmético". Preguntado explícitamente qué cambió, Jorge confirmó que no es una necesidad puntual nueva de v8.5 — quiere unificar todo en un solo stack mantenible, porque v8.5 no tiene repo ni control de versiones. Se documenta como reversión deliberada, no drift silencioso. `docs/ROADMAP.md` ítem 9 reescrito con la ruta en 4 fases (detalle en ese archivo); ítem 8 (coordinación de fecha de visita) queda enmarcado como su Fase 1. |
| **Plan de implementación de la Fase 1, guardado y pausado** | Escrito en `~/.claude/plans/vamos-a-revisar-el-wiggly-eclipse.md` (fuera de este repo, no versionado en git) — Jorge pidió pausar antes de ejecutar ("por ahora no puedo continuar"), confirmando que es lo primero a retomar la próxima sesión. Diseño en breve: dos columnas nuevas al final de `Solicitudes` en `mantenimiento.gs`/`asesorias.gs` ("Fecha programada de visita" + su columna de control de notificación, mismo patrón de auto-heal ya usado), un segundo trigger `onEdit` instalable (`manOnEditProgramacion`/`aseOnEditProgramacion`, independiente del de cierre existente) que avisa a solicitante+Zona/Sector al capturar la fecha, y exponer el campo nuevo en `?action=consulta`/`?action=pendientes`. Sin cambios al formulario de `otde.html` — es trabajo del lado del Sheet/backend. |
| **Documentación** | `docs/ROADMAP.md` (ítems 8-9 reescritos, +30/-20 líneas) y este checkpoint. No se tocó `docs/ARCHITECTURE.md` — el diagrama de `§19.1` se actualiza cuando la Fase 1 se construya de verdad, no antes (evitar documentar un flujo que el código todavía no tiene). No se tocó `CLAUDE.md` — ninguna convención de desarrollo cambió. |
| **Verificación** | N/A — sesión de solo lectura/planeación, sin código que probar. El plan queda por verificarse en la sesión que lo ejecute (ver su propia sección de verificación). |
| **Commits** | Pendiente — solo `docs/ROADMAP.md` modificado en este repo, a la espera del mensaje de cierre de esta sesión. |

---

## CHECKPOINT — 2026-08-24 (cont.) · Panel único de solicitudes pendientes, dropdown protector en Correo, y auto-ID en Formación Docente

| | |
|---|---|
| **Fecha** | 2026-08-24 |
| **Sesión** | Continuación directa del checkpoint de arriba: entender el flujo completo llevó a repasarlo trámite por trámite buscando qué reducía la dispersión real de Jorge (tener que entrar a 4 Sheets distintos, pasos manuales fáciles de olvidar) rumbo a que el proceso "camine en automático". Se construyó, desplegó y probó en vivo — no se quedó en diagnóstico. |
| **Panel OTDE (`apps-script/panel-otde.gs`, nuevo, 218 líneas)** | Apps Script aparte, en un Google Sheet propio, que junta las solicitudes abiertas de los 4 trámites con folio en una sola hoja ("Pendientes"), ordenadas por días de espera, coloreadas con los mismos hex de badge que `oficina-virtual.html`. Columna "Días" en rojo/negrita a partir de `PANEL_UMBRAL_DIAS_ALERTA` (3 por default) sin moverse. Menú "Panel OTDE": actualizar a mano o instalar refresco automático cada 30 min. Detalle completo en `docs/ARCHITECTURE.md §20`. |
| **`?action=pendientes&token=...` nuevo en los 4 backends con folio** | `mantenimiento.gs` (+53 líneas), `asesorias.gs` (+52), `soporte-remoto.gs` (+54) en este repo, y `WebApp.gs` (+70 líneas) en `Correos-institucionales/webform-2026-2027/` (repo aparte, sin git — respaldado antes de tocarlo: `webform-2026-2027.backup-20260824-142025/`). Hermano de `?action=consulta` pero expone **todas** las solicitudes abiertas de golpe (nombre/escuela/correo), no una sola por folio+correo conocidos — por eso, a diferencia de `consulta`, exige un token (`PANEL_TOKEN`, mismo secreto en los 5 proyectos). Criterio de "abierta": Mantenimiento/Asesorías/Soporte = Estatus ≠ Resuelto/Rechazado; Correo = `Estado general` todavía en `'Solicitud recibida'` (mismo criterio que ya usaba `resumenSemanal()`). |
| **Dropdown protector en "Estado general" de Correo** | `Alta.gs`/`CambioContrasena.gs`/`Reset2FA.gs`/`Incidencias.gs` (+16/+12/+12/+12 líneas). Hallazgo al leer el código real antes de proponer nada: esa columna, aunque técnicamente texto libre, en la práctica solo tenía 2 valores por tipo, escritos siempre por `xRevisarEdicion()`, nunca a mano — el cierre automático de Correo ya existía y no estaba documentado así en `CLAUDE.md`. El dropdown (`requireValueInList`, reaplicado en cada `doPost`) solo protege contra un typo manual, sin cambiar ninguna lógica. |
| **Auto-generación de ID de curso en Formación Docente (`formacion-docente.gs`, +57 líneas)** | "Generar ID de cursos faltantes" era acción de menú manual — si Jorge daba de alta un curso y olvidaba correrla, salía en el catálogo sin `ID_Curso`. `onEditCursos()` (instalable, mismo patrón que `manOnEditCierre`) ahora lo genera solo al escribir la `Categoria`; lógica compartida con el botón del menú vía `generarIdsCursosFaltantes_()`, sin duplicarla. |
| **Bug real cazado al desplegar — Apps Script's ▶️ Ejecutar no pasa argumentos** | Jorge corrió `manConfigurarTokenPanel` seleccionándola directo en el editor → `Exception: Invalid argument: value` (llegó `undefined`). Pasó en los 4 backends, así que ninguno tenía el token real guardado — el Panel fallaba después con `no_autorizado` en los 4 a la vez, un síntoma que parecía "el secreto no coincide" pero la causa real era que nunca se había guardado ninguno. Fix: envolver la llamada en una función temporal sin parámetros y ejecutar esa. Documentado en `docs/QA-NOTES.md #14` para no repetirlo con `activarModoPrueba`/similares. |
| **Documentación** | `docs/ARCHITECTURE.md §20` (nuevo), `CLAUDE.md` (notas en las 4 secciones de backend + sección nueva `panel-otde.gs`), `README.md` (línea de backends + árbol de archivos), `docs/QA-NOTES.md #14`, y `Correos-institucionales/CLAUDE.md` (repo aparte, mismo criterio de "solo lo que cambió"). No se tocó `docs/ROADMAP.md` — el pendiente de integración con v8.5 (opciones a/b/c) ya estaba documentado ahí desde el 11 ago 2026 y sigue vigente tal cual, no cambió. |
| **Verificación** | Los 4 endpoints probados en vivo con `curl` antes y después del despliegue de Jorge: `?action=consulta` sin cambios (`no_encontrado`) en los 4; `?action=pendientes` pasó de mensaje genérico a `{"status":"no_autorizado"}` sin token/con token incorrecto en los 4, confirmando el código nuevo desplegado sin romper nada existente. La ruta del token correcto no se probó desde aquí a propósito (no se pidió ni se compartió el secreto) — Jorge confirmó en vivo que el Panel ya trae datos reales: "1 solicitud(es) llevan 3+ días esperando" en el aviso de la hoja, sin línea de errores. Dropdown de Correo y auto-ID de Formación Docente quedaron pendientes de confirmación visual por Jorge al momento de este checkpoint. |
| **Commits** | Pendiente en `seprn-sitio` — 9 archivos modificados/nuevos sin comitear (`git diff --stat`: 508 líneas agregadas, 5 eliminadas), a la espera del mensaje de cierre de esta sesión. `Correos-institucionales/webform-2026-2027/` no tiene git — los 5 archivos tocados ya están pegados y redesplegados en el editor real (confirmado por Jorge), con el respaldo pre-edición conservado en `webform-2026-2027.backup-20260824-142025/`. |

---

## CHECKPOINT — 2026-08-24 · Esquema visual del flujo de trámites de la Oficina Virtual (5 diagramas Mermaid en `ARCHITECTURE.md` + Artifact interactivo)

| | |
|---|---|
| **Fecha** | 2026-08-24 |
| **Sesión** | Jorge dijo que la Oficina Virtual lo satura — se pierde el hilo de cómo opera el flujo completo de una solicitud, desde que se pide hasta que se cierra, porque la información está repartida entre `ARCHITECTURE.md` (§11/§12/§15/§16), `CLAUDE.md` y varios checkpoints de esta bitácora. Pidió algo visual, un diagrama por trámite, no uno unificado. |
| **`docs/ARCHITECTURE.md §19` (nuevo, 162 líneas)** | Un diagrama por trámite (Mantenimiento, Asesorías, Soporte, Correo Institucional, Centro de Formación Docente), con `flowchart` en Mermaid y, donde aplica, el ciclo de 5 estados. Convención compartida en los 5: nodos con borde sólido ocurren dentro del sitio, nodos punteados ocurren fuera de él (promoción manual, sistema externo) — es la representación visual del hallazgo central: en Mantenimiento/Asesorías/Correo el webform solo abre la solicitud, el resto del ciclo real pasa fuera del sitio sin integración automática. No se tocaron los diagramas ASCII existentes de §8/§12 (otro nivel de detalle, se referencian con "Ver también" en vez de duplicarse). |
| **Hueco cerrado en la documentación — Soporte Técnico Remoto sin sistema externo** | Ningún documento decía explícitamente si Soporte se promovía a un sistema aparte como Mantenimiento (v8.5) o Asesorías (Excel). Confirmado con Jorge: no — la atención vía TeamViewer y el cierre del ticket ocurren completos dentro del webform/Sheet. Queda registrado por primera vez en `§19.3`. |
| **Artifact interactivo publicado** | Página HTML autocontenida (Montserrat/Public Sans/IBM Plex Mono vía Google Fonts, paleta institucional guinda/arena/caramelo/midnight, sin Mermaid.js — los 5 diagramas de flujo son SVG hand-authored con la misma convención sólido/punteado) con tabs por trámite: Resumen (decodificador de folio por prefijo, 3 tarjetas de hallazgo, tabla comparativa de los 5 trámites) + un panel por trámite (datos rápidos, diagrama de flujo, stepper de 5 estados o caja de "texto libre" para Correo, callout de "qué pasa fuera del sitio", CTA a `oficina-virtual.html#buscar-folio`). Colores de badge de estatus (`b-pendiente`/`b-atencion`/`b-resuelto`/`b-rechazado`) tomados literales de `oficina-virtual.html` para que se lean como el mismo sistema. No es un archivo de este repo — los Artifacts no se versionan en git, se linkeó directo a Jorge. |
| **Documentación** | Solo `docs/ARCHITECTURE.md` (arriba). No se tocó `CLAUDE.md`/`docs/ROADMAP.md`/`README.md` — ninguna convención de desarrollo ni pendiente cambió, es documentación complementaria de flujos ya descritos. |
| **Verificación** | Los 5 `flowchart`/`stateDiagram-v2` de `§19` no se re-renderizaron localmente (sin visor Mermaid en este entorno) — sintaxis revisada a mano contra el estilo ya usado en Mermaid estándar; se verá renderizado la próxima vez que se abra este archivo en GitHub. El Artifact sí se abrió en vivo (Chrome vía `claude-in-chrome`): navegación por tabs, decodificador de folio, selector de sub-tipo de Correo, y los 5 diagramas SVG (incluida la rama condicional de Formación Docente) confirmados legibles y con las flechas/etiquetas en el lugar correcto. |
| **Commits** | Pendiente — `docs/ARCHITECTURE.md` modificado sin comitear (162 líneas agregadas), a la espera de que Jorge confirme el mensaje de cierre de esta sesión. |

---

## CHECKPOINT — 2026-08-18 · Smoke test de la Oficina Virtual, fix crítico de modo de prueba, escapado HTML/Markdown en los 7 correos automatizados, y paridad operativa de Correo con el sistema viejo

| | |
|---|---|
| **Fecha** | 2026-08-18 |
| **Sesión** | Jorge pidió un smoke test completo de la Oficina Virtual OTDE (hub + 4 trámites) antes de confiarla a producción, con foco en webforms, notificaciones automatizadas y consistencia de correos. Al cierre, pidió discutir y resolver la paridad operativa de Correo Institucional (`Correos-institucionales/webform-2026-2027/`, repo aparte sin git) frente al sistema viejo del Google Form. |
| **Hallazgo crítico y fix inmediato — `MODO_PRUEBA_CORREO` seguía activo** | Activado el 11 ago 2026 para pruebas, nunca desactivado: todo correo real de "solicitud recibida"/"cierre" de Mantenimiento y Asesorías se redirigía en silencio al correo de prueba de Jorge, sin que ni el solicitante ni Zona/Sector recibieran nada. Confirmado en vivo (Script Properties de ambos proyectos) y desactivado corriendo `manDesactivarModoPrueba()`/`aseDesactivarModoPrueba()` desde el editor de Apps Script — verificado que la property ya no existe en ninguno de los dos. |
| **Triggers de cierre verificados** | `manOnEditCierre`/`aseOnEditCierre`/`sopOnEditCierre` confirmados como activadores realmente registrados (pestaña "Activadores" de los 3 proyectos, no solo "corrió sin error") — sin repetición del incidente de Soporte del 10 ago 2026 (`docs/BITACORA.md`, checkpoint de esa fecha). |
| **Escapado HTML/Markdown en los 7 tipos de correo automatizado (seguridad)** | Ninguno escapaba los campos de usuario (nombre, escuela, equipos, observaciones, etc.) antes de insertarlos en el HTML del correo o el Markdown de Telegram — endpoints públicos, vector de phishing con membrete institucional real. `escapeHtml_()`/`escapeMarkdown_()` (prefijo `man`/`ase`/`sop` en `seprn-sitio/apps-script/`, sin prefijo en `Config.gs` de Correo) agregados en los 3 backends de trámite (84 líneas, commit `459bf9f`) y en los 5 archivos de Correo (`Config.gs`, `Alta.gs`, `CambioContrasena.gs`, `Reset2FA.gs`, `Incidencias.gs`). Pegado y redesplegado en los 4 proyectos de Apps Script en vivo el mismo día: Mantenimiento v10, Asesorías v9, Soporte v6, Webform Correo v5 — mismos IDs de implementación de siempre. |
| **Paridad operativa de Correo con el sistema viejo (Fase 3)** | Revisado `Code.gs`/`OnFormSubmit.gs`/`ResumenSemanal.gs`/`GenerarResumenSIGEE.gs` del sistema viejo contra el nuevo. Decisiones con Jorge: **(1)** no agregar correo de respaldo a Marcos (`CONFIG.correoResponsable` en `Config.gs` se queda sin usar, a propósito — el equipo ya revisa solicitudes a diario, un canal extra solo generaría ruido); **(2)** acotar Telegram en Correo a solo `Incidencias.gs` (el único caso realmente urgente) — se quitó `notificarTelegram(...)` de `CambioContrasena.gs` y `Reset2FA.gs`, Alta nunca lo tuvo; **(3)** portar `GenerarResumenSIGEE.gs` y `ResumenSemanal.gs` a `webform-2026-2027/`, adaptados a la hoja "Alta" consolidada y a los estados reales del sistema nuevo (`Solicitud recibida` → `Cuenta entregada`/`Reset notificado`/`Incidencia resuelta`) — ninguno de los dos era portable directo, el sistema viejo usaba 6 hojas con nombres/encabezados distintos. |
| **Hallazgo adicional — Telegram nunca había estado configurado en 3 de 4 proyectos** | Al revisar Script Properties para la decisión de arriba, se encontró que Webform Correo, Mantenimiento y Asesorías nunca tuvieron `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` puestas desde su despliegue (6 ago 2026) — el `if (!token || !chatId) return;` los volvía no-op silencioso, no un error visible. Solo Soporte los tenía. Configurados hoy en los 3 proyectos con el mismo bot/chat que ya usaba Soporte. |
| **`GenerarResumenSIGEE.gs` (nuevo, 108 líneas) y `ResumenSemanal.gs` (nuevo, 163 líneas)** | Agregados a `webform-2026-2027/`. El resumen SIGEE ya no necesita alias de encabezado por campo (a diferencia del original, que los necesitaba por los encabezados libres del Google Form) — el webform nuevo tiene nombres de columna fijos, se resuelven con `indexOfHeader()`. Trigger `resumenSemanal` (lunes 9am) instalado corriendo `crearTriggerResumenSemanal()` una vez desde el editor — confirmado en "Activadores" junto con `onEditWebform`, sin errores. Redesplegado como Webform Correo v6 (mismo ID de implementación). |
| **Documentación** | `CLAUDE.md` — la sección "Correo Institucional" describía el alcance de Telegram como estaba antes de hoy (3 tipos, uno condicionado por dominio) y decía explícitamente no configurar las Script Properties sin confirmar con Jorge; se actualizó para reflejar el alcance nuevo (solo Incidencias) y que Telegram ya está configurado. `Correos-institucionales/CLAUDE.md` (repo aparte) recibió el mismo ajuste, más la mención de los 2 archivos nuevos. No se tocó `docs/ARCHITECTURE.md`/`docs/ROADMAP.md`/`README.md` — nada de esto cambió la arquitectura ya descrita en `ARCHITECTURE.md §16` ni resolvió/agregó un pendiente que `ROADMAP.md` ya rastreara. |
| **Verificación** | Cada archivo pegado en un editor de Apps Script en vivo se verificó con hash SHA-256 exacto contra el archivo local antes de guardar (incluida una recarga completa de página para confirmar que quedó en Drive, no solo en el buffer del editor) — coincidencia confirmada en los 7 archivos de esta sesión. `node --check` sobre los 2 archivos nuevos y los 2 editados de Correo, sin errores de sintaxis. Un intento de pegar Alta.gs aterrizó por error en `CambioContrasena.gs` (el sidebar de Apps Script había cambiado de tamaño entre capturas) — detectado por hash antes de guardar, descartado recargando la página sin consecuencias. |
| **Commits** | `459bf9f` en `seprn-sitio` (`main`, local, no publicado — ver Pendientes). `Correos-institucionales/webform-2026-2027/` no tiene git; los 7 archivos tocados (5 editados + 2 nuevos) quedaron pegados y redesplegados en el editor real, sin rastro de "pendiente de commit" posible en ese repo. |

---

## CHECKPOINT — 2026-08-16 · Buscador de folio reordenado en Oficina Virtual, de cara a la presentación del 17 ago a supervisores y jefes de sector

| | |
|---|---|
| **Fecha** | 2026-08-16 |
| **Sesión** | Jorge presenta la Oficina Virtual (aún no en producción) el 17 ago 2026 a supervisores y jefes de sector — primer avance interno, no lanzamiento. Pidió mover el buscador de seguimiento de folio para que aparezca después de las tarjetas de servicios, no antes: prioriza la acción principal (elegir un trámite) sobre el caso secundario (consultar un folio que ya existe), más lógico para quien ve la página por primera vez. |
| **`oficina-virtual.html`** | Se reordenó `#buscar-folio` (bloque `.ov-buscador`) para que quede después de `.ov-grid` (las 6 tarjetas de servicio) en vez de antes, dentro de `#main-content`. Corte-y-pega puro — mismos ids, mismo JS, sin cambios de contenido. Se verificó antes de mover que ningún JS del sitio (`script.js` ni el `<script>` inline de la página) selecciona por posición/`nth-child` — todo es por `id`, así que el reorden es seguro. El único ajuste real fue de espaciado: `margin-bottom: 70px` → `margin-top: 60px` en `.ov-buscador`, para separarlo del grid que ahora lo precede (el `margin: 80px auto` de `.content-section` ya daba de sobra el espacio hacia el footer). |
| **Documentación** | `CLAUDE.md` — la línea que describía el buscador como "hero del hub" ya no era cierta; se actualizó para reflejar que ahora vive debajo de la grid, con la fecha y el motivo del cambio. No se tocó `docs/ARCHITECTURE.md`/`docs/ROADMAP.md`/`README.md` — es un reorden de UI dentro de una página ya documentada, sin flujo técnico nuevo ni pendiente resuelto. |
| **Guion de presentación (fuera de este repo)** | A petición de Jorge, se redactó un guion de presentación imprimible (discurso breve sobre la importancia de la Oficina Virtual, simplificación administrativa e impacto en calidad educativa) publicado como Claude Artifact — no es un archivo de este repo, se menciona aquí solo como contexto de por qué se hizo la sesión. |
| **Verificación** | Confirmado contra `localhost:8000` (servidor local ya corría de la sesión anterior): `curl` al HTML servido muestra el orden `section-header` → `ov-grid` → `ov-buscador#buscar-folio`, y los 4 links "Consultar estatus →" de las tarjetas siguen apuntando a `#buscar-folio` sin cambios. |
| **Commits** | Pendiente. |

---

## CHECKPOINT — 2026-08-13 · Página de Protocolos de Actuación (Acoso Escolar, Mochila de Paz, Abuso Sexual Infantil), enlazada desde Jurídico/Académica/OEVE

| | |
|---|---|
| **Fecha** | 2026-08-13 |
| **Sesión** | Jorge necesitaba publicar 3 PDFs de protocolos de actuación para que quedaran disponibles en línea y para descarga. El planteamiento inicial (uno por oficina: Jurídico/Académica/OEVE, cada una con "su" protocolo) resultó incorrecto — Jorge aclaró a medio camino que son 3 protocolos oficiales del **Gobierno del Estado de México/SEIEM** (no producidos por SEPRN) que aplican transversalmente, y que las 3 oficinas dan acceso a los mismos 3 documentos, sin mapeo 1:1. La página y los 3 banners ya construidos con el modelo equivocado se reescribieron con el modelo correcto antes de continuar. |
| **`protocolos.html` (nuevo)** | Hub con los 3 protocolos: Erradicación del Acoso Escolar, Mochila de Paz y Prevención, y Prevención/Detección/Actuación en Abuso Sexual Infantil-Acoso-Maltrato. Sigue el esqueleto estándar de página de área (`CLAUDE.md`). No está en el nav principal ni en el sitemap del footer — mismo patrón de aislamiento que `oficina-virtual.html` (tampoco listada ahí), se llega solo por un botón CTA. Cada tarjeta tiene botón **Ver** (`target="_blank"`, sin `download` — primer link "Ver" del sitio, todo lo demás fuerza descarga) y **Descargar**. Detalle completo en `CLAUDE.md`. |
| **PDFs** | `pdfs/protocolos/` (planos, sin subcarpeta por área — convención nueva, ~7.1 MB los 3 juntos): `Protocolo-Erradicacion-Acoso.pdf`, `Protocolo-Mochila-Paz.pdf`, `Protocolo-Prevencion-Deteccion-Actuacion-Abuso-Sex.pdf`, entregados directamente por Jorge. |
| **Banners CTA** | `juridico.html`, `academica.html`, `oeve.html` — mismo texto idéntico en las 3 (ya que no hay mapeo protocolo↔oficina), estilo `.protocolo-banner` adaptado del `.otde-banner` de `otde.html`, todos apuntan a `protocolos.html` sin ancla. |
| **Enlaces "Más información" verificados, no asumidos** | Jorge compartió 2 ligas oficiales externas sin decir a cuál protocolo correspondía cada una. Se verificó en vez de adivinar: el PDF de `conebi.edomex.gob.mx/.../dic181e.pdf` resultó ser, por hash SHA-256, el mismo archivo exacto que `Protocolo-Erradicacion-Acoso.pdf`; el contenido de `seiem.edu.mx/web/protocolo_derechos` (extraído por `curl`, `WebFetch` falló con ECONNRESET) coincide en título literal con el protocolo de Abuso Sexual Infantil. Ambos enlaces agregados a su tarjeta correspondiente; Mochila de Paz se quedó sin liga externa porque Jorge no compartió una. |
| **Diseño: portadas de PDF descartadas, íconos temáticos en su lugar** | Jorge preguntó por poner la portada de cada PDF como miniatura ("los maestros son muy visuales"). Se generó una vista previa real de la página 1 de cada PDF (`pdftoppm`, herramienta ya disponible en el sistema) antes de opinar: solo Mochila de Paz tiene una portada ilustrada real; las otras dos son páginas de Gaceta de Gobierno sin diseño (una de puro texto legal, la otra la portada genérica del Periódico Oficial completo). Con eso como evidencia, se descartó la idea de miniaturas de PDF (inconsistentes entre sí) y en su lugar se le dio a cada tarjeta un ícono de línea temático propio (escudo con check / mochila / corazón — mismo lenguaje visual que `.area-icon` de `areas.html`), badge circular 64px, y un acento de borde superior guinda en las 3 tarjetas por igual. |
| **Documentación** | `CLAUDE.md` (tabla de páginas + sección nueva `protocolos.html` con el patrón completo, regla 4 de convención de carpeta `pdfs/`). No se tocó `docs/ARCHITECTURE.md`/`docs/ROADMAP.md`/`README.md` — nada de esta sesión los volvió obsoletos (`protocolos.html` no introduce un flujo técnico/backend nuevo más allá de lo ya descrito en `CLAUDE.md`, y ningún pendiente de `ROADMAP.md` mencionaba esto; `README.md` tampoco lista `oficina-virtual.html`, mismo precedente de página solo-por-CTA). |
| **Verificación** | Probado en vivo contra `localhost:8000` (servidor ya corría de una sesión previa): los 3 banners, el scroll a `protocolos.html`, el link "Ver" abriendo el PDF real en pestaña nueva (confirmado con `tabs_context_mcp` que la URL servida es la del PDF, no una descarga), y el layout en viewport móvil (390px). |
| **Commits** | `fe476ac` en `main` local. Publicado a `origin/main` como `f6562f6` — junto en el mismo push con todo lo demás pendiente de publicar (webforms de Correo/Mantenimiento/Asesorías, Oficina Virtual OTDE), excluyendo `otde.html` a propósito (ver checkpoint de abajo, mismo día, para el detalle del mecanismo de publicación). |

---

## CHECKPOINT — 2026-08-13 (cont.) · Código QR de marca para Protocolos de Actuación, y mecanismo de publicación a producción excluyendo otde.html

| | |
|---|---|
| **Fecha** | 2026-08-13 |
| **Sesión** | Continuación de la sesión de Protocolos de Actuación. Jorge pidió (1) publicar a producción todo lo pendiente **menos** `otde.html` (trabajo de OTDE deliberadamente retenido desde el 6 ago 2026), y (2) generar un código QR con diseño propio del sitio que lleve a `protocolos.html`, para poder compartirlo por WhatsApp. |
| **Publicación a producción, mecanismo (reutilizado 2 veces esta sesión)** | Rama `publish-protocolos` creada desde `origin/main`, `git checkout main -- .` para traer todo el contenido de la rama local completa, luego `git checkout origin/main -- otde.html` para revertir solo ese archivo a la versión ya publicada — mismo mecanismo que el cierre del 10 ago 2026. Confirmado en ambas corridas que `git diff origin/main -- otde.html` da 0 líneas antes de comitear. Como el alcance de "lo pendiente de OTDE" era ambiguo (¿solo `otde.html`, o también `oficina-virtual.html` y `apps-script/*.gs`?), se le presentó la disyuntiva a Jorge en vez de asumir — eligió excluir solo `otde.html`. Efecto secundario aceptado a propósito: `oficina-virtual.html` (que sí se publicó) enlaza a `otde.html#asesorias`, pestaña que no existe en la versión de producción de `otde.html` — el link no rompe nada, simplemente no salta a ninguna pestaña; las demás pestañas referenciadas (`#mantenimiento`/`#correo`/`#soporte`) sí existen en producción, con su contenido antiguo. El buscador de estatus de folio de `oficina-virtual.html` sí funciona completo porque los 3 backends (`mantenimiento.gs`/`asesorias.gs`/`soporte-remoto.gs`) ya estaban desplegados en Apps Script en vivo desde el 10 ago 2026, independientemente de git. |
| **Primer push (`f6562f6`)** | Contenido del commit local `fe476ac` (página de Protocolos) más los 13 commits de OTDE ya acumulados sin publicar desde el 6 ago (webforms de Correo/Mantenimiento/Asesorías, sus backends, cierre automático, Oficina Virtual) — todo menos `otde.html`. Un solo commit de publicación en la rama `publish-protocolos`, fast-forward a `origin/main` sin conflictos. |
| **`images/qr-protocolos.png` (nuevo)** | Generado localmente con Python (`qrcode` + Pillow, instalados vía `pip3 install --user`, sin agregar dependencia al repo — no hay `requirements.txt`), no con un servicio externo de terceros. Corrección de errores alta (`ERROR_CORRECT_H`) para poder superponer el logomark NE/ZA (mismo patrón que `favicon.svg`/`logo.svg`: rect redondeado guinda, texto "NE"/"ZA" en arena) al centro sin perder legibilidad — verificado decodificando la imagen con `cv2.QRCodeDetector()` (OpenCV, mismo mecanismo de instalación) antes de darla por buena: confirma la URL exacta `https://educaneza.github.io/seprn-sitio/protocolos.html`. Módulos redondeados en guinda sobre blanco, tarjeta con el mismo lenguaje visual que `.protocolo-card` (acento superior, borde `#d6d1ca`, esquinas redondeadas), eyebrow/título/subtítulo/URL de respaldo ya incluidos dentro de la imagen misma. |
| **Sección "Código QR" en `protocolos.html`** | Nueva `.seccion` al final, imagen centrada + botón "Descargar código QR" (`download`, reusa `.download-button` ya definido en la página). Texto sugiere compartir por WhatsApp — recomendado enviarlo como documento, no como foto, para que WhatsApp no lo recomprima y arriesgue la lectura del QR. |
| **Documentación** | `CLAUDE.md` (sección `protocolos.html`, nueva entrada "Código QR"). No se tocó `docs/ARCHITECTURE.md`/`docs/ROADMAP.md`/`README.md` — el QR es una imagen estática con un patrón ya documentado por completo en `CLAUDE.md`, sin flujo técnico/backend nuevo que amerite `ARCHITECTURE.md`, y ningún pendiente de `ROADMAP.md` relacionado. |
| **Verificación** | Decodificación programática del QR final contra la URL esperada (arriba). Probado visualmente en `localhost:8000`: la sección se ve integrada en `protocolos.html`, la imagen carga y el botón de descarga funciona. |
| **Segundo push (`3a41d16`)** | Commit local `549f6cf` (código QR) publicado con el mismo mecanismo — rama `publish-protocolos` recreada desde el `origin/main` ya actualizado por el primer push, `otde.html` excluido de nuevo, fast-forward sin conflictos. |
| **Commits** | `549f6cf` en `main` local; publicado a `origin/main` como `3a41d16`. |

---

## CHECKPOINT — 2026-08-11 · Correo combinado a solicitante + Zona + Sector en Mantenimiento/Asesorías, modo de prueba, y hoja de ruta del flujo completo

| | |
|---|---|
| **Fecha** | 2026-08-11 |
| **Sesión** | Jorge probó el webform de Mantenimiento en producción y encontró dos huecos: el solicitante nunca recibía confirmación de que su solicitud llegó, y solo se avisaba a Zona *o* Sector, nunca ambos — la búsqueda de contacto se detenía en la primera coincidencia. Compartió también el sistema viejo v8.5 ("Sistema Automatizado de Reportes de Visitas") como contexto del flujo real completo (coordinación de fecha con Sector→Zona→escuela, reporte del técnico por visita, PDF automático, auto-cierre) para pedir una valoración de qué aplicaría al webform nuevo. Se acordó con Jorge, vía preguntas directas, el alcance: arreglar el correo en Mantenimiento y Asesorías (mismo código, mismo hueco); no tocar Soporte Remoto (su falta de Zona/Sector es decisión deliberada, no un descuido); documentar la "coordinación de fecha" y v8.5 como roadmap, sin construir nada de eso hoy. |
| **Correo combinado (`mantenimiento.gs`/`asesorias.gs`)** | `manBuscarContactoZonaSector()`/`aseBuscarContactoZonaSector()` → `manBuscarContactosZonaSector()`/`aseBuscarContactosZonaSector()` (plural): ahora devuelve un array con la fila de Zona **y** la de Sector si ambas existen (antes se detenía en la primera). Apertura: `manNotificarZonaSector()`/`aseNotificarZonaSector()` (que solo avisaba a Zona/Sector) se reemplazó por `manNotificarSolicitudRecibida()`/`aseNotificarSolicitudRecibida()`, que manda un solo correo con `to`=solicitante (siempre, Correo ya es obligatorio) y `cc`=Zona+Sector. Cierre: las 2 funciones sueltas (`manNotificarCierreSolicitante`+`manNotificarCierreZonaSector`, y sus pares `ase`) se consolidaron en `manNotificarCierre()`/`aseNotificarCierre()`, mismo patrón `to`/`cc`, un solo envío en vez de 2. Detalle completo en `docs/ARCHITECTURE.md §15`. |
| **Modo de prueba (los 4 backends con webform: Mantenimiento, Asesorías, Soporte Remoto, Formación Docente)** | Arranque de la sesión: Jorge pidió poder probar el flujo completo de los webforms sin disparar correos reales a escuelas/zonas/sectores. `manEnviarCorreo_()`/`aseEnviarCorreo_()`/`sopEnviarCorreo_()` son wrappers de `MailApp.sendEmail()` que ahora usan todos los envíos de sus respectivos backends; en `formacion-docente.gs` (que ya mandaba en lote/BCC vía `enviarCorreoLote()`) se ajustó esa misma función en vez de agregar un wrapper nuevo. Si la Script Property `MODO_PRUEBA_CORREO` está definida (`<prefijo>ActivarModoPrueba('correo')` desde el editor, `<prefijo>DesactivarModoPrueba()` para apagar — prefijos `man`/`ase`/`sop`/`fd`), todo correo se redirige a ese destino con `[PRUEBA]` en el asunto y una nota del destino real. No requiere redeploy (Script Property, se lee en cada envío). **Solo `mantenimiento.gs` y `asesorias.gs` se pegaron y redesplegaron en sus proyectos de Apps Script en vivo esta sesión** (con modo de prueba activado y probado) — los cambios de `soporte-remoto.gs` y `formacion-docente.gs` quedaron listos en este repo pero **no se pegaron ni desplegaron** en sus editores; su modo de prueba no está disponible en producción todavía. |
| **Probado en vivo** | Con modo de prueba activo, se mandó una solicitud real por `curl` a cada endpoint desplegado (Sector I/Zona 1, que tiene fila de Zona *y* fila de Sector — `fiz0042v@dee.edu.mx` y `fjs0015d@dee.edu.mx`) y se marcó la fila como `Resuelto` en el Sheet — confirmado en el registro de ejecuciones que `doPost` y `manOnEditCierre`/`aseOnEditCierre` corrieron sin error. Filas y PDFs de prueba (Sheet + Drive) se limpiaron después; no se tocaron las filas reales de prueba que Jorge ya tenía en ambos Sheets. |
| **Redesplegado** | `mantenimiento.gs` versión 8, `asesorias.gs` versión 7 — mismos IDs de implementación de siempre. Modo de prueba quedó **activo** en ambos (destino: correo de Jorge) para que siga probando desde el sitio real antes de confirmar producción. |
| **Ajuste el mismo día — CC según jerarquía del solicitante** | Jorge cayó en cuenta de que faltaba distinguir *quién* solicita, no solo el Sector/Zona reportados: si solicita una escuela, CC a Zona y Sector; si solicita la propia Zona, CC solo a Sector; si solicita el propio Sector, CC a nadie (no hay a quién notificar arriba). `js/cct-db.js` ya traía ese dato (`tipo`: `escuela`/`supervision`/`jefatura`/`subdireccion`, con CCT propio para cada Zona y Sector) pero solo se usaba client-side para repoblar Función; nunca viajaba al backend. Se agregó `tipoCct` al payload de ambos formularios, una columna nueva `Tipo de solicitante` en ambos Sheets, y una función `manFiltrarContactosPorTipo()`/`aseFiltrarContactosPorTipo()` que filtra el `cc` según ese tipo (vacío/desconocido se trata como escuela — decisión explícita de Jorge, cubre las solicitudes ya guardadas sin este dato). Verificado corriendo la función directo en el editor contra Sector I/Zona 1 (que tiene ambos contactos reales). Redesplegado: `mantenimiento.gs` versión 9, `asesorias.gs` versión 8. |
| **Modo de prueba también en `Correos-institucionales/webform-2026-2027/` (repo aparte, sin control de versiones — no se puede confirmar por `git diff`)** | Mismo mecanismo: `Config.gs` ganó `activarModoPruebaCorreo('correo')`/`desactivarModoPruebaCorreo()`/`enviarCorreoConModoPrueba_()`, y los 9 `MailApp.sendEmail()` de `Alta.gs`/`CambioContrasena.gs`/`Reset2FA.gs`/`Incidencias.gs` ahora pasan por ese wrapper (verificado por grep). Igual que Soporte Remoto y Formación Docente: código listo en el archivo local, **no se pegó ni redesplegó** en su proyecto de Apps Script real esta sesión. |
| **Roadmap documentado, no construido** | (1) Coordinación de fecha de visita: v8.5 ya resuelve esto con su hoja "Despacho" (acuse de recibo + notificar fecha al sector), pero corre desconectada del webform nuevo — sugerido un paso equivalente dentro de `mantenimiento.gs`/`asesorias.gs` para una sesión futura. (2) v8.5 (reporte del técnico, PDF, auto-cierre, dashboard, reporte mensual) sigue fuera de alcance por decisión reafirmada esta sesión — 3 opciones a futuro documentadas en `docs/ROADMAP.md` (statu quo / puente de datos sin tocar v8.5 / reemplazo eventual, no recomendado). |
| **Commits** | Pendiente. |

---

## CHECKPOINT — 2026-08-10 (noche, cont.) · Banner duplicado quitado + destino decidido para migrar trámites a páginas propias

| | |
|---|---|
| **Fecha** | 2026-08-10 |
| **Sesión** | Con la Oficina Virtual ya en producción, Jorge pidió dejar en `otde.html` solo el banner de Oficina Virtual (el de Centro de Formación Docente quedó redundante — ya es una card del hub). De paso preguntó dos cosas sobre el rumbo más grande del sitio, resueltas como decisión para una sesión futura, no como trabajo de hoy. |
| **Banner quitado** | `otde.html` — eliminado el bloque `<div class="otde-banner">` de Centro de Formación Docente (gradiente midnight, líneas ~546-561). Queda solo el banner guinda de Oficina Virtual, que ya menciona Formación Docente en su copy. `formacion-docente.html`/`instructivo-formacion-docente.html` no se tocaron — siguen alcanzables desde la card correspondiente en `oficina-virtual.html`. |
| **Decisión de arquitectura — destino de los 4 trámites, si algún día salen de `otde.html`** | Jorge preguntó dónde vivirían los formularios de Correo/Mantenimiento/Asesorías/Soporte si se les quita la tab de `otde.html` (hoy siguen ahí, esto NO se ejecutó esta sesión). Se investigó el `<script>` de `otde.html` (~2,300 líneas) antes de opinar: las funciones de cada trámite ya están agrupadas en bloques contiguos por prefijo (`sop*`/`man*`/`ase*`/`alt*·cam*·rst*·inc*`), con solo 4 helpers compartidos (`toggleForm`, `toTitleCase`, `fetchJsonConTimeout`, `otdePoblarFuncion`) — mover un trámite no sería una extracción enredada. Decisión: cada trámite se volvería su propia página (`mantenimiento.html`, `asesorias.html`, `correo.html`, `soporte.html`), mismo precedente que `formacion-docente.html`/`asistencia.html` — explícitamente **no** moverlos a `oficina-virtual.html` como tabs, porque eso solo trasladaría el problema de "archivo gigante" de un archivo a otro. |
| **Decisión de arquitectura — Licencias Office sí entra a la grid de Oficina Virtual** | Confirmado que la tab "Licencias Office" de `otde.html` es 100% estática (guía + link de descarga, cero JS/backend propio) — no es un trámite con folio, así que si se agrega como card al hub sería tipo "Recurso", no "Trámite", y sin el buscador de seguimiento. Tampoco ejecutado esta sesión. |
| **Nada más pendiente de esta sesión** | Ambas decisiones de arquitectura quedan documentadas aquí y en la memoria del proyecto para que una sesión futura que retome "sacar los trámites de otde.html" no tenga que re-discutir el destino. |
| **Commits** | Pendiente — cambio sin comitear en `otde.html`. |

---

## CHECKPOINT — 2026-08-10 (noche) · Oficina Virtual OTDE: hub de servicios + seguimiento de solicitudes

| | |
|---|---|
| **Fecha** | 2026-08-10 |
| **Sesión** | Jorge pidió retomar la visión de "oficina virtual" que había planteado y pospuesto el 7 ago 2026 (ver checkpoint de esa fecha). Primera vuelta de plan malinterpretó el alcance (una utilidad de solo consulta, reusando las tabs de `otde.html` vía deep-link); Jorge la corrigió en vivo: quería un hub real de servicios, con `otde.html` intacto y un banner nuevo llevando ahí. Motivación confirmada: falta que el solicitante pueda ver el estatus de su trámite (hoy todo es correo/Telegram, sin portal de consulta) — señal de que ya estaba anticipado en el código: comentario literal en `Incidencias.gs` (`Correos-institucionales/webform-2026-2027/`) sobre "cuando exista una página de consulta de folio". |
| **`oficina-virtual.html` (nuevo)** | Hub con hero+buscador de folio y grid de servicios (Centro de Formación Docente, Mantenimiento, Asesorías, Correo, Soporte, + card "Próximamente"), reusando `styles.css` y el patrón `.area-card` de `areas.html` en vez de inventar un sistema propio — detalle completo en `CLAUDE.md`. |
| **`otde.html`** | Sin cambios de estructura — solo un banner nuevo (gradiente guinda, arriba del de Formación Docente) y un manejador de `location.hash` en `DOMContentLoaded` para que los links del hub abran la tab correcta al llegar. |
| **Endpoints de consulta de folio, 4 backends** | Contrato uniforme `GET ?action=consulta&folio=...&correo=...` agregado a `mantenimiento.gs`, `asesorias.gs`, `soporte-remoto.gs` (los 3 en `seprn-sitio/apps-script/`) y `Correos-institucionales/webform-2026-2027/WebApp.gs` (repo git distinto). Mismo mensaje genérico de "no encontrado" si el folio no existe o el correo no coincide, para no dejar adivinar folios válidos por descarte — explícitamente no es autenticación real (folios secuenciales), mismo nivel de protección que el resto del sitio. |
| **`soporte-remoto.gs` gana `Estatus`** | Hasta ahora era el único de los 3 backends de trámite sin columna de estatus ni cierre automático. Se agregó replicando el patrón ya probado de Mantenimiento/Asesorías: dropdown de 5 valores, trigger `sopOnEditCierre` (requiere correr `sopInstalarTriggerCierre()` una vez tras redesplegar), auto-heal de encabezados (antes tampoco lo tenía), y menú "OTDE Soporte" nuevo. A diferencia de los otros dos, solo notifica al solicitante — Soporte no tiene `Contactos_Zona_Sector`. |
| **Verificación** | `node --check` sobre los 4 `.gs` tocados y los bloques `<script>` de `otde.html`/`oficina-virtual.html` — sin errores. Probado en localhost con servidor estático: el hub renderiza, el deep-link a `otde.html#mantenimiento` abre la tab correcta (confirmado leyendo `.servicio-tab.active`/`.servicio-content.active` en vivo), la validación de formato de folio funciona sin llegar a hacer fetch. |
| **Redesplegado a producción (mismo día, con Jorge controlando el alcance)** | Los 4 backends: `mantenimiento.gs` (v6), `asesorias.gs` (v6), `soporte-remoto.gs` (v5, incluye el `Estatus` nuevo) y `WebApp.gs` del proyecto `Correos-institucionales/webform-2026-2027/` (v4) — mismos IDs de implementación de siempre, verificados contra las constantes `*_APPS_SCRIPT_URL` antes de tocar nada. Cada paste se verificó con hash SHA-256 del archivo local contra `monaco.editor.getModels()` en el editor real antes de guardar — coincidencia exacta confirmada en los 4 antes de desplegar. `sopInstalarTriggerCierre()` corrido una vez para Soporte, pero **no instaló el trigger en silencio** (0 activadores tras correrlo desde el editor, sin error visible) — se instaló en su lugar vía la UI "Agregar activador" (`sopOnEditCierre`, Desde la hoja de cálculo, Al editar), que sí quedó registrado. Pendiente investigar por qué la llamada programática no tuvo efecto (funcionó para instalar el trigger, solo no se sabe la causa raíz). |
| **Bug real encontrado y corregido en producción** | `WebApp.gs` (Correo) tronaba `ReferenceError: HOJA_ALTA is not defined` en todo folio `OTDE-ALT-`/`OTDE-CAM-`/`OTDE-2FA-`/`OTDE-INC-` — el `const MAPA_PREFIJO_HOJA_CONSULTA` de nivel superior leía `HOJA_ALTA`/`HOJA_CAMBIO`/etc. (definidas en `Alta.gs`/`CambioContrasena.gs`/...), pero Apps Script no garantiza el orden de evaluación de `const` de nivel superior entre archivos del mismo proyecto — se detectó probando el endpoint recién desplegado (`curl` devolvió una página de error de Apps Script, no JSON). Fix: el mapa se arma ahora *dentro* de `manejarConsultaCorreo()`, no a nivel de módulo, redesplegado como v4. Verificado en vivo: los 4 prefijos de Correo responden `{"status":"no_encontrado"}` limpio. |
| **Verificación final en vivo** | Los 7 prefijos de folio (`OTDE-MAN-`/`OTDE-ASE-`/`OTDE-SOP-`/`OTDE-ALT-`/`OTDE-CAM-`/`OTDE-2FA-`/`OTDE-INC-`) probados por `curl` contra los 4 deployments reales — todos responden `{"status":"no_encontrado"}` para un folio inventado, confirmando que el código nuevo está en vivo. Flujo completo re-probado en el navegador contra los backends reales (folio `OTDE-ALT-0007` + correo inventado → mensaje de "no encontrado" correcto en el panel del hub). |
| **Documentación** | `CLAUDE.md` (tabla de páginas, sección nueva "Oficina Virtual OTDE", columnas de Soporte actualizadas a A-P), memoria de proyecto actualizada con el alcance corregido y el estado de despliegue. |
| **Commits** | Pendiente — cambios en el working tree de ambos repos (`seprn-sitio` y `Correos-institucionales`), no comiteados todavía. |

---

## CHECKPOINT — 2026-08-10 (tarde) · Smoke test + auditoría UX/UI, logomark resuelto vía manual de identidad del Edomex, y publicación con conflicto real en otde.html

| | |
|---|---|
| **Fecha** | 2026-08-10 |
| **Sesión** | Jorge pidió montar un localhost, corregir el banner de CTE (ya apuntaba mal desde el checkpoint anterior de este mismo día) y hacer un smoke test completo simulando un usuario final, más una auditoría UX/UI calificada con plan a 9.5. De ahí salieron dos hallazgos nuevos que se corrigieron, y por separado Jorge pidió retomar el pendiente de "Logomark SEPRN" — que resultó estar bloqueado por un manual de identidad gráfica gubernamental que Jorge no había mencionado hasta que se le preguntó directamente. |
| **Smoke test + auditoría UX/UI** | Navegación real contra `localhost:8000` (sin enviar ningún formulario real — esos escriben en Sheets/Telegram de producción). Auditoría actualizada sobre el mismo rubro de 9 dimensiones que ya usaba `docs/ROADMAP.md` desde mayo — promedio subió de ~7.8 a ~7.9/10 tras los fixes de esta sesión. Meta de 9.5/10 que pidió Jorge: evaluada como alcanzable en UI/UX/Performance solo con código, pero Branding y Conversión/Retención dependían de un logomark real y de decisiones de producto respectivamente — ver más abajo. Detalle completo en `docs/ROADMAP.md`. |
| **Banner de `index.html` corregido** | `.update-banner` pasó de "Octava Sesión Ordinaria 2025-2026 ya disponible" (el ciclo cerrado) a "Fase Intensiva 2026-2027 ya disponible" — el `href` a `cte.html` ya era correcto, solo el texto estaba desactualizado. |
| **Bug real: mensaje de error de CCT duplicado** | Encontrado con el smoke test (submit vacío → CCT inexistente): el aviso ámbar de fallback manual y el error rojo genérico "Ingresa una CCT válida" quedaban visibles a la vez. Causa raíz y fix en `docs/QA-NOTES.md #12` — mismo patrón corregido en los 6 lugares que lo comparten (`otde.html`: Mantenimiento/Asesorías/Soporte/Correo ×4; `formacion-docente.html`). |
| **Bug real: catálogo de Formación Docente sin feedback de espera larga** | Medido en pruebas: 15-25s por cold start de Apps Script, muy por encima del "esto puede tardar unos segundos" que promete el copy. No se cuelga (protegido por el timeout de 30s ya existente), pero no había ningún aviso mientras pasaba. Fix: segundo mensaje a los 10s ("Esto está tardando más de lo usual..."), y el texto del loader se resetea en cada reintento. Verificado simulando una respuesta lenta (12s) directamente en el navegador. |
| **Logomark: se descartó un escudo propio tras revisar el manual real** | Se pitchearon 3 conceptos (escudo+libro, escudo+peldaños, círculo+onda) como Artifact interactivo. A Jorge le gustó el más abstracto pero dijo que no se sentía "de la institución", y mencionó que SEPRN, como dependencia de gobierno, debe alinearse a un manual de identidad institucional — por eso un logo anterior (genérico) se había retirado sin reemplazo. Jorge proporcionó el documento real: `Guía de Contenidos Digitales.pdf` (raíz del repo, ~248MB, 25 páginas — guía de redes sociales del Gobierno del Estado de México, no el manual completo que repetidamente referencia). Extraído con `pdftotext`/`pdftoppm` porque excede el límite de 100MB de la herramienta de lectura. Hallazgos: (1) la paleta de colores oficial del manual coincide exactamente con `--guinda`/`--guinda-acento`/`--caramelo`/`--arena` ya usados en el sitio — validación no buscada; (2) el Escudo de Armas es único y oficial, usado dentro de una "pleca de logos" fija (Gobierno Federal + Escudo + Gobierno Edoméx + "organismo interno" + externos) — ninguna dependencia interna tiene su propio escudo, lo que descartó los dos conceptos con escudo y confirmó que el favicon existente (monograma "NE/ZA", sin escudo) ya era la categoría correcta; (3) las tipografías oficiales son Gotham/BW Modelica/Corporative Sans Alt, ninguna es Montserrat, y BW Modelica no debe usarse con "ñ" ni Corporative con "z" — ambas letras están en "Nezahualcóyotl" — hallazgo grande pero deliberadamente **no** actuado esta sesión ("solo el logomark por ahora"). |
| **Logomark: qué se construyó** | Se mantuvo la idea del favicon sin cambios, solo `font-family` de Georgia a Montserrat — verificado primero como comparación lado a lado en un Artifact (favicon actual vs. redibujado, en nav/footer/favicon reales) antes de tocar el repo; Jorge aprobó con "se ve chulo en montserrat, dale". Aplicado a `favicon.svg`, nuevo `logo.svg` (lockup horizontal de referencia), y al nav/footer de las 13 páginas que comparten `styles.css` (nueva clase `.logo-icon`, colorway invertido en el footer). Detalle técnico completo en `docs/ARCHITECTURE.md §18`. |
| **Publicación: conflicto real, no solo divergencia de historial** | Mismo mecanismo de `publish-<rama>` + cherry-pick que la sesión anterior de este mismo día, pero esta vez el cherry-pick chocó de verdad: `otde.html` en `origin/main` es de antes de que existieran las pestañas de Mantenimiento/Asesorías/Correo (parte de los commits de OTDE aún no publicados), así que el fix de CCT para esas pestañas no tenía sobre qué aplicarse ahí. Resuelto excluyendo `otde.html` por completo del commit publicado (`git checkout --ours otde.html` durante el cherry-pick, confirmado con `git diff origin/main..publish-logomark -- otde.html` vacío antes de pushear) — `.gitignore` y `docs/ROADMAP.md` sí se resolvieron a favor de la versión nueva (conflictos por archivos desactualizados en `origin/main`, sin riesgo funcional). Efecto práctico: el logomark quedó en vivo en 12 de las 13 páginas; `otde.html` sí tiene el ícono y el fix en el repo (rama `main` local), pero no en producción todavía — llegará junto con el resto del trabajo de OTDE. |
| **Documento de referencia sin comitear** | `Guía de Contenidos Digitales.pdf` agregado a `.gitignore` por nombre — 248MB excede el límite de 100MB de GitHub por archivo, se queda solo local. |
| **Documentación** | `docs/ROADMAP.md` (scores actualizados, banner y logomark marcados resueltos, 2 hallazgos nuevos), `docs/QA-NOTES.md` (ítem 12), `docs/ARCHITECTURE.md §18` (patrón nuevo), `CLAUDE.md` (`.logo-icon` en Sistema de diseño, 2 pendientes resueltos con matices). |
| **Verificación** | Todo probado en `localhost:8000` con recarga forzada (`Cmd+Shift+R`) antes de dar cada fix por bueno — el navegador cacheaba agresivamente el HTML servido por `python3 -m http.server`, lo que generó un falso negativo al verificar el logomark la primera vez. Responsive verificado en 390px (mobile) para el ícono en nav. `git diff a6c4ae0~1..a6c4ae0 --stat`: 19 archivos, 133 inserciones/46 eliminaciones. Confirmado antes de pushear que `otde.html` no tenía diferencia contra `origin/main`. |
| **Commits** | `a6c4ae0` en `main` local (incluye `otde.html`); publicado a `origin/main` como `90373b6` vía cherry-pick a `publish-logomark` (sin `otde.html`). Pendiente el de esta actualización de documentación de cierre. |

---

## CHECKPOINT — 2026-08-10 · Arranque del ciclo 2026-2027 en CTE, publicación aislada de OTDE, y fix de push por SSH multi-cuenta

| | |
|---|---|
| **Fecha** | 2026-08-10 |
| **Sesión** | Jorge necesitaba publicar la Fase Intensiva 2026-2027 en `cte.html` sin arrastrar los 12 commits de OTDE que seguían sin terminar de validar en `main`. Primero se armó el mecanismo de publicación aislada (rama `publish-cte` + cherry-pick), después se diseñó y construyó la estructura de ciclo/archivo para `cte.html`, y al momento de hacer el push real se descubrió que las credenciales de Git en esta Mac correspondían a la cuenta equivocada — se resolvió configurando SSH dedicado. |
| **Mecanismo de publicación aislada** | `origin/main` estaba 12 commits atrás de `main` local (todos de OTDE: webforms de Mantenimiento/Asesorías/Correo, backends, docs) porque ese trabajo no estaba listo para salir a producción. Se creó la rama `publish-cte` apuntando a `origin/main` (sin los 12 commits de OTDE) para poder cherry-pickear ahí, y solo ahí, los commits de CTE — así `main` local conserva el trabajo de OTDE sin publicarlo, y `publish-cte` sirve de "puerta" hacia producción para cambios sueltos que sí están listos. |
| **`cte.html`: ciclo actual + archivo colapsable del ciclo anterior** | Nueva estructura documentada en `docs/ARCHITECTURE.md §17`: `.section-header` "Ciclo Escolar 2026-2027" arriba, con la Fase Intensiva 2026-2027 como única sesión activa; debajo, un contenedor nuevo `.ciclo-archivo` (toggle propio `toggleCicloArchivo()`, independiente del `toggleSesion()` de acordeón único ya existente) agrupa las 11 sesiones del ciclo 2025-2026, colapsado por default. Se corrigió de paso un bug menor heredado: el iframe de Opening del extinto "Taller Intensivo de Cierre" tenía `src` en vez de `data-src`, así que cargaba aunque estuviera colapsado — ahora usa lazy-load igual que el resto. |
| **Reorganización de `pdfs/cte/`** | Jorge reorganizó a mano, en paralelo a la sesión, `pdfs/cte/<sesion>/` en `pdfs/cte/cte-2025-2026/<sesion>/` y `pdfs/cte/cte-2026-2027/cte-fase-intensiva/` — se detectó por `git status` (49 archivos aparecían como borrados) y se actualizaron todos los `href` de `cte.html` para apuntar a las rutas nuevas antes de publicar. Documentado en `docs/ARCHITECTURE.md §17`, `README.md` y `CLAUDE.md` (regla 4). |
| **Fase Intensiva 2026-2027: 9 materiales, sin video todavía** | 8 PDFs oficiales (nombres limpiados de los códigos de archivo tipo `2627_s0_...` a título legible, a petición de Jorge) + un noveno material confirmado explícitamente por Jorge: un artículo académico ("Territorio, Cultura y Contextualización Curricular", Miguel Zabalza Beraza) como lectura complementaria, etiquetado distinto (`Lectura complementaria • PDF`) para no confundirlo con los documentos oficiales de SEP. ZIP del paquete completo agregado después, cuando Jorge lo subió. Sin `video-container` — pendiente, ver `docs/ROADMAP.md`. |
| **Fix: Git usaba la cuenta de GitHub equivocada al hacer push** | El credential helper de macOS (`osxkeychain`) reusaba una credencial de otra cuenta (`auliamx`) para cualquier push a `github.com`, sin importar el repo — Jorge confirmó que este problema ya le había pasado antes en otros proyectos no relacionados. Se generó una llave SSH dedicada (`~/.ssh/github_educaneza`) y un alias en `~/.ssh/config` (`Host github-educaneza`) con `IdentitiesOnly yes` — sin ese flag, `ssh-agent` seguía ofreciendo primero una llave ya cargada de otro proyecto (`github_escuela`, cuenta `esc-miguel-hidalgo`) antes de la del alias, y GitHub autenticaba con la llave equivocada aunque el alias fuera el correcto. Se agregó `IdentitiesOnly yes` también al alias viejo `github-escuela` para prevenir el mismo problema a la inversa. El remoto `origin` de este repo se cambió de HTTPS a `git@github-educaneza:educaneza/seprn-sitio.git` — documentado en `README.md` §Despliegue para que una sesión futura no se confunda si ve una URL SSH en vez de HTTPS. |
| **Publicación** | `git push origin publish-cte:main` (vía el alias SSH nuevo) llevó los 2 commits de CTE a producción sin tocar los 12 de OTDE, que siguen solo en `main` local. Al volver a `main`, la rama quedó divergida de `origin/main` (14 vs. 2 commits) — es solo un tecnicismo de historial (el contenido de CTE ya coincide), se resuelve con un rebase limpio cuando OTDE esté listo para publicarse. |
| **Documentación** | `CLAUDE.md` (tabla de sesiones CTE reestructurada por ciclo, regla 4 de rutas de PDFs, 2 pendientes actualizados), `docs/ARCHITECTURE.md §17` (patrón nuevo completo), `docs/ROADMAP.md` (pendiente de banner corregido para no esperar una "novena sesión" que no va a existir + nuevo pendiente de video), `README.md` (árbol de `pdfs/cte/` y nota del remoto SSH). |
| **Verificación** | Probado en `localhost` (Python `http.server`) vía navegador antes y después de cada cambio de estructura: acordeón de Fase Intensiva abre con badge NUEVO y sus 9 materiales; bloque de archivo carga colapsado y expande sin romper la animación; abrir una sesión dentro del archivo sigue cerrando cualquier otra en toda la página (acordeón único intacto); video de una sesión archivada carga bien tras el fix de lazy-load; sin errores de consola. Verificación de rutas con un script Python que confirmó los 47 `href` de `pdfs/cte/` contra el filesystem real al final de la sesión (0 rotas, salvo la esperada: el `cuarta-sesion-completa.zip` del bloque de ejemplo comentado, que nunca existió). `git diff --numstat` de los 2 commits: `cte.html` 254/41, `CLAUDE.md` 49/17, más 47 archivos de `pdfs/cte/` (renombrados o nuevos). Conexión SSH confirmada con `ssh -T git@github-educaneza` → `Hi educaneza!` antes de intentar el push real. |
| **Commits** | `b904b49` (estructura de ciclo + reorganización de PDFs), `b700d66` (lectura complementaria), ambos publicados a `origin/main` vía cherry-pick a `publish-cte`. Pendiente el de esta actualización de documentación de cierre. |

---

## CHECKPOINT — 2026-08-07 (tarde) · Repaso de webforms (9 hallazgos), Función por tipo de CCT, y despliegue en vivo

| | |
|---|---|
| **Fecha** | 2026-08-07 |
| **Sesión** | Jorge hizo un repaso manual de los 4 webforms de `otde.html` (Correo, Mantenimiento, Asesorías, Soporte) en localhost antes del arranque del ciclo 2026-2027 y dio 9 hallazgos concretos de UX/datos. Al cerrar, pidió desplegar los 3 backends tocados a producción él mismo controlando el navegador, y después dos preguntas de arquitectura: cómo concentrar los 5 Sheets del ecosistema en una sola carpeta de Drive, y si conviene consolidar los 4 tipos de solicitud de Correo en una sola hoja. La visión más grande de Jorge de separar OTDE en una "oficina virtual" aparte de `otde.html` se planteó y se dejó explícitamente fuera de esta sesión, para su propia conversación futura. |
| **"Tipo de CCT" reemplaza a "SEPRN" como opción de Sector** | `js/cct-db.js` ya traía un campo `tipo` por CCT (`escuela\|supervision\|jefatura\|subdireccion`) que nada usaba salvo el dominio de correo. Los 7 bloques de fallback manual de CCT en `otde.html` (Alta/Cambio/Reset/Incidencias/Mantenimiento/Asesorías/Soporte, funciones `*ActualizarTipoCct()`) ahora piden explícitamente "Tipo de CCT" en vez de meter `SEPRN` como una opción más del `<select>` de Sector — corrige de paso un bug real: antes, una CCT de tipo `jefatura` (sector) quedaba forzada a contestar también "Zona", que no le aplica. |
| **Función/Cargo ramificada por tipo de CCT** | Nueva función compartida `otdeOpcionesFuncion(tipo)` en `js/cct-db.js` y el helper de DOM `otdePoblarFuncion(selectId, tipo)` en `otde.html`, usados por los 4 formularios que preguntan Función (Alta, Mantenimiento, Asesorías, Soporte): escuela → Director(a)/Subdirector(a)/Docente/PAAE; zona → Supervisor(a) Escolar/ATP/PAAE; sector → Supervisor(a) General/ATP/PAAE; SEPRN → Encargado(a) del Despacho/Subjefe(a)/Jefe(a) de Oficina/ATP/Administrativo. No requirió cambios de backend — los 4 `.gs` solo validan que `funcion` no venga vacío, no una lista fija. |
| **Otros 6 hallazgos de UX aplicados** | "2FA" renombrado a "Eliminar método de autenticación" en todo el sitio (mismo término que usa SIGEE) con aclaración en paréntesis para docentes; "Teléfono"/"WhatsApp" unificado a "Teléfono (WhatsApp)" + aviso de autorización de contacto en los 7 campos; correo obligatorio (antes opcional) en Mantenimiento/Asesorías/Soporte + aviso de institucional/spam en los 7 campos de correo del sitio; oficio de Mantenimiento/Asesorías restringido a solo PDF (antes PDF+imagen) + aviso de que igual hay que entregar el original físico; en Mantenimiento, "Equipos con falla" pasó de textarea libre a checklist (aula de medios / administrativas / red-si-ya-hay-infraestructura / otro, con cantidad por categoría de cómputo, marca/modelo opcional y estado de instalación) más una nota de qué sí/no atiende OTDE; en Soporte, nuevo campo obligatorio "¿Qué tipo de ayuda necesitas?". |
| **Bug real encontrado en la propia sesión** | `.soporte-form-group input { width:100% }` (regla ya existente) también estiraba los `<input type="checkbox">` a todo el ancho del contenedor, empujando su `<span>` de texto lejos a la derecha — visible al construir el checklist nuevo de Mantenimiento. Fix: `.soporte-form-group input[type="checkbox"] { width:auto; padding:0; }`. Corrige de paso, sin querer, el mismo problema latente que ya tenía el checkbox de confirmación de mantenimiento previo de Asesorías (agosto 2026), que nunca se había notado. |
| **Despliegue a producción, controlado por Claude vía navegador a petición de Jorge** | `mantenimiento.gs` (v5), `soporte-remoto.gs` (v4) y `asesorias.gs` (v5) — mismos 3 IDs de implementación de siempre, verificados contra las constantes `*_APPS_SCRIPT_URL` de `otde.html` antes de tocar nada. Cada paste se verificó con un hash SHA-256 del archivo local contra el contenido puesto en el editor de Apps Script (vía Monaco `getModels()[0].getValue()`) antes de guardar — el primer intento de copiar `mantenimiento.gs` sí falló (quedó pegado el texto suelto "Urgencia" en vez del código, por un mecanismo de portapapeles poco confiable en el entorno de automatización), pero se detectó por el hash antes de guardar, se deshizo con Cmd+Z, y se resolvió cambiando de técnica (transferir el contenido vía `window.name` entre pestañas y escribirlo directo en el modelo de Monaco, sin pasar por el portapapeles del SO) — nunca llegó código corrupto a producción. A `Solicitudes_Soporte_2026` (sin auto-heal de encabezados) se le agregó a mano la columna "Tipo de ayuda"; `mantenimiento.gs` sí auto-completa columnas nuevas en su próxima solicitud real. |
| **Preguntas de arquitectura — Sheets** | (1) Nombres exactos para juntar los 5 Sheets del ecosistema en una carpeta, confirmados abriendo cada proyecto de Apps Script (no adivinados): `Solicitudes_Correo_2026_2027`, `Solicitudes_Mantenimiento_2026`, `Solicitudes_Asesorias_2026`, `Formacion_Docente_2026_2027`, y **`Soporte Técnico Remoto`** (única excepción — el archivo de Drive no se llama como su pestaña interna `Solicitudes_Soporte_2026`). Se aclaró que renombrar/mover el *archivo* es seguro (el script referencia por ID, no por nombre/ruta) pero renombrar una *pestaña interna* sí rompería el código (`getSheetByName()` la busca por ese string exacto). (2) Se revisó en vivo el código de las 4 hojas de Correo (`Alta.gs` 24 columnas vs. `CambioContrasena.gs`/`Reset2FA.gs`/`Incidencias.gs` 15-17 columnas cada una) y se confirmó que las credenciales muertas de CoEEE caen en la pestaña "Incidencias" — se recomendó **no** consolidarlas en una sola hoja (columnas demasiado distintas entre Alta y el resto, y el refactor tocaría las 4 funciones de escritura + `OnEdit.gs`) y en su lugar armar una hoja "Resumen" con `QUERY`/`IMPORTRANGE` si se quiere una vista concentrada — mismo patrón ya acordado con Jorge de "hojas separadas + capa que unifica" para Mantenimiento/Asesorías/Soporte. Ningún Sheet se movió ni se consolidó en esta sesión, es solo la recomendación. |
| **Documentación** | `CLAUDE.md` (Tipo de CCT/Función por tipo documentado en §11 y §16, columnas nuevas de Mantenimiento/Soporte, versiones de despliegue), `docs/ARCHITECTURE.md §11` (el patrón compartido de CCT ahora incluye Tipo de CCT + Función ramificada), `docs/ARCHITECTURE.md §15` (checklist de equipos de Mantenimiento, correo obligatorio), `docs/QA-NOTES.md` (ítem 11 nuevo: checkbox estirado por regla de ancho compartida). |
| **Verificación** | `node --check` sobre los 3 `.gs` modificados (copiados a `.js` en scratchpad) y sobre los 2 bloques `<script>` extraídos de `otde.html` — sin errores. Antes de cada redeploy, hash SHA-256 del archivo local comparado byte a byte contra `monaco.editor.getModels()[0].getValue()` en el editor real — coincidencia exacta confirmada en los 3 antes de guardar. Probado en localhost con un servidor estático: los 7 tipos de CCT (escuela/zona/sector/SEPRN, vía CCT real y vía fallback manual) muestran la lista de Función correcta; checklist de equipos con cantidad condicional; validación bloquea envío sin equipo marcado/correo/PDF. `git diff --numstat`: 5 archivos, 546 inserciones/117 eliminaciones (otde.html 499/105, cct-db.js 25/0, mantenimiento.gs 12/4, soporte-remoto.gs 8/6, asesorias.gs 2/2). |
| **Commits** | Pendiente — ver propuesta de commit de cierre abajo. |

---

## CHECKPOINT — 2026-08-07 · Redeploy de fixes QA pendientes + cierre automático de tickets

| | |
|---|---|
| **Fecha** | 2026-08-07 |
| **Sesión** | Jorge pidió verificar si el "Pendiente para Jorge" del checkpoint anterior (redesplegar `asesorias.gs`/`formacion-docente.gs`) ya estaba resuelto — no lo estaba. Después, a partir de dos preguntas exploratorias sobre migrar a webform el reporte de cierre del técnico (sistema v8.5 de Reportes de Visitas) y el feedback de Asesorías, se acordó que lo que realmente faltaba era el cierre automático del ciclo hacia el solicitante y la Zona/Sector — los otros dos Google Forms se dejaron fuera de alcance. |
| **Verificación del redeploy pendiente** | Confirmado en vivo (no solo por fecha de versión) que ninguno de los 2 fixes había llegado a producción: `confirmaMantenimiento`/`Confirmó Mantenimiento Previo` no existían en el código desplegado de Asesorías (0 resultados), y `const MINUTOS_ANTES_INICIO_MIN = 20;` seguía presente en el de Formación Docente. Se repegó el código actual del repo en ambos editores de Apps Script y se desplegó nueva versión (mismo ID de implementación en los dos, no hizo falta tocar `otde.html`/`formacion-docente.html`). |
| **Cierre automático de Mantenimiento/Asesorías** | Nuevo trigger `onEdit` instalable por proyecto (`manOnEditCierre`/`aseOnEditCierre`, instalado vía `manInstalarTriggerCierre()`/`aseInstalarTriggerCierre()` — no un trigger simple, esos no pueden llamar `MailApp`): al marcar Estatus = `Resuelto` en la hoja `Solicitudes`, notifica por correo al solicitante y a la Zona/Sector (reusando `manBuscarContactoZonaSector`/`aseBuscarContactoZonaSector` ya existentes). La columna `Estatus` pasó de texto libre a dropdown (`Pendiente de validar` · `Validado` · `En atención` · `Resuelto` · `Rechazado`) para que el trigger tenga un valor confiable; se agregó una columna `Notificación de cierre enviada` para evitar reenvíos. `Rechazado` queda en el dropdown sin lógica todavía, deliberadamente. |
| **Prueba de punta a punta** | En los 2 Sheets reales (`Solicitudes_Mantenimiento_2026`, `Solicitudes_Asesorias_2026`, ambos sin datos de producción todavía): fila de prueba + contacto de prueba con correo propio (no un contacto real de Zona/Sector), Estatus → `Resuelto` desde la UI real de Sheets, confirmado en el registro de Ejecuciones de Apps Script (Mantenimiento: 5.44s, Asesorías: 1.437s, ambas "Completada", 0% error) y en la columna de control en `Sí`. Filas de prueba eliminadas después. |
| **Limpieza** | Se eliminó del editor en vivo de Formación Docente la función temporal `enviarAhoraManual_WEB2627001()` que había quedado pegada ahí desde el incidente del checkpoint anterior — no vivía en este repo. |
| **Documentación** | `CLAUDE.md` (cierre automático documentado en Mantenimiento/Asesorías, 3 ítems resueltos en "Pendientes vigentes"), `docs/ARCHITECTURE.md §15` (mecanismo de cierre automático + alcance explícitamente descartado), `docs/QA-NOTES.md` (ítem 10 nuevo: fix en el repo ≠ fix en producción si no se redespliega). |
| **Verificación** | `node --check` sobre `apps-script/mantenimiento.gs` y `apps-script/asesorias.gs` — sin errores. `git diff --stat`: 4 archivos, 405 inserciones/14 eliminaciones (191 en `mantenimiento.gs`, 173 en `asesorias.gs`, 24 en `CLAUDE.md`, 31 en `docs/ARCHITECTURE.md`). |
| **Commits** | Pendiente — ver el de esta actualización de documentación. |

---

## CHECKPOINT — 2026-08-06 · Corrección de los 3 bugs de QA encontrados en la sesión anterior

| | |
|---|---|
| **Fecha** | 2026-08-06 |
| **Sesión** | Continuación del mismo día: Jorge pidió una lista de pendientes priorizada y, a partir de ahí, corregir los 2 bugs de Asesorías y el bug de recordatorios de Formación Docente encontrados en la ronda de QA de la sesión anterior (ver checkpoint de abajo). |
| **Asesorías — checkbox sin viajar al backend** | El checkbox de confirmación de mantenimiento previo se validaba en el cliente pero nunca se incluía en el `datos` enviado a `asesorias.gs`, así que el Sheet nunca quedaba con evidencia de la confirmación. Se agregó la columna `Confirmó Mantenimiento Previo` (col. R) a `ENCABEZADOS_ASE_SOLICITUDES`, con auto-completado de encabezado en la hoja ya desplegada (mismo patrón que `obtenerHojaCursos()` en `formacion-docente.gs`, sin migración manual), validación server-side en `aseValidarCampos()`, y el envío real del campo desde `otde.html`. |
| **Asesorías — error del checkbox no se limpiaba** | Se agregó un listener `change` en `ase-confirma-mantenimiento` que quita la clase `visible` del mensaje de error en cuanto se marca, igual que el resto de los campos del formulario. |
| **Formación Docente — recordatorio marcado "enviado" sin mandarse** | `enviarRecordatoriosDiarios()` y `enviarRecordatoriosWebinar()` tenían un piso inferior en su ventana de evaluación (`diasParaInicio >= 0` / `minutosFaltantes >= 0`) que marcaba la columna de recordatorio en `TRUE` sin enviar nada si el curso ya había iniciado al momento de evaluarse. Se quitó ese piso: ahora reintentan en cada corrida subsecuente con el texto ajustado a "ya inició"/"ya comenzó" mientras el curso no haya terminado (`hoy <= Fecha_fin`); solo se resignan sin enviar cuando el curso ya concluyó por completo. Se eliminó la constante `MINUTOS_ANTES_INICIO_MIN`, que quedó sin uso. |
| **Documentación** | `CLAUDE.md` (tachados los 3 pendientes ya resueltos en "Pendientes vigentes"), `docs/QA-NOTES.md` (ítem 8 marcado como corregido), `docs/ROADMAP.md` (corregida la nota desactualizada que aún marcaba los 3 backends de ago 2026 como pendientes de desplegar — ya lo estaban desde el checkpoint anterior). |
| **Verificación** | `node --check` sobre `apps-script/asesorias.gs`, `apps-script/formacion-docente.gs`, y sobre los 2 bloques `<script>` extraídos de `otde.html` — sin errores. `git show --stat` del commit confirma 5 archivos, 91 inserciones/46 eliminaciones. |
| **Commits** | `0d1eba6` (fix de los 3 bugs), más el de esta actualización de documentación (ver `git log`). |
| **Pendiente para Jorge** | Re-desplegar `asesorias.gs` y `formacion-docente.gs` en Apps Script (Administrar implementaciones → Nueva versión) para que ambos fixes lleguen a producción — el repo ya tiene el código correcto, los proyectos en vivo siguen con la versión anterior. |

---

## CHECKPOINT — 2026-08-06 · Despliegue de 3 backends, QA pre-producción e incidente de recordatorio en vivo

| | |
|---|---|
| **Fecha** | 2026-08-06 |
| **Sesión** | Continuación del mismo día: primero Jorge pidió desplegar los 3 backends nuevos que seguían con placeholder `PENDIENTE_DE_DESPLEGAR` (Mantenimiento, Asesorías, Correo), después acotar Telegram a solo notificaciones urgentes y poblar `Contactos_Zona_Sector` con datos reales. Antes de sacarlo a producción, pidió una ronda de QA completa (perspectiva de usuario real + perspectiva de administrador, con énfasis en si el contenido capturado realmente simplifica procesos). Ya cerrando, surgió un incidente urgente: un recordatorio de webinar de Formación Docente marcado como "enviado" pero invisible para Jorge. |
| **Despliegue de los 3 backends** | Mantenimiento (`Solicitudes_Mantenimiento_2026`), Asesorías (`Solicitudes_Asesorias_2026`) y Correo (`Solicitudes_Correo_2026_2027`, único despliegue que enruta Alta/Cambio/Reset2FA/Incidencias por `datos.tipo`) desplegados con URL real en `otde.html`. Telegram acotado a solo lo urgente (Reset2FA/Incidencias sin condición, Cambio de Contraseña solo `@dee.edu.mx`, Alta sin Telegram) — decisión explícita de Jorge, no placeholder. `Contactos_Zona_Sector` poblada en Mantenimiento y Asesorías (88 filas: 75 por Zona + 13 de respaldo por Sector) a partir de `OTDE_Base_Contactos_v2.xlsx` que Jorge proporcionó. |
| **QA pre-producción** | Mantenimiento y Correo (los 4 sub-formularios: Alta, Cambio de Contraseña, Reset 2FA, Incidencias) probados de punta a punta con interceptor de `fetch()` — CCT autocomplete + fallback manual, Title Case, la regla de dominio Director(a)+Cuenta de oficina→`@dee.edu.mx`, payloads reales verificados contra la validación de cada `.gs`: todo correcto. Asesorías: 2 bugs reales encontrados (checkbox de confirmación de asesoría previa no viaja en el payload; su mensaje de error no se limpia al marcarlo) — ver `CLAUDE.md` §Pendientes vigentes. Revisión de administrador: las columnas de cada Sheet sí simplifican procesos reales (`GenerarResumenSIGEE.gs` para Alta, ruteo automático por Zona/Sector), pero Mantenimiento/Asesorías no le avisan al solicitante cuando su ticket se resuelve — pendiente de decisión con Jorge. |
| **Incidente: recordatorio de webinar invisible** | Jorge reportó que un recordatorio de hoy (webinar "Crecer en un mundo digital...", 13:00 hrs) aparecía como enviado en la Sheet pero no encontraba el correo en su bandeja de Enviados. Causa raíz: `enviarCorreoLote()` manda `to: Session.getEffectiveUser().getEmail()` con los inscritos en `bcc` — Gmail archiva esa copia como Recibida, no Enviada (comportamiento sistémico de todas las automatizaciones de OTDE, confirmado también en el SGCI viejo) — ver `docs/QA-NOTES.md #9`. Hallazgo adicional real: el aviso de ESE webinar específico sí se mandó a las 9:52 a.m., pero con la plantilla vieja (sin el rediseño HTML de esta mañana), porque el código nuevo no se guardó hasta las 10:30 a.m. — el activador de las 12:39 p.m. (ya con el código nuevo) se lo saltó por encontrar la columna ya en `TRUE`. Se corrigió a mano: se limpió el flag en la Sheet y se corrió una función temporal (`enviarAhoraManual_WEB2627001`, pegada solo en el editor de Apps Script del proyecto en vivo, no en este repo) que mandó "ya inició, conéctate" con el diseño nuevo a los 15 inscritos reales — 14 entregados, 1 rebote por buzón lleno del destinatario (ajeno al sistema). Se confirmó además una instancia real en producción del bug de "recordatorio marcado sin haberse mandado" — ver `docs/QA-NOTES.md #8` — en el Seminario "Convivencia digital entre estudiantes". |
| **Verificación** | Revisión directa en Gmail (`otde.nezahualcoyotl@gmail.com`, con `in:anywhere` para incluir Recibidos/Spam) del correo real de las 9:52 a.m. y del correo manual de la 1:12 p.m.; registro de ejecuciones de Apps Script (`Mis ejecuciones`) para confirmar hora y estado de los activadores automáticos; hoja `Cursos` revisada directamente para las columnas `Recordatorio_*_enviado`. |
| **Commits** | `a5766b7` (despliegue de los 3 backends en `otde.html`), más el de esta consolidación de documentación (ver `git log`). |

---

## CHECKPOINT — 2026-08-06 · Smoke test de Formación Docente + rediseño de recordatorios automáticos

| | |
|---|---|
| **Fecha** | 2026-08-06 |
| **Sesión** | Jorge pidió un smoke test completo del Centro de Formación Docente y verificar que los recordatorios automáticos funcionaran de verdad, con calificación argumentada (8.5/10) y plan de mejora. A partir de ahí pidió ajustar los tiempos de los recordatorios, rediseñar el correo, agregar redes sociales y proteger las respuestas hacia su cuenta institucional — y desplegar todo a producción en la misma sesión. |
| **Smoke test** | Flujo completo probado en producción vía navegador (catálogo dinámico, selección multi-curso, paso de registro previo externo, validación de formulario, autocomplete de CCT + fallback manual, layout móvil) sin disparar el `doPost` real. Recordatorios verificados de forma solo-lectura en Apps Script (`Mis activadores`/`Mis ejecuciones`): ambos triggers instalados y corriendo con 0% de tasa de error. 2 bugs reales encontrados: error de CCT que no se limpiaba al seleccionar una sugerencia válida, y el aviso "empieza en 2 días" se perdía en silencio si la cuota de correo se agotaba justo ese día — ver `docs/QA-NOTES.md #6`. |
| **Tiempos de recordatorio reescritos** | Regla nueva acordada con Jorge: "empieza en 1 día" para cursos de varios días (+ fallback en cursos de un solo día sin `Hora_inicio`), "vas a la mitad" sin cambios (30+ días), "empieza en 30 minutos" (ventana 20-40 min) para cualquier curso con `Hora_inicio` capturada — evaluado con un disparador nuevo cada 15 min (antes cada hora). `instalarRecordatoriosAutomaticos()` ahora borra y recrea los activadores en cada corrida en vez de solo agregar si faltan, para que el cambio de intervalo se aplique de verdad. Se agregó `verificarActivadoresInstalados()`: alerta a Jorge por correo (máx. 1x/día) si algún activador desaparece. |
| **Correo rediseñado** | `construirCorreoHtml()` nuevo: plantilla HTML con paleta institucional, chip de categoría, caja de detalle del curso, botón CTA, y 3 íconos de redes sociales en el footer (Facebook `facebook.com/SubNeza`, YouTube y Canal de WhatsApp institucionales — mismas URLs reales del sitio). Bug real encontrado en la propia revisión visual: faltaba `<meta charset="utf-8">`, los acentos se corrompían — ver `docs/QA-NOTES.md #7`. `enviarCorreoLote()` ahora manda `replyTo: otde.nezahualcoyotl@dee.edu.mx` — Jorge reportó que un "Responder" le llegaba a su Gmail; se confirmó en el propio deployment que esa cuenta institucional es un *alias* de su Gmail real, de ahí el bug. |
| **Despliegue a producción** | Proyecto de Apps Script renombrado ("Proyecto sin título" → "Formacion Docente - Backend (formacion-docente.gs)"), código pegado y Versión 8 desplegada (mismo ID de implementación, no hizo falta tocar `APPS_SCRIPT_URL` en `formacion-docente.html`), y menú "OTDE Formación → Instalar recordatorios automáticos" corrido de nuevo desde la hoja real — confirmado por el propio mensaje del sistema y por `Mis activadores` (trigger de 15 min recreado bajo el proyecto ya renombrado). |
| **Verificación** | `node --check` sobre `apps-script/formacion-docente.gs` (copiado a `.js`) y sobre el `<script>` extraído de `formacion-docente.html`, sin errores. Los 3 correos (empieza en 1 día / vas a la mitad / 30 minutos) generados desde la función real `construirCorreoHtml()` y revisados visualmente en el navegador (escritorio, móvil, con y sin liga) antes de desplegar. |
| **Commits** | Pendiente — ver propuesta de commit de cierre abajo. |

---

## CHECKPOINT — 2026-08-05 · Auditoría de cimientos + 3 webforms nuevos (Correo Alta, Mantenimiento, Asesorías)

| | |
|---|---|
| **Fecha** | 2026-08-05 |
| **Sesión** | Sesión larga: primero se construyeron 3 entregables nuevos siguiendo el mismo patrón (webform + Apps Script propio + Sheet con folio + Telegram), después Jorge pidió una auditoría completa de estructura/documentación al notar que `otde.html` y `CLAUDE.md` habían crecido como bitácora en vez de como referencia. |
| **Correo Institucional** | Webform de Alta de cuenta con dominio auto-derivado por CCT (`otdeDominioParaCCT()`, regla verificada en vivo contra SIGEE — el sistema real de CoEEE) + Cambio de Contraseña/Reset 2FA/Incidencias migrados del Google Form viejo. Backend nuevo y paralelo en `Correos-institucionales/webform-2026-2027/` (repo aparte); el sistema en vivo no se tocó. Ver `docs/ARCHITECTURE.md §16`. |
| **Mantenimiento** | Nueva tab con webform que complementa el oficio (sigue siendo obligatorio) con captura digital + oficio adjunto a Drive. `apps-script/mantenimiento.gs`. Ver `docs/ARCHITECTURE.md §15`. |
| **Asesorías** | Nueva tab (no existía como trámite) — mismo patrón, más selector de tipo de asesoría y casilla de confirmación de mantenimiento previo. `apps-script/asesorias.gs`. Ver `docs/ARCHITECTURE.md §15`. |
| **Auditoría/consolidación** | `.gitignore` agregado (no existía); comentarios `SERVICIO N` de `otde.html` corregidos (estaban mal numerados, 2 tabs sin marcador); contradicción interna en `docs/ROADMAP.md` sobre Formación Docente eliminada; `docs/ROADMAP.md`/`docs/ARCHITECTURE.md` reconciliados en favicon/`aria-current`; 3 secciones fechadas por separado de `otde.html` en `CLAUDE.md` consolidadas en una sola; este archivo (`docs/BITACORA.md`) creado, separando la bitácora cronológica de `docs/ROADMAP.md` (que queda solo para lo futuro); skill `/close` creado, adaptado del patrón de `aulia/.claude/skills/close/SKILL.md`. |
| **Verificación** | `otde.html`: `node --check` sobre el `<script>` extraído + `grep` de IDs duplicados, sin hallazgos. `docs/ROADMAP.md`/`docs/ARCHITECTURE.md` releídos completos para confirmar que ya no se contradicen. |
| **Commits** | `f9c0290` (`.gitignore`), `bdcd3c2` (3 webforms), más los de esta consolidación (ver `git log`). |

---

## CHECKPOINT — 2026-07-13 · Retiro de Jornada Verano 2026

Cerró el periodo de inscripciones a la Jornada de Capacitación Verano 2026: se eliminó el
banner en `otde.html` y se borraron `jornada-verano-2026.html` e
`instructivo-jornada-verano-2026.html` — ya nadie debe llegar a ese wizard. El backend
`apps-script/formacion-docente.gs` sigue vivo (también sirve a `formacion-docente.html`); solo
se perdió la parte específica de esas 2 páginas. Commit: `88c7e2e`.

## CHECKPOINT — 2026-07-07 · Centro de Formación Docente: deploy + rediseño premium + cutover

Desplegado en producción: Spreadsheet real `Formacion_Docente_2026_2027`,
`apps-script/formacion-docente.gs` con URL real. Arquitectura relacional en Sheets
(`Docentes`/`Cursos`/`Inscripciones`, no una hoja por curso). Cutover de Jornada Verano 2026 a
este mismo backend. Recordatorios automáticos por correo. Prueba social real de inscritos.
Rediseño visual premium (Inter/Inter Tight, tarjetas pastel, panel lateral sticky) — ver
`docs/DESIGN_SYSTEM.md`. Smoke test + 5 bugs reales corregidos — ver `docs/QA-NOTES.md`.
Commit: `6c2d08c`.

## CHECKPOINT — 2026-07-01 · Instalador de Office + Soporte Técnico Remoto potenciado

Pestaña "Licencias Office" en `otde.html` (instalador validado por CCT + guía paso a paso).
Formulario "Solicitar Soporte Técnico Remoto" con autocompletado de CCT y fallback manual.
Backend nuevo `apps-script/soporte-remoto.gs` con notificación por bot de Telegram. Referencia
cruzada entre ambas pestañas. Mismo día: `jornada-verano-2026.html` (wizard 3 pasos) e
`instructivo-jornada-verano-2026.html` (guía imprimible) — ambos retirados el 13 jul 2026, ver
checkpoint arriba.

## CHECKPOINT — 2026-06-16 · Rediseño visual élite

Hero oscuro midnight `#0C1A2E` full-bleed, strip de métricas animadas, sistema de diseño con
clases reutilizables (`.section-header`, `.metrics-strip`, `.btn-primary-dark`). Cierre del
evento Charla IA con función `reenviarConfirmacionListaEspera()` para post-evento.

## CHECKPOINT — junio 2026 · Fase 1 Quick Wins + Sistema de Registro de Eventos

Quick wins: Google Fonts no bloqueante, contraste hero WCAG AA, `aria-current` + favicon en
todas las páginas, cobertura mobile responsiva, acordeón CTE semántico. Sistema de Registro de
Eventos para la Conferencia IA 2026: formulario con autocompletado CCT, Apps Script con folios
y cupos por sector, check-in por PIN/QR (`asistencia.html`), manual interno
(`docs/manual-sistema-registro.html`).

---

*Checkpoints anteriores a junio 2026 no se reconstruyeron retroactivamente — ver `git log` si
hace falta ese historial.*
